type person
  name
  partner

pub play =
  couple = build
    ada = person "ada" none
    bob = person "bob" ada
    ada.partner = bob
    ada
  print "{couple.name} and {(couple.partner).name}"
  >> print "back where we started: {((couple.partner).partner).name}"
