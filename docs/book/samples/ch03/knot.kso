type person
  name
  partner

build
  ada = person "ada" _
  bob = person "bob" ada
  ada.partner = bob

print "{ada.name} and {ada.partner.name}"
  .> (_ -> print "back where we started: {ada.partner.partner.name}")
