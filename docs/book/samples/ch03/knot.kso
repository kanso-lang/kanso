type person
  name
  partner

build
  ada = person "ada" none
  bob = person "bob" ada
  ada.partner = bob

print "{ada.name} and {ada.partner.name}"
>> print "back where we started: {ada.partner.partner.name}"
