xs = [3 1 2]
print "map {map xs (n -> n * 10)}"
  >> print "filter {filter xs (n -> n > 1)}"
  >> print "sort {sort xs}"
  >> print "sum {sum xs}"
  >> print "push {push xs 4}"
  >> print "at {xs[2]}"
  >> print "length {length xs}"
  >> print "slice {slice xs 1 2}"
  >> print "concat {concat xs [9]}"
