import "./using"
import "std/testing"

ok = decode "\{\"name\": \"kanso\", \"tags\": [\"fast\", \"simple\"]}"
bad = decode "\{\"name\": nope}"
print (describe ok)
  .> (_ -> print "recoded: {encode ok}")
  .> (_ -> print (testing/when_failed bad told))
