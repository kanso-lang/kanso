pub fn describe v
  "decoded: {v}"

# The reason, after a licensed foreign party has separated it from the
# failure. An arm here naming this module's own err would never match: the
# decoder that raised it lives in this module too.
pub fn told (parse_failure p reason)
  "parse failed at byte {p}: {reason}"
