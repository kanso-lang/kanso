import "./lit"
import "./report"

print (show (decode_literal "true"))
  .> (_ -> print (show (decode_literal "null")))
  .> (_ -> print (show (decode_literal "nope")))
  .> (_ -> print (show (decode_literal "?")))
