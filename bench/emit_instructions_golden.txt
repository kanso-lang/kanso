# WHAT THE COMPILER SPENDS EMITTING, counted at the frame that does it.
#
# `kanso::codegen::emit_ir` inclusive, over `kanso build pkg/codegen_corpus`
# in the staged box, read by scripts/gates/emit_instructions.sh.
#
# WHY THE ROW EXISTS. The three `kanso check` rows stop before codegen. The
# two codegen rows count the child tree — the clang driver, the convention
# probe's clang, `clang -cc1` and `ld` — with kanso's own process excluded,
# because it waits for a child and a loop whose iteration count belongs to the
# scheduler is external state. Between the two sat the work this project
# wrote, and nothing counted it.
#
# WHY THE ANCHOR IS EXACT, measured two ways on 2026-09-17, on this project's
# container:
#
#   the release path, four profiles under `--trace-children=yes` -- two
#   readings of the shipped binary and two of a probe binary whose
#   `pid_tag_of` returns a constant -- every one at 394,910,642, byte for
#   byte, while the process around it moved 233 and then 112;
#
#   this gate's own command, three times, each from a freshly staged and
#   warmed box -- every one at 394,912,504.
#
# The two numbers differ by 1,862 because they are two commands: a different
# tier, and valgrind instrumenting child spawns in the first. That is the
# reason a row is one command and not a family of them. What both say is the
# thing worth having: repeat the command and the frame does not move.
#
# Both numbers are a CONTAINER's and are written here as evidence, never as
# the row: only CI's numbers may be recorded, which is what
# scripts/gates/measured_on.sh exists to enforce.
#
# The frame is 92.78% of what kanso's own process spends on a release build,
# so what the codegen rows' exclusion drops is the wait and very little else.
#
# HOW IT MOVES. Any change to the emitter moves it, and so does a change to
# the program it is emitting — `pkg/codegen_corpus` is the subject and its own
# size is part of the row. A change under `lib/` moves it too, because
# `lib/*.kso` is `include_str!`'d into the compiler.
#
# THE VALUE BELOW IS A PLACEHOLDER AND NO HOST WILL EVER COMPARE AGAINST IT.
# There is no `measured-on` line, so scripts/gates/measured_on.sh refuses every
# host: a container stops, and CI measures, prints and fails without comparing.
# The number is this container's, and it is here because
# tests/every_compile_vein_row_has_a_direction.rs asks every compile vein for
# exactly one row and an empty file is not that shape. The first CI sitting
# replaces it and writes the measured-on line under it; until then the row is
# a shape rather than a claim.
#
# 2026-09-17, the first sitting (kanso#1470). CI's reading, taken on the
# runner, anchored at `codegen::emit_ir` inclusive. 382,212,543.
# measured-on glibc=2.39-0ubuntu8.9 rustc=1.98.1
#
# This is the compiler's own half of a build -- what the two codegen rows
# stopped counting when kanso's process was excluded from them. This container
# read 394,912,504 from three stagings and 394,910,642 on four release-path
# profiles; the runner reads 12.6 million lower, which is the host difference
# this golden's header said no box could compare away.
#
# The value 382,309,867 stood here until this line was written and was NOT
# CI's, whatever the note beside it said. The file carried no measured-on
# line, so the gate could not tell one host's number from another's and
# refused to compare at all. The row above is the vein's first reading on a
# runner and the line under it says which runner.
# 2026-09-17, kanso#1478 on the tree merged with main 5e256ce0, CI's sitting.
# 382,212,543 -> 60,197,743, a FALL of 322,014,800 -- 84.25%. WORK, and the
# largest single move this vein has recorded. Five passes that answered a
# whole-program question once per name answer it from an index now, and this
# row counts exactly the phase where those passes run. A second count in the
# same job read 60,197,743.
#
# The C toolchain did not move with it. `codegen_instructions_dev` and
# `codegen_instructions_release` both passed unchanged in the same sitting, and
# the dev gate's notice reads codegen_dev_kanso_excluded=85,091,397 against
# main's 407,173,801 -- kanso's own process on the codegen corpus falls 79.1%
# while clang and ld count the same to the instruction. That is the emitted IR
# being byte-identical, measured rather than asserted.
#
# 2026-09-17, merged with main 2a0cec7c. Every row below is MAIN'S, carried
# forward. This branch measured against a base that kanso#1478 (emitted code
# -84.25%) and kanso#1493 (start-up -22.14%) have both moved since, so the
# numbers it took are against a tree that no longer exists -- its emit row read
# 382,216,372 where main now reads 60,197,743. Carrying main's forward gives
# the gate one number to fail against rather than none, and makes CI's diff
# read exactly what this branch does to today's main. The merged sitting is
# CI's to take.
#
# 2026-09-17, kanso#1486 on the tree merged with main 2a0cec7c, CI's sitting.
# emit_instructions 60,197,743 -> 60,200,209, a RISE of 2,466 (+0.0041%).
# LAYOUT, 41 parts per million.
# 2026-09-18, kanso#1507 on the tree merged with main, CI's sitting. 60,197,743 ->
# 60,197,827, a RISE of 84 (+0.00014%). LAYOUT. This branch adds a
# `match std::env::var("KANSO_LTO_JOBS")` to `release_clang` in src/main.rs and
# `KANSO_LTO_JOBS=1` to three `env -i` lines in a gate script. src/main.rs is
# the compiler, so its bytes and the layout under them move, and every row that
# runs the compiler moves with them. Seven rows moved here and all seven are
# under five ten-thousandths of a per cent, with mixed signs -- which is what a
# shifted binary looks like and not what work looks like.
#
# 2026-09-18, re-merged after kanso#1486 landed underneath. THE VALUE BELOW IS
# MAIN'S, carried forward. kanso#1507's own sitting on the pre-merge tree read
# 60,197,827 and is recorded above; it was taken on a base that did not
# carry kanso#1486's 1.17% fall across the three check routes, so it describes
# a tree that no longer exists. Carrying main's forward gives the gate one
# number to fail against rather than none, and makes CI's diff read exactly
# what this branch does to today's main.
#
# 2026-09-18, kanso#1507 on the tree merged with kanso#1486, CI's sitting.
# 60,200,209 -> 60,196,725, a FALL of 3,484 (-0.00579%). LAYOUT. The value
# above was main's, carried forward because this branch's own reading predated
# kanso#1486; this is the merged tree measured. src/main.rs gained a four-line
# `match` on an environment variable and src/main.rs is the compiler, so its
# bytes move and every row that runs the compiler moves with them. Six rows
# moved and the two codegen rows did not: they AGREED with this branch's own
# readings to the instruction, across two CI jobs on two different trees,
# which is what `-Wl,-plugin-opt=jobs=1` bought.
#
# 2026-09-18, kanso#1502 on the tree merged with main, CI's sitting.
# 60,196,725 -> 60,206,729, a RISE of 10,004 (+0.0166%).
# LAYOUT. These rows carry runtime.c's bytes rather than compiling it, and
# every binary's `.text` grew 32 bytes on this change; the moves are all under
# two hundredths of a per cent.
# 2026-09-18, kanso#1510 (kanso#1484 rebuilt) on the tree merged with main,
# CI's sitting. 60,196,725 -> 59,636,068, a FALL of 560,657 (-0.9314%).
# The emitter's own share of the same saving: it asked the same question and
# now reads a constant.
# 2026-09-18, kanso#1509 (kanso#1480 rebuilt) on the tree merged with main,
# CI's sitting. 60,196,725 -> 52,115,454, a FALL of 8,081,271 (-13.4248%).
# THIS ONE IS THE CHANGE. `emit_instructions` anchors at `codegen::emit_ir`
# inclusive, and the linearity analysis was built three times inside it --
# once each for in_place_pushes, reusable_records and string_builders, on
# consecutive lines. Building it once takes 8,081,271 instructions out of the
# emitter, 13.42% of what emitting costs.
#
# 2026-09-18, kanso#1511 on the tree merged with main, CI's sitting.
# 52,115,454 -> 52,119,322, a RISE of 3,868 (+0.0074%).
# LAYOUT. `codegen::emit_ir` does not run the interpreter at all; the
# predicate it now shares with the oracle was already in this binary.
# 2026-09-18, re-merged onto main after kanso#1509. THE VALUE BELOW IS MAIN'S,
# carried forward: kanso#1509 moved this row on its own and what this branch
# adds on top of it has not been measured, so the merged reading is CI's.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1509,
# CI's sitting. 52,115,454 -> 52,125,468, a RISE of 10,014 (+0.0192%).
# LAYOUT. The two divisions this branch merges live in float rendering,
# which none of these routes runs: three are `kanso check`, `emit_ir`
# stops before the backend, and the interpreted start-up links the runtime
# without rendering a float. What moved is where the code sits. The second
# reading in the same job matched the first to the instruction.
# 2026-09-18, kanso#1510 (kanso#1484 rebuilt) on the tree merged with main
# after kanso#1509, CI's sitting. 52,115,454 -> 51,554,407, a FALL of
# 561,047 (-1.0765%). This is a route the removed derivation runs, and the saving lands here.
# The second reading in the same job matched the first to the instruction.
#
# 2026-09-18, re-merged onto main after kanso#1510. THE VALUE BELOW IS MAIN'S,
# carried forward: kanso#1510 moved this row and what this branch adds on top
# has not been measured, so the merged reading is CI's.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1510,
# CI's sitting. 51,554,407 -> 51,542,361, a FALL of 12,046
# (-0.0234%). LAYOUT. The two divisions this branch merges are in float
# rendering and none of these routes renders a float. All six fell, where
# the sitting before kanso#1510 landed had the same six rising: one layout
# term, opposite sign, which is what a shifted binary looks like.
# carried forward: kanso#1510 moved this row on its own and what this branch
# adds on top of it has not been measured, so the merged reading is CI's.
#
# 2026-09-18, kanso#1511 on the tree merged with main after kanso#1510,
# CI's sitting. 51,554,407 -> 51,543,885, a FALL of 10,522
# (-0.0204%). LAYOUT. `codegen::emit_ir` does not run the interpreter at all.
#
#
# 2026-09-18, re-merged onto main after kanso#1516. THE VALUE BELOW IS MAIN'S,
# carried forward; the merged reading is CI's.
# 2026-09-18, kanso#1516 on the tree merged with main, CI's sitting.
# 51,546,788 -> 51,554,663, a RISE of 7,875 (+0.0153%).
# LAYOUT. src/eval.rs is the compiler, so its bytes move and every row that
# runs the compiler moves with them; none of these routes evaluates a name,
# because all four stop before the interpreter and the fifth is the emitter.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1516,
# CI's sitting. 51,554,663 -> 51,543,783, a FALL of
# 10,880 (-0.0211%).
# LAYOUT. src/runtime.c is include_str!'d into the compiler (src/main.rs)
# and digested into a constant (src/hash.rs), so editing it moves 450,100
# bytes of the compiler's own .rodata and every route that runs the compiler
# moves with it. None of these five routes executes the runtime: three stop
# before codegen, the fourth writes IR, the fifth starts the interpreter.
# ^ SUPERSEDED: that reading was taken before kanso#1517 landed under this
#   branch. THE VALUE BELOW IS MAIN'S, carried forward; the merged
#   reading is CI's.
#
# 2026-09-18, kanso#1517 on the tree merged with kanso#1516, CI's sitting.
# 51,554,663 -> 51,451,897, a FALL of 102,766 (-0.1993%).
# LAYOUT. `codegen::emit_ir` does not run the interpreter.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1517,
# CI's sitting. 51,451,897 -> 51,457,106, a RISE of
# 5,209 (+0.0101240%).
# LAYOUT. src/runtime.c is include_str!'d into the compiler (src/main.rs)
# and digested into a constant (src/hash.rs), so editing it moves 450,100
# bytes of the compiler's own .rodata. None of these routes executes the
# runtime: three stop before codegen, the fourth writes IR, the fifth
# starts the interpreter.
# ^ SUPERSEDED: that reading was taken before kanso#1520 landed under this
#   branch. THE VALUE BELOW IS MAIN'S, carried forward; the merged
#   reading is CI's.
#
# 2026-09-18, kanso#1520 against main at kanso#1519, CI's sitting.
# 51,451,897 -> 51,456,464, a RISE of 4,567 (+0.0089%).
# LAYOUT. This branch changes how a shared vector is copied in the
# INTERPRETER; none of these routes runs it. src/eval.rs is the compiler,
# so its bytes move and every route that runs the compiler moves with them.
#
# 2026-09-18, kanso#1533, CI's sitting. 51,630,538 -> 51,617,476,
# a FALL of 13,062 (-0.0253%).
# Layout, the same way.
# 2026-09-18, kanso#1543. 51,617,476 -> 51,619,793, +2,317. Layout: this gate
# anchors on `codegen::emit_ir` and the diff is in `eval`. Counted twice in
# the job, identical both times.
# 2026-09-19, kanso#1502 on the tree merged after kanso#1543 and kanso#1547,
# CI's sitting. emit_instructions 51,619,793 -> 51,616,373, a move of -3,420.
#
# 2026-09-19, kanso#1502 re-merged onto main after kanso#1552 and kanso#1553,
# CI's sitting on run 35419502516. emit_instructions 51,616,373 -> 51,618,058, +1,685 (+0.0033%).
# `codegen::emit_ir` inclusive, in the compiler's own process. Layout again:
# the emitter writes the same IR either way. `emit_again` read 51,618,058.
# AND memcmp IS PRELOADED, 2026-09-23, under the same rule.
#
# Every run this gate counts preloads scripts/gates/address_blind/compare.c
# over libc's memcmp and bcmp. glibc's __memcmp_avx2_movbe asks, for a
# comparison under 32 bytes, whether (s1 | s2) & 4095 lies within 32 bytes of
# a page end, and takes a longer branch when it does. So comparing the same
# bytes cost a different number of instructions depending on where the linker
# put one operand and the allocator put the other. kanso#1561 added forty lines
# to src/runtime.c, which the compiler embeds. On one host that moved 478 of the
# compile row's comparisons onto the page-cross branch with every call count
# unchanged, and that row rose 2,688. With the preload the two trees read
# 36,656,738 each, and the two processes' PROGRAM TOTALS agreed to the
# instruction.
#
# The replacement's cost depends only on the length and where the first
# difference falls: a head and a tail that may overlap up to 32 bytes, 32-byte
# AVX2 steps above that. Its first version compared eight bytes a step and cost
# three times libc's bcmp per call on the interpreted run; this one costs about
# what libc's fast path does. The value below was measured with it and is not
# comparable with any reading taken before 2026-09-23.
#
# AND memcpy AND memmove, the same day. With memcmp preloaded, merging
# kanso#1561's runtime under this branch still moved the compile, entry and
# library rows by 44, 122 and 122, and in each table the whole move was
# __memcpy_avx_unaligned_erms. Its path for a large copy depends partly on the
# distance between destination and source: 1,500 bytes cost 177 instructions at
# one distance and 179 at another. scripts/gates/address_blind/copy.c replaces
# both and chooses every branch by the length: a head and a tail that may
# overlap up to 128 bytes, 128-byte AVX2 steps above that. Copies libc makes internally, a thousand-odd instructions a run, cannot
# be reached by a preload and are still counted as libc does them.
#
emit_instructions=45312275
