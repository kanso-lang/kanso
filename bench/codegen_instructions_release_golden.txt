# Retired instructions for a whole `kanso build` of bench/codegen_corpus on the
# production tier: the build that ships, where the compiler is allowed to spend
# to make the program fast.
#
# THE COMPILE ROWS STOP BEFORE CODEGEN. compile_instructions, entry_instructions
# and library_instructions all count `kanso check`, which answers whether a
# program is well formed and then stops. Everything after that — the emitter
# writing LLVM IR, the clang driver, clang -cc1, the linker and the linker's own
# child — was uncounted, and it is the larger half of what a build costs. This
# row counts the whole tree: five processes under `--trace-children`, their
# summary lines summed.
#
# WHY TWO TIERS. The two builds are different programs with different jobs, and
# a single number over both would let one pay for the other. Ruled 2026-09-16.
#
# MEASURED IN A BOX, for the reason the other compile rows are: the count moves
# with the length of the directory the compiler runs in. scripts/gates/
# codegen_box.sh stages the package one level down, as pkg/codegen_corpus, so
# that `kanso build` does not find a directory beside its argument's stem and
# refuse — a refusal that still emits the IR first, so it looks like a build
# and counts two processes instead of five. The gate refuses a tree shorter
# than four.
#
# THIS ROW DEPENDS ON CLANG, which the other compile rows do not: most of the
# tree is the C toolchain, so the measured-on line names it beside glibc and
# rustc.
#
# 2026-09-17, the first sitting (kanso#1470). CI's reading; this container
# read 7,684,965,191 on a different clang and a different machine, which is not
# comparable. Five processes, and the second reading in the same job agreed
# with the first. The release tier is 1.30x the development tier here, which is
# the whole reason the ruling asked for two rows rather than one.
# measured-on glibc=2.39-0ubuntu8.9 clang=19.1.1 rustc=1.98.1
#
# 2026-09-17 (kanso#1470): NOT WRITTEN, and the value below is the one this
# row has always held. CI counted the release tier twice in one job and read
# 7,239,553,333 and then 7,239,550,228 -- VERDICT (2), a reproduction failure,
# 3,105 apart on a tree of about 7.24 billion. The dev tier, counted across
# the same pair of runs on the same box, came back byte-identical at
# 1,003,426,243, so the box and the box's staging are not the variable and
# `-O3 -flto` is.
#
# A row this vein reads by an exact compare holds one value, so writing either
# face would be writing a coin. The gate now prices every process it names
# (`codegen_procs_release first=[...] again=[...]`), which is the one thing the
# last sitting could not say: five processes moved by 3,105 between them and
# the log named none of them. The next sitting says which, and the choice --
# normalize what moves it, or exclude it and name the exclusion in this header
# under the 2026-09-15 rule -- waits on that.
#
# 2026-09-17, later the same day: the instrument was run on this container
# instead of waiting for CI, and it names the process on the first try.
#
#   reading a   kanso=425,656,322  clang=32,184,722  clang=31,624,903
#               clang=1,617,293,611  ld=5,145,605,822   total 7,252,365,380
#   reading b   kanso=425,656,555  clang=32,184,722  clang=31,624,903
#               clang=1,617,293,611  ld=5,145,605,822   total 7,252,365,613
#
# Every child is byte-identical and kanso's own process moves 233. Inside it,
# two frames of 1,346 differ: `kanso::build` +189 and `__memcmp_avx2_movbe`
# +44.
#
# The memcmp is the pid. `pid_tag()` puts the process id into the names of the
# .ll, the staging file and the runtime object; its WIDTH was pinned at seven
# digits in an earlier round, and that left the CONTENT free, so a comparison
# over those paths stops at a different byte each run. A probe binary with
# `pid_tag_of` returning a constant was built and run twice: memcmp came back
# byte-identical and the total moved 112 instead of 233.
#
# What the probe leaves is 112 instructions in ONE frame of 1,346 --
# `kanso::build` itself, self cost, with every callee byte-identical. That is
# an inlined loop inside `build` whose iteration count is not the compiler's
# to choose. The dev tier, the same code against a child that finishes seven
# times sooner, reproduces exactly; the release tier does not. Nothing here
# has been shown to be the compiler's work.
#
# So the choice this header was waiting on is now a choice between: making the
# temp names carry no pid (which is worth 121 of the 233 and is a fix worth
# making on its own), and what to do about a row whose subject includes a
# process that waits for another process. Counting the children alone, or
# giving kanso's own half its own row, are the two shapes that would leave
# every counted thing deterministic.
#
# 2026-09-17, later: the exclusion is built. The row is the child tree --
# three `clang` processes and `ld`, every one of which came back byte for
# byte across two readings -- and kanso's own process is out of it, named in
# the gate's header with the two measurements that put it there. The value
# below is the old whole-tree number and stands only so the gate has one
# value to fail against; the first sitting under the new shape writes it.
#
# 2026-09-17, CI's first sitting. 6,826,827,769, with
# `codegen_release_again` reading 6,826,827,769 -- deterministic in one job,
# like the dev tier beside it.
# #
# The release tier is 11.5x the development tier, which is what `-O3 -flto`
# against `-O0` costs and the reason the 2026-09-16 gavel put the two on
# different sides: one is paid once per release and the other between a
# keystroke and an answer. The container's placeholder read 12,107,507,377,
# pre-exclusion and not comparable.
# 2026-09-17, kanso#1493 on the tree merged with main 5e256ce0, CI's sitting.
# 6,826,827,769 -> 6,826,829,520, a RISE of 1,751 -- 256 parts per billion, on
# a row whose `-O3 -flto` tier is the largest number this tree counts. The
# same C-toolchain layout the dev row shows, at the scale that tier runs at.
# A second count in the same job read 6,826,829,520.
#
# 2026-09-18, kanso#1507. THE THREAD COUNT IS PINNED NOW, and the value below
# is main's, carried forward until CI takes a sitting under the new shape.
#
# kanso#1487 excluded kanso's own process from this row and certified what was
# left -- three `clang` processes and `ld` -- deterministic across two
# readings. It drew two faces again within the day: 6,838,057,046 then
# 6,838,057,035 in one job, on one binary, eleven apart, with all three clang
# children byte for byte and the whole delta in `ld`.
#
# `ld` splits LTO codegen across threads. callgrind counts every thread, and
# how the work lands is the scheduler's to decide rather than the input's.
# Reproduced on a container, two links of byte-identical bitcode, both clang
# children byte for byte every time:
#
#   different output path, plugin picks:  20,565,047,254  20,565,047,243   -11
#   same output path, plugin picks:       20,565,047,241  20,565,049,584 +2,343
#   same output path, jobs=1:             20,574,502,681  20,574,502,681      0
#
# The magnitude changes between pairs, which is what ruled out the output path
# and the pid before it -- a fixed string would cost a fixed number. Only the
# thread count explains a delta that is eleven one pair and 2,343 the next.
#
# So the gate asks for one thread, per the 2026-09-15 rule that external state
# is put into a known state rather than explained afterwards. It is not the
# default: a user's release build has no row to keep and every reason to use
# its cores.
#
# THE ABSOLUTE VALUE MOVES and nothing measured before this is comparable with
# anything measured after. Single-threaded LTO partitions the work differently
# -- about 0.046% higher on the container's pair. Round one is deliberately red
# and CI's own reading replaces this line.
#
# 2026-09-18, kanso#1507 on the tree merged with main, CI's sitting.
# 6,826,829,520 -> 6,822,651,561, a FALL of 4,177,959 (-0.0612%). THIS ONE IS
# NOT LAYOUT AND IT IS NOT A SAVING EITHER: the row counts a different thing
# now. `-Wl,-plugin-opt=jobs=1` makes GNU ld's LLVM plugin run codegen on one
# thread, and callgrind counts every thread, so the total moves with the
# partitioning. Nothing measured before this change is comparable with anything
# measured after it.
#
# WHAT IT BOUGHT, from CI's own per-process notice. The three clang processes
# read 32,265,497 / 31,732,189 / 1,617,286,141 on the first count and the same
# three numbers on the second -- byte-identical, where before this they were
# the drifting half of the row. Reproduced on the container as well: two
# complete pipeline runs, staged and warmed exactly as the gate does it, agree
# to the instruction on all four counted processes, and they agree even though
# the temp-file names differ between the runs (`kanso_pn_probe_0020122.ll`
# against `..._0021879.ll`, `codegen_corpus-89a40b.o` against
# `...-0c759c.o`), which rules the paths out as the term.
#
# WHAT IS LEFT, and it halts this vein on CI rather than here. CI's two
# readings differ by 11, all of it inside `ld`: 5,141,367,734 then
# 5,141,367,745. The container cannot reproduce it -- `ld` reads
# 5,146,602,703 twice across the two pipeline runs -- so the residue is
# something the runner has and this box does not. The gate now diffs the two
# readings frame by frame when they disagree, so the next job that sees it
# names the frame instead of the magnitude.
#
# 2026-09-18, kanso#1502 on the tree merged with main, CI's sitting.
# 6,822,651,561 -> 6,824,410,196, a RISE of 1,758,635 (+0.0258%).
# The release tier is the only row that COMPILES src/runtime.c rather than
# carrying its bytes, so a change to the digit loop is felt here: 1,758,635
# instructions of clang and ld, 0.026%. `codegen_release_again` read
# 6,824,410,196, the same number in the same job, which is kanso#1507's
# `-Wl,-plugin-opt=jobs=1` holding on another tree that changes runtime.c.
# 2026-09-18, THE GATE NOW CLEARS ITS OUTPUT PATH BEFORE EVERY BUILD, so this
# row measures a different thing and the value below is the OLD one, left in
# place for CI to move. `ld` reads whatever is already at `-o`, and what it
# finds there changes the count. On the container, one binary, one corpus, the
# same `ld` command each time, varying only the output path's state:
#
#     absent            5,163,341,031   (twice, to the instruction)
#     an empty file     5,163,341,036   (twice, +5)
#     100 bytes         5,163,343,385
#     5 MB              5,163,343,385
#     the real binary   5,163,343,385   (+2,354 over absent)
#
# Three groups, each internally identical, and size stops mattering once the
# file is non-empty. The gate read the third group by accident -- the warm-up
# left its binary at `-o` -- and now reads the first by construction. THIS IS
# A MEASUREMENT CHANGE AND NOT A COMPILER SAVING: nothing about the compiler
# moved, and the fall this produces is the gate no longer counting `ld`'s
# inspection of a file left over from the previous build.
#
# 2026-09-18, CI's own sitting for the cleared gate. 6,822,651,561 -> 6,824,133,280,
# a RISE of 1,481,719 (+0.0217%). RE-BASE, not a change in the
# compiler: nothing about the compiler moved and both readings in the job
# agreed to the instruction, which this row had not managed before.
#
# The container reads the release row the OTHER WAY, 412,910 DOWN, with
# the whole difference inside `ld` and all three clang processes
# byte-identical. Two hosts, opposite signs; nothing here predicts the
# runner's, and the size of this term is not a property of the change.
#
# 2026-09-18, A CORRECTION TO THE 2026-09-17 ENTRY ABOVE. That entry says the
# row is "three `clang` processes and `ld`, every one of which came back byte
# for byte across two readings". That is true of clang and is NOT true of `ld`.
# Three cost-goldens jobs on 2026-09-18 halted this vein for a reproduction
# failure, each with every clang process byte-identical and the whole
# difference inside `ld`:
#
#   kanso#1504   5,160,407,609 then 5,160,407,598   -11
#                probes 1,816,463 -> 1,816,452
#   kanso#1502   5,139,582,528 then 5,139,582,517   -11
#                probes 1,822,415 -> 1,822,404
#   kanso#1537   6,824,133,291 then 6,824,133,280   -11
#                probes 1,819,373 -> 1,819,362
#
# Every one of them lands entirely in `llvm::StringMapImpl::LookupBucketFor`,
# which is the frame this header's own analysis asked the gate to name, and
# every one of them is exactly eleven. Three different probe counts, one
# residue.
#
# The 2026-09-17 sentence stands as what the two readings it had showed. What
# it does not support is the general claim, and the general claim is what a
# later reader takes from it. kanso#1538 and kanso#1504's SECOND job both
# reproduced, so the draw is a property of the job rather than of any branch's
# diff -- one branch has now been on both sides of it.
#
# This corrects the record only. Whether to pin the release tier is a decision
# in design/pending-gavels.md and is not settled here.
# 2026-09-18, THE GATE NOW PINS THE LTO OBJECT'S NAME, so this row measures a
# different link and the value below is the OLD one, left for CI to move.
# clang wrote the object to `/tmp/<stem>-XXXXXX.o` with fresh hex every run and
# `ld`'s plugin probed a `StringMap` with that path. Twenty-two names, one
# binary, one corpus, every other input held:
#
#     twenty names          5,163,341,031
#     `4b8c1a`, `fedcba`    5,163,341,042
#
# Eleven apart, deterministic per name, about one name in eleven. That is the
# eleven this row has disagreed with ITSELF by since kanso#1507 pinned the
# plugin's threads, and kanso#1502's own job drew both buckets in one run:
# 6,820,866,344 then 6,820,866,355.
#
# `-save-temps=obj` derives the name from the input instead. On the container
# the whole pipeline then read 6,811,830,244 twice against 6,813,182,505 three
# times without it, so the flag RE-BASES this row by about 1.35 million as well
# as steadying it. A MEASUREMENT CHANGE, not a compiler saving.
# 2026-09-19, kanso#1513 on the tree merged after kanso#1540, kanso#1541,
# kanso#1544 and kanso#1546, CI's sitting. codegen_instructions_release 6,824,133,280 -> 6,833,786,335,
# a move of +9,653,055.
#
# THIS IS THE FIXED-TEMP PIN RE-BASING THE ROW, and every compile-side row
# moves with it because the pin changes src/main.rs: the binary's layout moves,
# and these rows track layout. A re-basing is not a regression and the
# objective's trend gate is told which it is by bench/objective_sources.txt.
#
# EVERY ROW IN THIS JOB REPRODUCED ON A SECOND READING, including this one.
# That is the point of the change rather than a detail of it: before the pin,
# codegen_instructions_release could draw two values eleven apart on one binary
# -- kanso#1502's job in this same sitting drew 6,820,866,355 and then
# 6,820,866,344 -- and the name the pin fixes is what decided which.
#
# 2026-09-19, kanso#1502 re-merged onto main after kanso#1552 and kanso#1553,
# CI's sitting on run 35419502516. codegen_instructions_release 6,833,786,335 -> 6,825,827,822, a FALL of
# 7,958,513 (-0.1165%).
# THIS ROW GOES DOWN, which is unusual here and is the point: the release
# tier is the only row that COMPILES src/runtime.c rather than carrying its
# bytes, and this branch removes two divisions from the float renderer. Less
# source at `-O3 -flto` is less work for clang and ld.
#
# `codegen_release_again` read 6,825,827,822, so kanso#1507's plugin pin and
# kanso#1513's fixed-temp name are both holding on a tree that changes
# runtime.c.
#
# 2026-09-24, kanso#1605 (number span, beat classified once, shipped std
# modules skip their check), CI's row: codegen_instructions_release
# 1,614,704,366 -> 1,614,369,990, a FALL of 334,376 (-0.0207%).
#
# 2026-09-24, kanso#1611 (carrying kanso#1605, #1608, #1609 and #1610 over
# main with kanso#1604), CI's row: codegen_instructions_release 1,614,369,990
# -> 1,613,895,452, a FALL of 474,538 (-0.0294%).
#
# 2026-09-24 (kanso#1613): a byte view read in its frame keeps its header
# there. codegen_instructions_release 1,613,895,452 -> 1,613,900,206, a RISE
# of 4,754 (+0.0003%).
#
# 2026-09-24 (kanso#1614): the ordered view, the in-place builder grow and the
# mark walk. codegen_instructions_release 1,613,900,206 -> 1,614,602,673, a
# RISE of 702,467 (+0.0435%).
#
# 2026-09-24 (kanso#1619, carrying kanso#1614, #1615, #1617 and #1618).
# codegen_instructions_release 1,614,602,673 -> 1,614,559,051, a FALL of
# 43,622 (-0.0027%).
#
# 2026-09-24 (kanso#1620): an arm no value reaches is not emitted.
# codegen_instructions_release 1,614,559,051 -> 857,150,087, a FALL of
# 757,408,964 (-46.9112%).
#
# kanso#1621 at CI's sitting: dead cycles, unnamed strings, dead failure
# paths, reboxed words, the interpreter's argument loops and low byte.
# codegen_instructions_release 857,150,087 -> 713,520,095, a FALL of
# 143,629,992 (-16.7567%).
#
# kanso#1622 at CI's sitting: one overflow trap a function, blank lines by
# binary search, entries in one block, the beat mark inline, empty literals
# and ints rendered in place. codegen_instructions_release 713,520,095 ->
# 715,952,319, a RISE of 2,432,224 (+0.3409%).
#
# kanso#1623 at CI's sitting: tail cycles, direct calls and four runtime doors
# on preserve_none. codegen_instructions_release 715,952,319 -> 715,952,202, a
# FALL of 117 (-0.0000%).
#
# kanso#1624 at CI's sitting: an append that must grow asks first.
# codegen_instructions_release 715,952,202 -> 715,946,899, a FALL of 5,303
# (-0.0007%).
#
# kanso#1626 at CI's sitting: a fifth push takes eight slots.
# codegen_instructions_release 715,946,899 -> 715,946,959, a RISE of 60
# (+0.0000%).
#
# kanso#1627 at CI's sitting: an accumulator's buffer grows where it is.
# codegen_instructions_release 715,946,959 -> 715,866,504, a FALL of 80,455
# (-0.0112%).
#
# kanso#1630 at CI's sitting: a rewind with nothing to take back asks only
# whether the pointer moved. codegen_instructions_release 715,866,504 ->
# 715,981,278, a RISE of 114,774 (+0.0160%).
#
# kanso#1631 at CI's sitting: two byte appends in a row ask for room once.
# codegen_instructions_release 715,981,278 -> 715,986,547, a RISE of 5,269
# (+0.0007%).
#
# kanso#1633 at CI's sitting: a proven list is measured and indexed in place.
# codegen_instructions_release 715,986,547 -> 698,557,830, a FALL of
# 17,428,717 (-2.4342%).
#
# CI measured the literal word on main after kanso#1634, at 964f41e9.
# codegen_instructions_release 698,557,830 -> 698,399,695, a FALL of 158,135
# (-0.0226%).
#
# CI measured the carried tree at 913723b0. codegen_instructions_release
# 698,399,695 -> 698,405,052, a RISE of 5,357 (+0.0008%).
#
# CI measured the float search at a71892d9. codegen_instructions_release
# 698,405,052 -> 698,560,720, a RISE of 155,668 (+0.0223%).
#
# CI measured the four carried changes at cf18309e.
# codegen_instructions_release 698,560,720 -> 698,557,632, a fall of 3,088.
# codegen_instructions_release 698,560,720 -> 698,557,632, a FALL of 3,088
# (-0.0004%).
#
# CI measured the carried list seed at daac530d. codegen_instructions_release
# 698,557,632 -> 698,554,515. codegen_instructions_release 698,557,632 ->
# 698,554,515, a FALL of 3,117 (-0.0004%).
#
# CI measured the builder seed at 42d0b138. codegen_instructions_release
# 698,554,515 -> 698,599,960, a RISE of 45,445 (+0.0065%).
#
# CI measured the builder seed at 947d34ad, with the declare no longer written
# into programs that never call it. codegen_instructions_release 698,599,960
# -> 698,561,887, a FALL of 38,073 (-0.0054%).
#
# CI measured the half-step grow at 78bf6606. codegen_instructions_release
# 698,561,887 -> 698,561,899, a RISE of 12 (+0.0000%).
codegen_instructions_release=698561899
