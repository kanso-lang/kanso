# What an INTERPRETED run costs, counted rather than timed.
#
# `kanso run --interp` on bench/interp_corpus, anchored at
# `run_interpreted_on_stack` -- the entry of the thread the interpreter runs
# on. `kanso::main` is the wrong anchor here and the first sitting says why:
# it read 48,026,664 against 2,700,128,254 for the whole process, because
# `kanso run --interp` pins a one-gigabyte stack and hands the program to a
# thread of its own, so the main thread holds the front end and 1.8% of the
# run.
#
# THE CORPUS BUILDS ITS OWN INPUT. It interpolates a document of 220 objects
# and decodes it six times, so the workload is a property of the corpus alone.
# Every other benchmark in this tree reads bench/large.json, and a row that
# reads a file is a row that moves when the file does.
#
# WHY IT EXISTS. Clay ruled on 2026-09-16 that the objective becomes a
# development welfare, a production welfare and a meta over them, and named the
# interpreted engine's own order: "start-time is vastly more important than
# speed which is more important than memory usage." This is the middle of the
# three. It is an exact vein of its own and not yet an objective term, the way
# `.text` is under the 2026-09-05 ruling.
#
# 2026-09-16 (the vein opens, and it opened onto a reproduction failure):
# two runs of one binary over one corpus read 2,651,460,189 and 2,648,375,305,
# 3,084,884 apart, where the front end's own anchor read 48,026,664 twice. The
# interpreter's tables -- `fns`, `types`, `knots`, the typeset cache and the
# two cycle-guard sets -- were `std::collections`, whose `RandomState` draws a
# fresh key from the OS on every process, so each run probed them in a
# different order and did a different amount of work reaching the same answer.
# That is exactly the defect kanso#1449 cost three CI rounds to find on the
# compile path, still living in the one file the spec that catches it excused.
# The excuse was "the interpreter's own cost is not counted by any exact
# vein", and this file is that vein, so it is gone and src/eval.rs is spelled
# `crate::hash` now.
#
# It is a fall as well as a fix: three consecutive runs read 2,375,580,224,
# which is 275,879,965 below the first of the two disagreeing readings, a
# 10.40% fall. SipHash-1-3 was hashing the interpreter's every name lookup.
#
# The row below is THIS CONTAINER's, rustc 1.94.1 against the runner's 1.98.1,
# so the gate refuses to compare it here. Round one is deliberately red and
# CI's own reading is what stands.
# 2026-09-16 (CI's own first sitting): the row above was this container's
# 2,375,580,224 and the runner reads 2,324,888,431, 2.18% lower. The container
# reads about 1.2% HIGH on the three compile rows, so this row's divergence is
# the same sign and about twice the size -- an interpreted run is mostly the
# interpreter's own loop, where a compile is mostly the front end's, and the
# two do not have to move together between toolchains. CI's is the number.
#
# 2026-09-17, kanso#1462, round four. The interpreted run falls 3,920,499
# on the head merged with kanso#1464: the linearity analysis runs on the
# interpreter's compile path too, and indexing it is felt here. CI read
# 2,320,967,932 and then 2,320,967,932 again in the same job -- the six
# instructions this row used to move by were the allocator picking a
# random base address, which kanso#1466 took away.
#
# 2026-09-17, kanso#1462 on the tree merged with kanso#1472, CI's sitting. The
# row this branch is for: the interpreter hashes with the compiler's own seedless
# hash rather than against an attacker it does not have. A FALL of 142,311,375
# (-6.13%) against the value the merge carried forward.
#
# 2026-09-17, kanso#1462, same sitting. A FALL of 97,472 (-0.0045%), and this
# one is the change: the interpreted run is what this branch touches, and the
# fixed-seed hashing is what it does. The objective does not weigh this vein.
#
# 2026-09-17, kanso#1468 on the tree merged with kanso#1462. CI reads
# 2,178,796,919 against main's 2,178,559,085, a RISE of 237,834 -- 0.0109%.
# LAYOUT, and the profile says so rather than the reasoning alone. `kanso run
# --interp` returns from run_interpreted before codegen is reached
# (src/main.rs, the early return on both the run and play paths), so nothing
# this branch edits executes on this corpus. A callgrind profile of the
# corpus on the branch carries exactly one codegen frame, an instantiated
# prune_unnamed closure worth 284 instructions in the whole run; the other
# 237,550 are the compiler's bytes moving under an interpreter that never
# enters them. The objective does not weigh this vein.
#
# 2026-09-17, kanso#1478 as the one tip, on main 298636b7. CI reads
# 2,182,341,803 against main's 2,178,559,085, a RISE of 3,782,718 -- 0.174%.
# LAYOUT, and this one was measured rather than assumed, because 3.78 million
# is sixteen times the largest move this vein had shown before.
# #
# THE ROW EXCLUDES THE FRONT END. The gate anchors at
# `run_interpreted_on_stack`, the interpreter's own thread, so the 46.3
# million `kanso::main` spends parsing and checking this corpus is not in the
# number at all. A front-end change cannot move this row by working; it can
# only move it by moving the binary.
# #
# BOTH SIDES REPRODUCED on the container under rustc 1.98.1: main
# 2,178,694,946 and this head 2,182,308,031, a local delta of 3,613,085
# against CI's 3,782,718. The delta is spread evenly over every frame of the
# interpreter rather than sitting in one:
# #
#       eval           18,445,521,672 -> 18,479,680,323   +0.185%
#       call           11,157,959,351 -> 11,181,808,771   +0.214%
#       call_named     11,127,354,432 -> 11,151,189,142   +0.214%
#       eval_tail       9,866,786,211 ->  9,888,320,078   +0.218%
#       dispatch       10,516,297,101 -> 10,535,892,811   +0.186%
#       run_main        2,178,075,324 ->  2,181,688,409   +0.166%
# #
# A uniform fifth of a per cent across the whole interpreter is what a moved
# working set looks like; work concentrates in one frame and this does not.
# The size of it is the size of the perturbation: this head carries five
# source changes across src/codegen.rs, src/linear.rs and src/beat.rs, where
# every earlier reading on this vein came from a single file. The objective
# does not weigh this vein.
# 2026-09-17, kanso#1482 on the tree merged with kanso#1462. CI reads
# 2,178,502,266 against main's 2,178,559,085, a fall of 56,819 -- 0.0026%.
# LAYOUT: `kanso run --interp` returns from run_interpreted before codegen,
# so `FnEmit::write` never runs on this corpus. The size of the move is what
# this vein's layout term looks like -- kanso#1468 moves it 237,834 the other
# way in the same round from an edit in the same file.
#
# 2026-09-17, merged with main. Both branches moved this row on their own
# base, so the merged tree is reading neither number. The value below is
# main's, carried forward so the gate has one value to fail against
# rather than none; the merged sitting is CI's to take.
#
# 2026-09-17, kanso#1478 against main, CI's sitting. 2,178,502,266 ->
# 2,182,337,099, a RISE of 3,834,833 (+0.176%). LAYOUT, by construction: this row
# anchors at the interpreter's own thread, the front end is outside it, and every
# change in this run of five is in the front end or the emitter. A move of this
# size spread evenly over every frame is what a shifted working set looks like.
# 2026-09-17, kanso#1478 on the tree merged with main 5e256ce0, CI's sitting.
# 2,182,337,099 -> 2,182,307,043, a FALL of 30,056 (-0.0014%). This row is the
# one that reaches codegen, so some of the fall is the split's own emitting and
# some is layout; at 14 parts per million the two cannot be separated here and
# nothing is claimed about which. A second count in the same job read
# 2,182,307,043, so the binary is stable and the disagreement was with the
# golden.
#
# 2026-09-18, kanso#1505. THE PRINTED LINE IS EXCLUDED FROM THIS ROW, and the
# exclusion is named here per the 2026-09-15 rule. `interp_corpus` prints what
# it decoded; LineWriter runs `core::slice::memchr::memrchr` over the formatted
# bytes to find the last newline, and what that frame costs moves with the
# binary's LAYOUT rather than with anything the interpreter does.
#
# kanso#1487 measured the term and took it off the module, entry and library
# rows. It missed this one, and this row has drawn two faces ever since:
# kanso#1486's two sittings read 2,182,526,878 and 2,182,526,865 -- thirteen
# apart, on trees whose only difference was three goldens, a page and a log
# entry. Thirteen is the same thirteen those three rows were drawing.
#
# The gate prints `interp_printed=` as a notice, so the figure that was taken
# off is readable rather than only subtracted.
#
# THE VALUE BELOW IS MAIN'S AND ROUND ONE IS DELIBERATELY RED ON IT. Changing
# what the row counts moves its absolute value, so no number measured before
# this change is comparable with one measured after, and this container cannot
# run the gate -- the golden was taken under rustc 1.98.1 and the box runs
# 1.94.1. CI measures and its own reading replaces this line.
#
# 2026-09-18, kanso#1505, CI's sitting WITH THE LINE EXCLUDED.
# 2,182,307,043 -> 2,182,303,844. The difference is 3,199 and it is not a
# saving: it is the printed line's subtree, which this row stops counting.
# Larger than the module row's ~825 because `interp_corpus` prints the
# document it decoded rather than one summary line.
#
# NOTHING MEASURED BEFORE THIS CHANGE IS COMPARABLE WITH ANYTHING MEASURED
# AFTER IT. The row counts a different thing now. What it buys is that the two
# faces stop: the thirteen that kanso#1486 drew across two sittings lived in
# this subtree, and it is no longer in the number.
#
# 2026-09-18, kanso#1486 merged with main 073a4077. The value below is MAIN'S,
# carried forward. kanso#1505 changed what this row COUNTS -- the printed
# line's subtree is excluded now -- so the 2,182,526,878 this branch measured
# describes the old shape and is not comparable with anything taken under the
# new one. CI's sitting on the merged tree replaces this line, and the
# difference it reads is what this branch does to the row as it now stands.
#
# 2026-09-18, kanso#1486 merged with main 073a4077, CI's sitting under the new
# shape. 2,182,303,844 -> 2,182,523,679, a RISE of 219,835 (+0.0101%). LAYOUT.
# This branch edits src/check.rs, src/inline.rs and src/lib.rs, all of them
# front end, and the gate anchors at `run_interpreted_on_stack` -- the front
# end is outside the number by construction, so nothing this branch changes
# executes inside it. The size is the size this vein's layout term has shown
# before: kanso#1468 moved it 237,834 (0.0109%) from a single-file edit, and
# this is a three-file edit reading 219,835.
#
# AND THE OBJECTIVE DOES WEIGH THIS VEIN NOW. Every entry above this one closes
# by saying it does not, and each was true when it was written; the 2026-09-16
# gavel made the objective a development welfare, a production welfare and a
# meta over them, and `interp_instructions interp_instructions` has been a line
# of bench/objective_sources.txt ever since -- the middle of Clay's three for
# the interpreted engine. So a rise here costs welfare, and the sentence this
# file repeats three times is a stale one to carry forward.
# 2026-09-18, kanso#1507 on the tree merged with main, CI's sitting. 2,182,303,844 ->
# 2,182,293,088, a FALL of 10,756 (-0.00049%). LAYOUT. This branch adds a
# `match std::env::var("KANSO_LTO_JOBS")` to `release_clang` in src/main.rs and
# `KANSO_LTO_JOBS=1` to three `env -i` lines in a gate script. src/main.rs is
# the compiler, so its bytes and the layout under them move, and every row that
# runs the compiler moves with them. Seven rows moved here and all seven are
# under five ten-thousandths of a per cent, with mixed signs -- which is what a
# shifted binary looks like and not what work looks like.
#
# 2026-09-18, re-merged after kanso#1486 landed underneath. THE VALUE BELOW IS
# MAIN'S, carried forward. kanso#1507's own sitting on the pre-merge tree read
# 2,182,293,088 and is recorded above; it was taken on a base that did not
# carry kanso#1486's 1.17% fall across the three check routes, so it describes
# a tree that no longer exists. Carrying main's forward gives the gate one
# number to fail against rather than none, and makes CI's diff read exactly
# what this branch does to today's main.
#
# 2026-09-18, kanso#1507 on the tree merged with kanso#1486, CI's sitting.
# 2,182,523,679 -> 2,182,576,109, a RISE of 52,430 (+0.00240%). LAYOUT. The value
# above was main's, carried forward because this branch's own reading predated
# kanso#1486; this is the merged tree measured. src/main.rs gained a four-line
# `match` on an environment variable and src/main.rs is the compiler, so its
# bytes move and every row that runs the compiler moves with them. Six rows
# moved and the two codegen rows did not: they AGREED with this branch's own
# readings to the instruction, across two CI jobs on two different trees,
# which is what `-Wl,-plugin-opt=jobs=1` bought.
#
# 2026-09-18, kanso#1502 on the tree merged with main, CI's sitting.
# 2,182,576,109 -> 2,182,584,048, a RISE of 7,939 (+0.0004%).
# LAYOUT. These rows carry runtime.c's bytes rather than compiling it, and
# every binary's `.text` grew 32 bytes on this change; the moves are all under
# two hundredths of a per cent.
# 2026-09-18, kanso#1511 on the tree merged with main, CI's sitting.
# 2,182,576,109 -> 2,182,638,759, a RISE of 62,650 (+0.0029%).
# The interpreter now asks `codegen::knotted_constants` once per run, on the
# first constant cell it builds, and looks the name up in the answer at every
# later one. This benchmark is the interpreted run, so it pays both.
# 2026-09-18, kanso#1510 (kanso#1484 rebuilt) on the tree merged with main,
# CI's sitting. 2,182,576,109 -> 2,182,620,735, a RISE of 44,626 (+0.0020%).
# LAYOUT. The derivation this branch removes runs before `kanso check` reaches
# these routes, so the saving is outside them; what moved is where the code
# sits. Three check routes and the interpreted row rise by a few thousand each
# against falls of 567,547 and 560,657 on the two rows that pay for the
# derivation.
#
# 2026-09-18, re-merged onto main after kanso#1509. THE VALUE BELOW IS MAIN'S,
# carried forward, and the branch's own reading above it stands as history
# rather than as this tree's row. The five compile-side goldens were carried
# forward for the same reason, and carrying those five while leaving this one
# at the branch's reading made the trend gate read a PURE REGRESSION: the
# falls of 567,547 and 560,657 that pay for this rise live in rows that are
# now main's. Every compile-side row on this branch is main's, so the merged
# sitting is CI's and arrives in one piece.
#
# 2026-09-18, kanso#1510 (kanso#1484 rebuilt) on the tree merged with main
# after kanso#1509, CI's sitting. 2,182,576,109 -> 2,182,620,735, a RISE of
# 44,626 (+0.0020%). LAYOUT. The derivation this branch removes runs before this route
# reaches its work, so the saving is outside it and what moved is where
# the code sits; the two rows that pay for it fall by about 560,000 each.
# The second reading in the same job matched the first to the instruction.
#
# 2026-09-18, re-merged onto main after kanso#1510. THE VALUE BELOW IS MAIN'S,
# carried forward: kanso#1510 moved this row and what this branch adds on top
# has not been measured, so the merged reading is CI's.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1510,
# CI's sitting. 2,182,620,735 -> 2,182,501,648, a FALL of 119,087
# (-0.0055%). LAYOUT. The two divisions this branch merges are in float
# rendering and none of these routes renders a float. All six fell, where
# the sitting before kanso#1510 landed had the same six rising: one layout
# term, opposite sign, which is what a shifted binary looks like.
# carried forward: kanso#1510 moved this row on its own and what this branch
# adds on top of it has not been measured, so the merged reading is CI's.
#
# 2026-09-18, kanso#1511 on the tree merged with main after kanso#1510,
# CI's sitting. 2,182,620,735 -> 2,182,597,360, a FALL of 23,375
# (-0.0011%). AND THIS ROW DOES NOT ISOLATE THE CHANGE. The first round, against the
# pre-kanso#1510 base, read +62,650 and that entry called it the predicate
# walk plus a lookup per constant cell. This round, against the base
# kanso#1510 left, reads the other way. One change, two signs, so what the
# walk costs is below what this row resolves -- the reproducible signal for
# this change is `interp_allocs`, which read +16 on both rounds.
#
#
# 2026-09-18, re-merged onto main after kanso#1516. THE VALUE BELOW IS MAIN'S,
# carried forward; the merged reading is CI's.
# 2026-09-18, kanso#1516. ROUND ONE IS DELIBERATELY RED ON THIS ROW. The value
# below is main's, because this container's rustc is 1.94.1 against the
# runner's 1.98.1 and the gate refuses the comparison; CI measures and its own
# reading replaces this line.
#
# What this branch does, measured HERE and written down as evidence rather than
# as the row: two binaries built in one worktree, staged into boxes of equal
# path length, profiled with the gate's own anchor and glibc tunables. Base
# 2,007,688,216, head 1,836,055,421 -- a FALL of 171,632,795, 8.55%.
#
# WORK, and the mechanism is named. `eval_ident` walked a ladder for every name
# that is not a local: `fns`, then three descriptor names, then `types`, then
# `fns` and `types` a SECOND time and a sixty-name array, then `Rc::from(name)`
# to build the reference. A counter on each rung read 1,056,329 calls on this
# corpus, 724,304 of them locals that stop at the environment walk, 332,024
# reaching the type probe and 325,412 returning a reference -- so a third of
# all resolutions paid four probes and an allocation to answer a question whose
# answer cannot change during a run. The answer is remembered on first sight
# now, and the ladder runs once per name.
#
# 2026-09-18, kanso#1516 on the tree merged with main, CI's sitting. 2,168,428,538 -> 1,997,105,566, a FALL of
# 171,322,972 (-7.9008%). THIS ONE IS THE CHANGE.
#
# The container projected -171,632,795 (-8.55%) from two binaries built in one
# worktree; CI reads -171,322,972 on a different toolchain. The two deltas
# agree to within 309,823 -- 0.18% of the delta itself -- which is what a real
# saving looks like measured twice on two hosts.
#
# 2026-09-18, kanso#1517 (stacked on kanso#1516). ROUND ONE IS DELIBERATELY RED
# ON THIS ROW. The value below is the row the branch underneath measured on CI,
# carried forward: this host runs rustc 1.94.1 against the runner's 1.98.1 and
# the gate refuses to compare, so what this branch does to the row is CI's to
# read.
#
# Measured here against kanso#1516's head, same worktree, boxes of equal path
# length: 1,836,055,421 -> 1,802,816,521, a FALL of 33,238,900 (-1.81%).
# Against main, the two changes together: 2,007,688,216 -> 1,802,816,521,
# -204,871,695, -10.20%.
#
# WORK. `call_named` walked a ladder of its own on every call -- `err`, the
# types map, the function map -- and `group_of` inside `eval_tail` probed both
# maps to decide one bit about every `FnRef` callee. Neither map changes during
# a run, so both read one memory now. The profile put those frames at
# 49,549,566 and the measured fall is 33,238,900: the change adds a probe at
# each site as well as removing two, so the profile's figure was a ceiling.
# What the remaining 16,310,666 is made of is not separated here.
#
# START-UP DOES NOT PAY FOR IT. The function groups became `Rc<Vec<..>>` so the
# memory can hold one without borrowing from `self`, which is an allocation per
# group at construction. `kanso play` on the start-up corpus reads 3,399,666
# before and 3,384,980 after, a FALL of 14,686 -- and that route takes the
# native path rather than building an `Interp`, so the move is layout and the
# allocation per group is not in this number at all. What it says is that
# nothing on the start-up path got worse.
#
# 2026-09-18, kanso#1517 on the tree merged with kanso#1516, CI's sitting.
# 1,997,105,566 -> 1,963,826,350, a FALL of 33,279,216 (-1.6664%).
# THIS ONE IS THE CHANGE. The container projected -33,238,900 and CI reads
# -33,279,216 on a different toolchain: 40,316 apart, 0.12% of the delta.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1517,
# CI's sitting. 1,963,826,350 -> 1,963,826,365, a RISE of
# 15 (+0.0000008%).
# FIFTEEN INSTRUCTIONS on 1.96 billion, which is 0.0000008% and is not
# work. The interpreted corpus decodes a document it built itself and
# never enters the C runtime, and on the tree merged after kanso#1516 this
# row read its golden EXACTLY. What changed underneath it is kanso#1517,
# not this branch. A move of this size at frame level has been recorded
# here before, at +-13 (kanso#1469); whether it is the same frame was not
# checked, so this says only that fifteen is too small to be the two
# divisions and that the row is otherwise unmoved.
# ^ SUPERSEDED: that reading was taken before kanso#1518 landed under this
#   branch. THE VALUE BELOW IS MAIN'S, carried forward; the merged
#   reading is CI's.
#
# 2026-09-18, kanso#1518 against main at kanso#1517, CI's sitting.
# 1,963,826,350 -> 1,555,890,579, a FALL of
# 407,935,771 (-20.7725%).
# THE FRAME MEMORY AND FOUR RESERVES. This container projected the fall at
# 410,520,397 and the runner read 407,935,771 -- 0.63% of the delta apart,
# which is the usual agreement between the two hosts on a difference.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1518,
# CI's sitting. 1,555,890,579 -> 1,555,890,587, a RISE of 8 (+0.00000051%).
# EIGHT INSTRUCTIONS, and the fourth reading in one day to land under thirty.
# Four src/runtime.c edits, four different functions, four sittings: 15 and 26
# against the kanso#1517 tree, 19 and 8 against this one. The interpreted
# corpus decodes a document it built itself and never enters the C runtime,
# so none of them can be work, and `interp_allocs` read 3,879,653 on ALL FOUR
# -- an allocation counter counts operations rather than a host, and real work
# would have moved it. Four readings agreeing on that is the check; the size
# of any one of them on its own would not be.
# ^ SUPERSEDED: that reading was taken before kanso#1520 landed under this
#   branch. THE VALUE BELOW IS MAIN'S, carried forward; the merged
#   reading is CI's.
#
# 2026-09-18, kanso#1520 against main at kanso#1519, CI's sitting.
# 1,555,890,579 -> 1,260,262,910, a FALL of 295,627,669 (-19.0005%).
# THE CLONE SIZED FOR ITS GROWTH, and the number is THREE TIMES what this
# container projected. The container read the fall at 94,998,647 and the
# runner reads 295,627,669. Both hosts agree EXACTLY on what the change
# does -- interp_allocs falls 36,066 and interp_peak_bytes 76,113 on both,
# to the byte -- and disagree threefold on what it costs. What a copy of N
# elements costs in instructions is the rustc that built the binary; how
# many copies happen is the program.
# CI's reading on the merged tree, interp_instructions: 1,260,262,910 -> 1,260,262,917 (+7). The branch
# touches no compiler source; this is the layout the merge left.
# ^ SUPERSEDED: that reading was taken before kanso#1522 landed under this
#   branch. THE VALUE BELOW IS MAIN'S, carried forward; the merged
#   reading is CI's.
#
#
# 2026-09-18, kanso#1522 against main at kanso#1521, CI's sitting.
# 1,260,262,910 -> 1,138,001,430, a FALL of 122,261,480 (-9.7013%).
# A BOUND NAME WAS COPIED TWICE. `match_one` pushed a fresh `String` into a
# `Bindings` and `bind` allocated a second copy of the same bytes on top of
# it; the first commit takes the name by value and the second stores a
# `Name`, which keeps twenty-two bytes or fewer in the node. The allocation
# row below it falls 33.92% for the same reason.
#
# AND THE CONTAINER'S PROJECTION HELD THIS TIME, where kanso#1520's was three
# times out. This box read the fall at 127,890,665 and the runner reads
# 122,261,480, 4.40% apart. The two changes differ in what they move:
# kanso#1520 changed how many BYTES a copy moves, and what a copy of N bytes
# costs in instructions is the rustc that built the binary; this one changes
# how many ALLOCATIONS happen, which is a count the program decides. A
# container projection travels when the thing it projects is a count.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1522, CI's
# sitting. 1,138,001,430 -> 1,138,001,437, a rise of SEVEN.
#
# WHAT SUPPORTS THE NUMBER. Two branches read it independently on the same
# day: this one, carrying two divisions in Ryu's float rendering, and
# kanso#1504, caching the innermost beat mark. Neither goes near the
# interpreted corpus. They ran on different runners -- kanso#1502's was Intel,
# family 0x6 model 0x6a, which bench/dispatch.txt does not record -- and both
# read 1,138,001,437 exactly. `interp_allocs` read 2,539,998 on both and on
# main, which is the check that makes this readable: an allocation counter
# counts decisions the program makes, and the interpreted corpus is not doing
# anything different.
#
# WHAT DOES NOT. The golden's 1,138,001,430 came from kanso#1522's own PR tree;
# 1,138,001,437 is what a tree containing that change MERGED reads. That the
# two differ by seven is reproduced. WHY they differ is not established, and an
# earlier note on kanso#1504 claiming a pattern of sevens across three trees was
# withdrawn -- one of its three readings was an artifact of the gate's second
# count, which kanso#1524 fixes. Layout is the standing guess and it is only a
# guess.
interp_instructions=1138001437
# measured-on glibc=2.39-0ubuntu8.9 rustc=1.98.1
