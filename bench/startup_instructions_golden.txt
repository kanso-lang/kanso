# Retired instructions for `kanso play startup_corpus/main.kso`, counted under
# callgrind and anchored at the `kanso::main` frame.
#
# WHAT THIS COUNTS THAT THE OTHER THREE COMPILE ROWS DO NOT. They count what
# compiling a body of code costs: `bench/compile_instructions_golden.txt` a
# directory, `bench/entry_instructions_golden.txt` a file with statements,
# `bench/library_instructions_golden.txt` a file of definitions. All three run
# `kanso check`, which stops before codegen. This one runs `kanso play` on a
# program holding a single `print`, so what it counts is what a run costs
# BEFORE it has any work to do — the loader, the prelude, the whole of the
# backend deciding what to emit for almost nothing. A one-line program is the
# point of it: what is left when the program is that small is start-up.
#
# WHY THE VEIN EXISTS. Clay ruled on 2026-09-16 that the objective becomes a
# development welfare, a production welfare and a meta over them, and that
# start-up belongs to the development side: `kanso test` pays it on every
# invocation and production never pays it once. The same microsecond is
# enormous in one context and free in the other, which is the reason that
# gavel gives for splitting rather than re-weighting. The counters for that
# side did not exist. This is the first of them.
#
# It is pinned here as its own exact vein and is NOT an objective term yet, the
# way `.text` is pinned under the 2026-09-05 ruling. The objective takes it
# when the model splits, which is that gavel's own build.
#
# ONE ROW, ONE VALUE, ruled 2026-09-05. No per-chip key and no pinned pair: a
# build that counts two numbers is a reproduction failure and is hunted rather
# than recorded. `scripts/gates/compile_instructions.sh` carries the
# measurements that ruling was made on, and the `startup_binary sha256` and
# `startup_sample` lines this gate prints are where such a hunt starts.
#
# THE ROW IS CI'S, never a container's. The container this file was written on
# runs rustc 1.94.1 against CI's 1.98.1 and reads about one per cent high on
# the other instruction rows, so the number below is a projection until CI
# replaces it.
#
# measured-on glibc=2.39-0ubuntu8.9 rustc=1.98.1
#
# 2026-09-16 (the vein opens, with the change that made it worth opening):
# `codegen::Backend::emit`'s `declares` filter asked `referenced(sym)` for each
# of about 1,204 DECLARES lines, and `referenced` re-split DECLARES and
# re-scanned its non-declare lines on every ask — roughly one and a half
# million line scans to decide which declarations to keep. Three quarters of
# start-up was substring search and 92.6% of it had that one owner.
#
# Measured on this container at gate settings, one box and one corpus, the two
# binaries differing only in that expression:
#
#     base    69,183,407
#     fixed    4,919,980
#     delta  -64,263,427   -92.89%   14.06x
#
# Read twice on two stagings of the fixed binary, identical both times. It is
# deterministic by construction as well as by measurement: the symbol set this
# change introduces is `crate::hash::Set`, which seeds its hasher fixed, and
# not std's, which seeds per process -- a randomly seeded table would make this
# row differ between two runs of one binary, which is the reproduction failure
# the ruling above forbids. tests/the_compile_path_hashes_with_a_fixed_seed.rs
# named the line before it could happen.
#
# THE VALUE BELOW IS THE CONTAINER'S, and round one is deliberately red on it.
# CI measures and its own reading replaces this line.
# 2026-09-16 (CI's own first sitting): the row above was this container's
# 4,919,980 and the runner reads 6,018,394, 22.3% higher. The container has
# read about 1.2% high on the three compile rows all month, so the sign is the
# other way here and the size is twenty times larger. That is what the row is:
# start-up is the loader, the prelude and the backend deciding what to emit for
# one `print`, and the loader's share of it belongs to the runner's glibc and
# its rustc rather than to anything the compiler does. The compile rows are
# dominated by the compiler's own passes and this one is not, which is why the
# projection that holds for them does not hold here. CI's is the number.
#
# 2026-09-17, kanso#1461, round three. The row is taken with the caches
# WARM now. `kanso play` takes the native path, so the first process of a
# job writes kanso_runtime_<profile>_<key>.o and kanso_run_<key> into the
# temp directory and every one after it reuses them. On 2026-09-17 one
# binary in one job read 6,018,427 and then 4,869,632 -- 1,148,795 apart,
# a fifth of the row. The gate now runs the program once before it counts,
# which is the state every reading after the first would have seen. This
# figure is that second reading; CI takes the next one.
#
# 2026-09-17, kanso#1461, round four. The warm-up works: CI read
# 4,876,986 and then 4,876,986 again in the same job, where the round
# before it read 6,018,427 and then 4,869,632. The row was counting a cold
# runtime-object cache, and now it counts the compile.
#
# 2026-09-17, kanso#1461 on the tree merged with kanso#1472, CI's sitting. The
# row this branch is for: the interpreter's start-up stops asking the declares
# scan once per name. A FALL of 38,686 (-0.793%) against the value the merge
# carried forward.
#
# 2026-09-17, kanso#1461 on the tree merged with kanso#1465, CI's sitting. The
# row this branch is for, re-read on the merged tree: 23 instructions above the
# previous sitting's 4,838,300 on a different tree. This is the interpreter's own
# start-up and the 13.8x fall this branch bought is already in the value.
#
# 2026-09-17, kanso#1468. CI reads 5,077,750 against 4,838,323, a RISE of
# 239,427 (+4.95%), and it is real work rather than layout. This row is the one
# that reaches codegen, and the branch indexes two whole-body scans there.
# Reproduced in a container and attributed, main against the branch on this
# corpus:
# 
#     kanso::main            4,882,857 ->  5,148,482   +265,625
#     codegen::called_symbols        0 ->    278,812   +278,812  (new)
#     memchr_aligned           921,736 ->    879,487    -42,249
#     is_contained_in          626,252 ->    577,318    -48,934
#     next_match               381,383 ->    379,562     -1,821
# 
# The index costs 278,812 to build and saves 93,556 on the scans it replaces,
# because most of that cost is reading DECLARES -- twelve hundred lines, a
# constant, the same for every process -- while the saving is proportional to
# how much the program emits. A single `print` emits almost nothing, so this
# corpus is where the trade is worst. `kanso build bench/runbench` falls 69.64%
# on the same change. The objective weighs neither row, so nothing here is a
# welfare question; the trade is stated because the row moved.
#
# 2026-09-17 (kanso#1478): startup_instructions 5,077,750 -> 5,082,671, a RISE of
# 4,921 (+0.0969%), 2,719 of it against the parent. The seventh index is more
# bytes for the loader to place, and the trade it buys is the 88,438 the entry
# row above gives back.
# 2026-09-17 (kanso#1476): startup_instructions 5,077,750 -> 5,079,952, a RISE of
# 2,202 (+0.0434%). Against the parent's 5,079,380 the rise is 572, and that is
# the only row this branch moves on its own: the index it builds is a few more
# bytes for the loader to place. The three `kanso check` rows above are the
# parent's to the instruction.
# 2026-09-17, kanso#1475. 3,354 above kanso#1473's reading.
# 2026-09-17, kanso#1473. The start-up rise this stack carries, 1,724 below
# kanso#1468's reading of it.
#
# 2026-09-17, merged with the base. The base moved this row's FILE and not
# the compiler: since this branch measured, the only thing that landed
# under it is a row written into a golden, so the value below is this
# branch's own sitting and it still stands.
#
# 2026-09-17, merged with the base. The base moved this row's FILE and not
# the compiler: since this branch measured, the only thing that landed
# under it is a row written into a golden, so the value below is this
# branch's own sitting and it still stands.
#
# 2026-09-17, merged with the base. The base moved this row's FILE and not
# the compiler: since this branch measured, the only thing that landed
# under it is a row written into a golden, so the value below is this
# branch's own sitting and it still stands.
#
# 2026-09-17 (kanso#1470): startup_instructions 4,838,323 -> 4,837,892, a FALL of
# 431 (-0.0089%). Layout, same sitting as the compile row.
# 2026-09-17, kanso#1482, CI's sitting. LAYOUT, same reason, and this one
# fell: 956 lower. The four rows together are +93 +564 +511 -956, which is
# what a code-layout shift looks like -- no direction, and no relation to
# what the branch changed.
# 2026-09-17, kanso#1462, same sitting. A RISE of 49 (+0.001%). Layout again,
# and the smallest move this vein has recorded.
#
# 2026-09-17, merged with main. Both branches moved this row on their own
# base, so the merged tree is reading neither number. The value below is
# main's, carried forward so the gate has one value to fail against
# rather than none; the merged sitting is CI's to take.
#
# 2026-09-17, kanso#1468 on the tree merged with kanso#1462. CI reads
# 5,077,523 against main's 4,838,372, a RISE of 239,151 -- 4.94%. This is the
# one row on this branch that moves by WORK rather than by layout, and it is
# the trade the change makes rather than a surprise.
# #
# `kanso play` takes the native path, so unlike the three `kanso check` rows
# it runs the emitter -- on a one-line program, which is the point of the
# vein and also why it is the one corpus where the index cannot pay for
# itself. Both sides were reproduced on the container under rustc 1.98.1,
# 161 and 169 instructions from CI's two readings: main 4,838,211, the branch
# 5,077,692, a local delta of 239,481 against CI's 239,151.
# #
# WHERE IT GOES. `declares_context_calls` builds its set once per process out
# of DECLARES's 1,187 lines and costs 599,739 instructions inclusive, 10.57%
# of the whole row -- two and a half times the rise. The per-line searching it
# replaces was therefore worth about 360,000 here, and the difference is what
# this row records. On a real program the same set answers 163 declare
# questions and 1,024 more, and `kanso build bench/runbench` falls 69.90%.
# #
# So the shape is a fixed cost against a per-line one, and start-up is the
# corpus with no lines to spread it over. Building the set more cheaply than
# a run of hashed inserts is the open lead; the objective does not weigh this
# vein yet, so nothing waits on it.
#
# 2026-09-17, kanso#1478 as the one tip, on main 298636b7. CI reads 5,082,497
# against main's 4,838,372, a RISE of 244,125 -- 5.05%.
# #
# IT DECOMPOSES, because kanso#1468 took its own sitting on this row two hours
# earlier and read 5,077,523. So kanso#1468 is 239,151 of this and the four
# branches above it are 4,974 between them. The 239,151 is measured and
# attributed in that branch's own note: `declares_context_calls` builds its
# set once per process out of DECLARES's 1,187 lines, 599,739 instructions
# inclusive, against about 360,000 of per-line searching removed.
# #
# The shape is the same for all five: a fixed index built once per program,
# against a saving proportional to what the program has. `kanso play` on a
# one-line program is the corpus with nothing to spread it over, which is
# what this vein is for. kanso#1484, stacked on kanso#1468, writes the
# DECLARES set down as a sorted constant and takes the larger part back.
#
# 2026-09-17, kanso#1470 on main 298636b7. CI reads 4,837,941 against main's
# 4,838,372, a fall of 431 -- 0.009%. `kanso play` takes the native path and
# this branch adds counter plumbing to src/main.rs; 431 is what that weighs
# here.
# 2026-09-17, kanso#1482 on the tree merged with kanso#1462. CI reads
# 4,837,381 against main's 4,838,372, a fall of 991 -- 0.020%. `kanso play`
# takes the native path and does run the emitter, so some of this is the
# change working; a one-line program emits few enough lines that the saving
# and the layout term the four rows above show are the same size, and this
# reading does not separate them. Both point the same way.
#
# 2026-09-17, merged with main. Both branches moved this row on their own
# base, so the merged tree is reading neither number. The value below is
# main's, carried forward so the gate has one value to fail against
# rather than none; the merged sitting is CI's to take.
#
# 2026-09-17, merged with main at 5e256ce0 (kanso#1491), which moved this row
# 1,373 instructions downward under this branch. The value below is this
# branch own CI sitting; the merged number is CI to take.
#
# 2026-09-17, kanso#1478 against main, CI's sitting. 4,837,381 -> 5,081,497, a
# RISE of 244,116 (+5.046%). NOT LAYOUT. `kanso play` takes the native path and
# runs the emitter, which is where this run of five changed code.
# 
# WHAT IT IS. Each of the five replaces a whole-program scan run once per name
# with an index built once per process, and this workload is a single `print`.
# There is nothing to amortise the construction over. Profiled under rustc 1.98.1
# in the gate's own box, `kanso::main` inclusive reads 4,837,246 on main and
# 5,081,820 here -- 323 from CI's reading of the same pair, which is as close as
# two containers get. The frames that arrive are the `OnceLock<Set<&str>>` behind
# `declares_context_calls` at 599,550 and `called_symbols` at 310,752; the frames
# that leave are the per-name scans, 362,928 + 274,780 + 107,365 + 107,269 +
# 41,018 + 19,651.
# 
# ALMOST ALL OF IT IS ONE OF THE FIVE. kanso#1468 alone read +239,217 on its own
# CI sitting, so the other four together add 4,899. One index accounts for the
# row.
# 
# THE TRADE IS THE RIGHT WAY ROUND. The five take 61.6x off
# `kanso build bench/runbench` between them, with the emitted IR byte-identical
# on every one. A one-line program is the workload where an index is all cost,
# and measuring what is left when the program is too small to cost anything is
# what this row is for.
#
# 2026-09-17, kanso#1470 against main, CI's sitting. 4,837,381 -> 4,836,950, a
# FALL of 431 (-0.0089%). `kanso play` runs the emitter, which is where the new
# counters sit; they are compiled out of a shipped binary and the gate builds one
# that carries them, so what this reads is the same layout term as the rows
# above, at the same scale.
# 2026-09-17, kanso#1478 on the tree merged with main 5e256ce0, CI's sitting.
# 5,081,497 -> 5,081,099, a FALL of 398 (-0.0078%). The merge moved this one
# DOWNWARD where it moved the three check rows up, which is what a layout term
# looks like: it has no sign of its own.
# 2026-09-17, merged with main fcf6238e (kanso#1478). That branch RAISED this
# row 242,727 -- seven indexes are more bytes for the loader to place -- so the
# 3,711,750 this branch measured was against a baseline that no longer exists.
# The value below is main's, carried forward so the gate has one number to fail
# against rather than none, and so CI's diff reads exactly what this branch
# does to today's main. The merged sitting is CI's to take.
#
# 2026-09-17, kanso#1493 on the tree merged with main fcf6238e, CI's sitting.
# 5,081,099 -> 3,955,899, a FALL of 1,125,200 (-22.14%). The runtime source is
# digested at BUILD time now, so a run no longer hashes 8,400 lines of C to
# decide whether its cached runtime object is current. `kanso play` takes the
# native path, which is why this vein sees it and the three `kanso check` rows
# do not: they stop before codegen and never ask the question.
#
# The value the merge carried forward was main's, so this diff is the whole of
# what the branch does to today's main, taken in one job.
#
# 2026-09-17, merged with main 2a0cec7c. Every row below is MAIN'S, carried
# forward. This branch measured against a base that kanso#1478 (emitted code
# -84.25%) and kanso#1493 (start-up -22.14%) have both moved since, so the
# numbers it took are against a tree that no longer exists -- its emit row read
# 382,216,372 where main now reads 60,197,743. Carrying main's forward gives
# the gate one number to fail against rather than none, and makes CI's diff
# read exactly what this branch does to today's main. The merged sitting is
# CI's to take.
#
# 2026-09-17, kanso#1486 on the tree merged with main 2a0cec7c, CI's sitting.
# startup_instructions 3,955,899 -> 3,951,284, a FALL of 4,615 (-0.1167%).
# Start-up reaches codegen but compiles one `print`, so most of this is the
# same layout term the row above shows, at this row's scale.
# 2026-09-18, kanso#1507 on the tree merged with main, CI's sitting. 3,955,899 ->
# 3,955,888, a FALL of 11 (-0.00028%). LAYOUT. This branch adds a
# `match std::env::var("KANSO_LTO_JOBS")` to `release_clang` in src/main.rs and
# `KANSO_LTO_JOBS=1` to three `env -i` lines in a gate script. src/main.rs is
# the compiler, so its bytes and the layout under them move, and every row that
# runs the compiler moves with them. Seven rows moved here and all seven are
# under five ten-thousandths of a per cent, with mixed signs -- which is what a
# shifted binary looks like and not what work looks like.
#
# 2026-09-18, re-merged after kanso#1486 landed underneath. THE VALUE BELOW IS
# MAIN'S, carried forward. kanso#1507's own sitting on the pre-merge tree read
# 3,955,888 and is recorded above; it was taken on a base that did not
# carry kanso#1486's 1.17% fall across the three check routes, so it describes
# a tree that no longer exists. Carrying main's forward gives the gate one
# number to fail against rather than none, and makes CI's diff read exactly
# what this branch does to today's main.
#
# 2026-09-18, kanso#1507 on the tree merged with kanso#1486, CI's sitting.
# 3,951,284 -> 3,951,796, a RISE of 512 (+0.01296%). LAYOUT. The value
# above was main's, carried forward because this branch's own reading predated
# kanso#1486; this is the merged tree measured. src/main.rs gained a four-line
# `match` on an environment variable and src/main.rs is the compiler, so its
# bytes move and every row that runs the compiler moves with them. Six rows
# moved and the two codegen rows did not: they AGREED with this branch's own
# readings to the instruction, across two CI jobs on two different trees,
# which is what `-Wl,-plugin-opt=jobs=1` bought.
#
# 2026-09-18, kanso#1502 on the tree merged with main, CI's sitting.
# 3,951,796 -> 3,951,975, a RISE of 179 (+0.0045%).
# LAYOUT. These rows carry runtime.c's bytes rather than compiling it, and
# every binary's `.text` grew 32 bytes on this change; the moves are all under
# two hundredths of a per cent.
# 2026-09-18, kanso#1510 (kanso#1484 rebuilt) on the tree merged with main,
# CI's sitting. 3,951,796 -> 3,384,249, a FALL of 567,547 (-14.3617%).
# THIS IS THE CHANGE, and it is much larger here than anywhere else. The set
# of symbols DECLARES calls was re-derived rather than written down, and the
# interpreter's start-up is where that derivation was paid for most. What the
# saving IS has been measured; why it lands so much harder on this row than on
# the emitter's is not claimed here beyond the obvious -- start-up runs the
# derivation over the whole declare block before any program does anything.
# 2026-09-18, kanso#1509 (kanso#1480 rebuilt) on the tree merged with main,
# CI's sitting. 3,951,796 -> 3,933,223, a FALL of 18,573 (-0.4700%).
# The interpreter's start-up carries the compiler's bytes, and this branch
# removes two whole analysis constructions from the emitter's path, so the
# binary's layout moves with it. A fall rather than a rise, and larger than
# this row's usual layout term.
#
# 2026-09-18, kanso#1511 on the tree merged with main, CI's sitting.
# 3,933,223 -> 3,932,978, a FALL of 245 (-0.0062%).
# LAYOUT, and it FELL. Start-up takes the native path, which counts cells in
# the runtime rather than in the interpreter.
# 2026-09-18, re-merged onto main after kanso#1509. THE VALUE BELOW IS MAIN'S,
# carried forward: kanso#1509 moved this row on its own and what this branch
# adds on top of it has not been measured, so the merged reading is CI's.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1509,
# CI's sitting. 3,933,223 -> 3,933,390, a RISE of 167 (+0.0042%).
# LAYOUT. The two divisions this branch merges live in float rendering,
# which none of these routes runs: three are `kanso check`, `emit_ir`
# stops before the backend, and the interpreted start-up links the runtime
# without rendering a float. What moved is where the code sits. The second
# reading in the same job matched the first to the instruction.
# 2026-09-18, kanso#1510 (kanso#1484 rebuilt) on the tree merged with main
# after kanso#1509, CI's sitting. 3,933,223 -> 3,364,974, a FALL of
# 568,249 (-14.4474%). This is a route the removed derivation runs, and the saving lands here.
# The second reading in the same job matched the first to the instruction.
#
# 2026-09-18, re-merged onto main after kanso#1510. THE VALUE BELOW IS MAIN'S,
# carried forward: kanso#1510 moved this row and what this branch adds on top
# has not been measured, so the merged reading is CI's.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1510,
# CI's sitting. 3,364,974 -> 3,363,173, a FALL of 1,801
# (-0.0535%). LAYOUT. The two divisions this branch merges are in float
# rendering and none of these routes renders a float. All six fell, where
# the sitting before kanso#1510 landed had the same six rising: one layout
# term, opposite sign, which is what a shifted binary looks like.
# carried forward: kanso#1510 moved this row on its own and what this branch
# adds on top of it has not been measured, so the merged reading is CI's.
#
# 2026-09-18, kanso#1511 on the tree merged with main after kanso#1510,
# CI's sitting. 3,364,974 -> 3,364,755, a FALL of 219
# (-0.0065%). LAYOUT. Start-up takes the native path, which counts cells in the runtime.
#
#
# 2026-09-18, re-merged onto main after kanso#1516. THE VALUE BELOW IS MAIN'S,
# carried forward; the merged reading is CI's.
# 2026-09-18, kanso#1516 on the tree merged with main, CI's sitting.
# 3,363,774 -> 3,364,523, a RISE of 749 (+0.0223%).
# LAYOUT. src/eval.rs is the compiler, so its bytes move and every row that
# runs the compiler moves with them; none of these routes evaluates a name,
# because all four stop before the interpreter and the fifth is the emitter.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1516,
# CI's sitting. 3,364,523 -> 3,363,366, a FALL of
# 1,157 (-0.0344%).
# LAYOUT. src/runtime.c is include_str!'d into the compiler (src/main.rs)
# and digested into a constant (src/hash.rs), so editing it moves 450,100
# bytes of the compiler's own .rodata and every route that runs the compiler
# moves with it. None of these five routes executes the runtime: three stop
# before codegen, the fourth writes IR, the fifth starts the interpreter.
# ^ SUPERSEDED: that reading was taken before kanso#1517 landed under this
#   branch. THE VALUE BELOW IS MAIN'S, carried forward; the merged
#   reading is CI's.
#
# 2026-09-18, kanso#1517 on the tree merged with kanso#1516, CI's sitting.
# 3,364,523 -> 3,362,788, a FALL of 1,735 (-0.0516%).
# LAYOUT. `kanso play` takes the native path and builds no `Interp`.
#
# 2026-09-18, kanso#1502 on the tree merged with main after kanso#1517,
# CI's sitting. 3,362,788 -> 3,363,586, a RISE of
# 798 (+0.0237303%).
# LAYOUT. src/runtime.c is include_str!'d into the compiler (src/main.rs)
# and digested into a constant (src/hash.rs), so editing it moves 450,100
# bytes of the compiler's own .rodata. None of these routes executes the
# runtime: three stop before codegen, the fourth writes IR, the fifth
# starts the interpreter.
# ^ SUPERSEDED: that reading was taken before kanso#1520 landed under this
#   branch. THE VALUE BELOW IS MAIN'S, carried forward; the merged
#   reading is CI's.
#
# 2026-09-18, kanso#1520 against main at kanso#1519, CI's sitting.
# 3,362,788 -> 3,363,378, a RISE of 590 (+0.0175%).
# LAYOUT. This branch changes how a shared vector is copied in the
# INTERPRETER; none of these routes runs it. src/eval.rs is the compiler,
# so its bytes move and every route that runs the compiler moves with them.
#
# 2026-09-18, kanso#1533, CI's sitting. 3,365,595 -> 3,363,916,
# a FALL of 1,679 (-0.0499%).
# Layout, the same way.
# 2026-09-18, kanso#1543. 3,363,916 -> 3,363,672, -244. Layout, and the only
# one of the five that fell. Direction carries no information here.
# 2026-09-19, kanso#1502 on the tree merged after kanso#1543 and kanso#1547,
# CI's sitting. startup_instructions 3,363,672 -> 3,363,249, a move of -423.
# 2026-09-19, kanso#1502 on the tree merged after kanso#1513, CI's sitting.
# startup_instructions 3,363,249 -> 3,363,186, a move of -63.
# LAYOUT. kanso#1513 added the fixed-temp pin to src/main.rs, so the compiler's
# own bytes moved and these rows carry them. The job read the same number twice
# -- "this binary is stable" -- so the disagreement was with the golden rather
# than within the run, and the change under test is the merge.
# AND memcmp IS PRELOADED, 2026-09-23, under the same rule.
#
# Every run this gate counts preloads scripts/gates/address_blind/compare.c
# over libc's memcmp and bcmp. glibc's __memcmp_avx2_movbe asks, for a
# comparison under 32 bytes, whether (s1 | s2) & 4095 lies within 32 bytes of
# a page end, and takes a longer branch when it does. So comparing the same
# bytes cost a different number of instructions depending on where the linker
# put one operand and the allocator put the other. kanso#1561 added forty lines
# to src/runtime.c, which the compiler embeds. On one host that moved 478 of the
# compile row's comparisons onto the page-cross branch with every call count
# unchanged, and that row rose 2,688. With the preload the two trees read
# 36,656,738 each, and the two processes' PROGRAM TOTALS agreed to the
# instruction.
#
# The replacement compares eight bytes at a time, and its cost depends only on
# the length and where the first difference falls. It costs more than libc's:
# the compile row read 666,638 higher on that host, because the avx2 path
# compares 32 bytes in one instruction. The value below was measured with it and
# is not comparable with any reading taken before 2026-09-23.
#
# AND memcpy AND memmove, the same day. With memcmp preloaded, merging
# kanso#1561's runtime under this branch still moved the compile, entry and
# library rows by 44, 122 and 122, and in each table the whole move was
# __memcpy_avx_unaligned_erms. Its path for a large copy depends partly on the
# distance between destination and source: 1,500 bytes cost 177 instructions at
# one distance and 179 at another. scripts/gates/address_blind/copy.c replaces
# both, 128 bytes a step with unaligned AVX2 moves, and costs what the length
# says. Copies libc makes internally, a thousand-odd instructions a run, cannot
# be reached by a preload and are still counted as libc does them.
#
startup_instructions=3367191
