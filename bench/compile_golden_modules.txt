# What compiling a MODULE costs — an entry, a local module beside it, and the
# standard library each pulls in. Every sample in compile_golden.txt is a
# single file with no imports, which left the loader, enrollment and
# qualification costing whatever they liked.
#
# Kept apart from that file because the welfare index sums its rows: a sample
# added there reads as a regression the size of the sample.
#
# 2026-09-06 (the fold reads its length once): lines 5,176 -> 5,186, calls
# 753 -> 755, branches 430 -> 431, visits 2,403 -> 2,409; rounds and defines
# hold. `list/fold_flat` carries a fifth parameter, so its declaration and both
# edges of its tail cycle each take one more argument -- the same +2 calls,
# +1 branch, +10 lines every program in the emitted goldens moved by, and six
# more expression visits to decide the extra name. This sample is the one that
# imports, so it is the only row here that std/list can reach; the five in
# bench/compile_golden.txt import nothing and hold. Held as the price of seven
# runtime work rows falling, encodebench by 1.3766%.
#
# 2026-09-06 (the proven-tags append): lines 5,186 -> 5,232, calls 755 -> 756,
# branches 431 -> 434, defines 98 -> 99; rounds and visits hold at 6 and 2,409.
# That is the k_b_append_mut_int twin, which the emitter writes into the
# preamble every program carries, so it reaches this sample without the sample
# importing anything. The front end does not decide anything new -- hence the
# flat rounds and visits -- it just carries 46 more lines through.
#
# BOTH NOTES ABOVE WERE STRIPPED ONCE. `KANSO_REGEN_COMPILE_GOLDEN=1` keeps the
# header it was born with and drops everything after it that is not a data row,
# so the first note lasted three hours and vanished on the next regeneration.
# `all_counters.sh --write` preserves notes; this path does not. Restored by
# hand; the asymmetry is filed.
#
# 2026-09-07 (the length twin reads a string's count memo): lines 5,232 ->
# 5,247, branches 434 -> 436; calls, defines, rounds and visits hold. The same
# +15 lines and +2 branches every row of compile_golden.txt moved by, for the
# same reason: the twin's two new arms arrive through the preamble.
module lines=5177 calls=727 branches=430 defines=100 rounds=6 visits=2471
