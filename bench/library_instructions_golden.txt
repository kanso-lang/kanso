# Retired instructions for `kanso check library_corpus/library_corpus.kso`,
# counted under callgrind, anchored at the `kanso::main` frame.
#
# WHAT THIS COUNTS THAT THE OTHER TWO COMPILE ROWS DO NOT. `kanso check`
# routes a single file by its content, and the three routes are three
# compiles. bench/compile_instructions_golden.txt checks a DIRECTORY, which is
# a module: compile_module_inner. bench/entry_instructions_golden.txt checks a
# file with a top-level expression, which is an entry: compile_parsed_entry.
# This checks a file of definitions alone, which is a LIBRARY:
# compile_library. `kanso test` takes that path on every run and so does
# `kanso check` on most files in the tree, and until this row opened nothing
# counted it.
#
# The three move together for the lexer, the parser, inference and the checks
# they share, and independently for the passes only one of them runs. This row
# is the one that answers for `compile_library`'s own pass order. All three run
# `canonicalize_bare_aliases` in front of the whole-program check now --
# kanso#1328 did it on the module path, kanso#1335 on the entry path and
# kanso#1338 here -- and the row below is what that last one cost.
#
# ONE ROW, ONE VALUE, ruled 2026-09-05 and enforced by
# tests/the_compile_row_holds_one_value.rs for the module row. No per-chip key,
# no pinned pair: a build that counts two numbers is a reproduction failure and
# is hunted rather than recorded. compile_instructions.sh carries the
# measurements that ruling was made on.
#
# THE ROW IS CI'S, never a container's. The container this file was written on
# has rustc 1.94.1 against CI's 1.98.1 and reads about 0.8% high on both of the
# other compile rows, so its number is a projection and not a sitting.
#
# measured-on glibc=2.39-0ubuntu8.9 rustc=1.98.1
#
# 2026-09-08 (the vein opens): library_instructions=164,253,088.
#
# CI's own first sitting, on binary sha 9bc8f829af68 with .text=2,599,486 --
# the same job that counted compile_instructions=48,791,172 and
# entry_instructions=162,170,772, so all three rows answer for one build.
#
# The container that wrote this file projected 165,589,540 from the same recipe
# minus the host check. That is 1,336,452 high, or +0.8136%, which lands on the
# offset the other two rows already record between rustc 1.94.1 there and CI's
# 1.98.1 -- the module row's and the entry row's both sit at about +0.8%. The
# projection is kept as the thing this sitting was checked against and is not a
# row: a number this file may hold is one CI counted.
#
# 2026-09-08 (the alias pass moves in front of the check, kanso#1338):
# library_instructions=164,253,088 -> 162,970,167, a fall of 1,282,921 or
# 0.7811%. `compile_library` stops running the whole-program check over the
# synthetic twins the alias pass deletes, which is the same saving the module
# and entry paths took in kanso#1328 and kanso#1335.
#
# The container projected 164,300,594, a fall of 1,288,946 or 0.7784%. It
# overstated the saving by 6,025 instructions -- half a per cent OF THE FALL --
# while reading 0.8136% high on the LEVEL. A level offset between toolchains
# does not carry to a delta, which is the second sitting to say so: kanso#1335
# recorded the same thing on the entry and module rows, and in that direction
# too the projections came out conservative.
#
# THE OTHER TWO COMPILE ROWS DID NOT MOVE AT ALL. Both are byte-identical in
# the job that counted this one -- compile_instructions=48,791,172 and
# entry_instructions=162,170,772 -- and this change edits src/lib.rs, the
# compiler's own Rust. CLAUDE.md's prior is that `compile_instructions` usually
# moves on such an edit through layout alone, and it holds; this is the second
# recorded change small enough to leave it alone, after kanso#1285.

# For scale beside the other two: the module row is 48.8M and the entry row
# 162.2M. This row sits beside the entry row because the two corpora are the
# same shape and both paths merge the dependency program before checking it.

# 2026-09-09 (kanso#1346): library_instructions=162,840,377, a FALL of 129,790
# (-0.0796%) from 162,970,167.
#
# Layout. The reading is the one written on the other two rows: the shipping
# shape adds no work to a successful compile, compile_allocs and
# compile_peak_bytes are byte-identical to main in this job, and all three
# instruction rows fell together.
#
# The note above this one recorded a change small enough to leave the module
# row alone. This is the third kind, and the most useful one for the prior:
# a change that provably adds NO work still moved all three rows, which is
# what makes the layout reading a measurement here rather than an inference.
# 2026-09-09 (kanso#1349): library_instructions=162,839,321, a FALL of 1,056
# (-0.0006%) from 162,840,377.
#
# The fold-seed uniqueness condition. Layout, on the reading written on the
# other two rows. This row moved LEAST of the three in relative terms, 0.0006%
# against 0.0013% and 0.0012%, which is what a layout move looks like and not
# what a change in the work would look like: the library and entry corpora
# name the identical ten imports and compile the same declarations, so a real
# change in analysis work would move them together.
# 2026-09-10 (kanso#1361): library_instructions=163,473,663, a RISE of 41,997
# (+0.0257%) from 163,431,666.
#
# Records print qualified, everywhere. ast::Program gains `root`, the name an
# importer would write for the root module, set where each compile path
# returns: one String per program for a file's stem or a directory's name.
# Everything else the change does is rendering, and `kanso check` never
# renders. All three instruction rows rose together and in one band --
# 0.0353%, 0.0131%, 0.0257% -- which is the layout reading the notes above
# give, with that one String of real per-program work under it.
# 2026-09-10 (the runner image moves, kanso#1361): measured-on takes
# glibc=2.39-0ubuntu8.9 and the row below does not move. Counted 163,473,663 on
# 8.8 in round one and 163,473,663 on 8.9 in round four, same commit content and
# same clang and rustc. The module row's note carries the reading, and
# bench/instructions_golden.txt the account of what the revision did move.
# 2026-09-10 (kanso#1362): library_instructions=163,543,712, a RISE of 70,049 (+0.0429%) from 163,473,663.
#
# The backends build the partial over a value. src/runtime.c is include_str!'d
# into the compiler, so its new k_partial0..4 and the k_call0..4 branches are
# bytes the compiler carries and compiles; src/codegen.rs and src/wasm_backend.rs
# change beside it. The entry and library rows rise together at 0.0433% and
# 0.0429% where the module row rises 0.0266%, which is the split those rows
# already record: the two corpora that merge a dependency program pay the
# larger share.

# 2026-09-10 (kanso#1363): library_instructions=162,541,723, a FALL of
# 1,001,989 (-0.6127%) from 163,543,712.
#
# `done` is minted and std/list's one-field `done` record is deleted. This row
# and the entry row move together again, 0.6127% against 0.6053%, the split the
# notes above already record: the two corpora that merge a dependency program
# pay the larger share, and here they save it.

# 2026-09-10 (kanso#1364): library_instructions=162,023,537, a FALL of 518,186
# (-0.3188%) from 162,541,723.
#
# The fused chain operators. Layout, as on the other two rows. This row and the
# entry row move together again -- 0.3188% against 0.3228% -- which is the pair
# these notes keep recording: the two corpora are the same shape and both merge
# a dependency program before checking it.
# 2026-09-10 (a constructor no longer builds into a record it is about to
# hold): CI's sitting, 162,023,537 -> 162,069,092 (+45,555 / +0.0281%).
# src/linear.rs asks `field_reads_in` of every mention a candidate
# constructor makes, where counting mentions asked nothing; the walk is
# the price of the answer. The three compile rows rise together and by
# nearly the same fraction, which is what a per-declaration check looks
# like when every route runs it.
# 2026-09-11 (a validated string knows its length): 164,324,373 -> 164,342,505,
# a rise of 18,132 (+0.0110%). The same layout move as the other two compile
# rows; see bench/compile_instructions_golden.txt.
# 2026-09-12 (the box check asks a question the program has already answered):
# CI's sitting, 169,284,150 -> 168,056,074, a fall of 1,228,076 (-0.7255%).
# The `any_boxed` hoist; see bench/compile_instructions_golden.txt. Ten
# instructions from the entry row's fall on the same change, which is what
# kanso#1344 found these two corpora to be: the same measurement.
# 2026-09-12 (three paydown rounds on the box check): CI's sitting,
# 168,056,074 -> 166,320,043, a fall of 1,736,031 (-1.0330%). The entry and
# library rows fall within 1,767 instructions of each other on the same change,
# which is kanso#1344's finding restated: the two corpora name the identical
# imports and are the same measurement.
# 2026-09-12 (five compile-cost paydowns in the whole-program checks):
# 164,342,505 -> 160,184,317, a FALL of 4,158,188 (-2.5302%). The same three
# fusions and two necessary conditions; see
# bench/compile_instructions_golden.txt.
# 2026-09-12 (the shadow set deferred, on top of the five paydowns):
# 160,184,317 -> 158,447,681, a further FALL of 1,736,636 (-1.0842%), and
# 164,342,505 -> 158,447,681 against main, a FALL of 5,894,824 (-3.5868%).
# See bench/compile_instructions_golden.txt. This row and the entry row fall
# within 1,490 instructions and 0.0009 percentage points of each other, which
# is what a per-declaration saving looks like on two routes that differ only
# in how they were entered.
# 2026-09-12 (a declaration's callees deduplicated by range, and a dead scan):
# 158,447,681 -> 157,092,747, a FALL of 1,354,934 (-0.8552%). See
# bench/compile_instructions_golden.txt. This row and the entry row have fallen
# together on every round since they were minted; here they part by 0.2725
# percentage points, the widest gap yet. The library route checks a file of
# definitions where the entry route checks statements too, so a saving that
# scales with declarations reaches them in different proportion.
# 2026-09-12 (a name is a type only where a cheap test says it could be):
# 157,092,747 -> 156,463,912, a FALL of 628,835 (-0.4003%). See
# bench/compile_instructions_golden.txt. This row and the entry row are back
# within 0.0104 percentage points of each other after last round's 0.2725,
# which is what a saving keyed to identifiers rather than to declarations looks
# like: both routes walk the same bodies.
# 2026-09-12 (three checks each rebuilt the same (name, arity) table):
# 156,463,912 -> 155,663,482, a FALL of 800,430 (-0.5116%). See
# bench/compile_instructions_golden.txt. This row and the entry row part by
# 0.0096 percentage points, the closest they have ever run.
# 2026-09-12 (the decidable check joins the one descent):
# 155,663,482 -> 155,103,784, a FALL of 559,698 (-0.3596%). See
# bench/compile_instructions_golden.txt. This row and the entry row part by
# 0.0018 percentage points, closer than last round's 0.0096 and the closest
# they have run. Both routes walk the same bodies and the saving is per node,
# so there is nothing left for the two entrances to differ over.
# 2026-09-12 (the shapes walk joins the one descent too) -- SUPERSEDED, see
# below: 155,103,784 -> 154,346,192 was read on the pre-catch-mask base.
# 2026-09-12 (a dispatch group's catch mask is decided once):
# 155,103,784 -> 153,023,863, a FALL of 2,079,921 (-1.3410%). See
# bench/compile_instructions_golden.txt. This row and the entry row part by
# 0.0015 percentage points, the closest they have run: both routes merge the
# same library and call into it the same way, and there is nothing left for the
# two entrances to differ over on a per-call-site saving.
# 2026-09-12 (the shapes walk joins the one descent too, re-measured on the
# catch-mask base):
# 153,023,863 -> 152,261,219, a FALL of 762,644 (-0.4984%). See
# bench/compile_instructions_golden.txt. This row and the entry row part by
# 0.0035 percentage points. Both routes walk the same bodies and the saving is
# per node, so there is nothing for the two entrances to differ over.
# 2026-09-12 (the literal-argument check joins the one descent):
# 152,261,219 -> 150,211,345, a FALL of 2,049,874 (-1.3463%). See
# bench/compile_instructions_golden.txt. This row and the entry row part by
# 0.2840 percentage points, the WIDEST they have run on one of these folds
# against 0.0035 and 0.0015 before it. The library corpus is definitions alone
# and the entry corpus runs statements, and this check keys on call sites in
# bodies: the two corpora hold different mixes of those, which the earlier
# folds' per-node savings could not see.
# 2026-09-12 (the field-read check joins the one descent): 150,211,345 ->
# 149,164,244, a fall of 1,047,101 (-0.6971%). The same fold, on the library
# route.
# 2026-09-13 (the block-born check joins the one descent): 149,164,244 ->
# 148,288,999, a fall of 875,245 (-0.5868%). The library row moves with the
# entry row here, as it does for anything in the shared checks: the fold is in
# `check_merged_after_aliases`, which every route runs.
# 2026-09-13 (Arities::of reads the slot it is about to write): 148,288,999 ->
# 148,091,856, a fall of 197,143 (-0.1329%). `check_named_per_node` runs on
# every route, so this row moves with the other two.
# 2026-09-13 (the effect type): 148,091,856 -> 150,092,130, a RISE of
# 2,000,274 (+1.3507%). `check_box_where_value` runs in the shared checks,
# so this row moves with the entry row to within 0.0022 points.
# 2026-09-13 (the `<t>effect` spelling): 152,460,583 -> 154,382,827, a RISE
# of 1,922,244 (+1.2608%). The library path pays it too, and pays the most of
# the three: a file of definitions alone is all declarations, so the tail walk
# runs on every one of them with no statements to dilute it.
# Ruled 2026-08-29; admitted by the floor under the language clause.
#
# 2026-09-13 (the runtime's counter sites, re-based): library_instructions 154,382,827 -> 154367035,
# a FALL of 15,792 (-0.0102%). CI's sitting on the merged base carrying
# kanso#1395.
#
# THE SIGN FLIPPED WHEN THE BASE MOVED. Against the base kanso#1395 left, the
# same diff read 152,460,583 -> 152,481,754, a RISE of +21,171. That is what this file's own
# warning means by the layout vein: `kanso check lib/json` stops before
# codegen and cannot run a runtime gate, so nothing a counter gate does can
# reach this row. What moves it is that src/codegen.rs and src/main.rs both
# change -- counters_wanted() becomes a function both halves ask, and
# cached_runtime_object keys on it -- and their bytes move where the
# compiler's own code lands. Where it lands depends on everything else in the
# binary, so a base that moved takes the sign with it, and the magnitude on
# compile_instructions came back the same to two instructions.
#
# compile_allocs (30,273) and compile_memory are byte-identical on both bases.
#
# 2026-09-13 (kanso#1405, the digit test asked in place). The change is a
# LIBRARY change -- lib/*.kso is include_str!'d into the compiler -- so all
# four of these move. CI's sitting, against the rows this file held before:
#   compile_allocs        30,273 ->      30,258   (-15)
#   compile_instructions  46,104,930 ->  46,072,247  (-32,683, -0.0709%)
#   entry_instructions    153,613,687 -> 153,586,146 (-27,541, -0.0179%)
#   library_instructions  154,370,895 -> 154,378,731 (+7,836,  +0.0051%)
# Three fall and the library row rises; the source is eight lines shorter and
# one binding lighter, and the library row is the layout vein reacting to a
# compiler whose bytes moved. Runtime did NOT move: `work:success` on the same
# run, runbench 2,003,021,871, identical to its golden.
# 2026-09-13 (kanso#1409): library_instructions 153,175,369 -> 152,469,420, a
# FALL of 705,949 (-0.4609%). The second fused descent; see the compile row.
# 2026-09-13 (kanso#1413): library_instructions 151,715,592 -> 150,868,905, a
# FALL of 846,687 (-0.5581%). The box question joins the second fused descent.
# 2026-09-13 (kanso#1415): library_instructions 150,868,905 -> 148,544,439, a
# FALL of 2,324,466 (-1.5407%). The qualified-name join; see the compile row.
# 2026-09-14 (kanso#1416): library_instructions 148,544,439 -> 147,572,025, a
# FALL of 972,414 (-0.6546%). The borrowed dedup keys; see the compile row.
# 2026-09-14 (kanso#1414): library_instructions 147,572,025 -> 147,378,070, a
# FALL of 193,955 (-0.1314%). Summed across the three the compile term moves
# -508,707 (-0.1503%). All three fall together, which is what a decision the
# front end stops making looks like: the entry and library rows track each
# other to 84 instructions because they run the same passes over corpora of
# the same shape.
# 2026-09-14 (kanso#1417): library_instructions 147,378,070 -> 147,374,531, a
# FALL of 3,539 (-0.0024%). The k_b_at arm order; see the compile row. All
# three rows fall and compile_allocs does not move at all, which is the
# signature of a layout move: the same decisions, reached through a binary
# whose bytes sit differently. The runtime vein is where this change was
# actually aimed, and it is the one that carries the win.
# 2026-09-14 (kanso#1418): library_instructions 147,378,070 -> 144,858,538,
# a FALL of 2,519,532 (-1.7096%). The advisory worklist; see the compile row.
# Summed across the three the compile term moves 337,862,334 -> 331,792,865,
# -6,069,469 (-1.7963%). The entry and library rows track each other to 2,213
# instructions because they run the same passes over corpora of the same
# shape.
# 2026-09-14 (kanso#1417, FOURTH sitting): library_instructions 144,858,538 ->
# 144,834,698, a FALL of 23,840 (-0.0165%). Summed across the three the
# compile term moves -54,168 (-0.0163%).
# 2026-09-14 (kanso#1417, FIFTH sitting): library_instructions 144,842,133 ->
# 144,837,840, a FALL of 4,293 (-0.0030%), on the base kanso#1419 left.
# Summed across the three the compile term moves 331,756,407 -> 331,746,012,
# -10,395 (-0.0031%).
#
# FIVE SITTINGS OF ONE DIFF, AND THE SPREAD IS AN ORDER OF MAGNITUDE. The
# same three lines of src/runtime.c have now been measured against five bases:
#
#     base          module      entry     library      summed
#     kanso#1415       +35     -1,854        +230      -1,589
#     kanso#1416      +883     -2,236      -2,878      -4,231
#     kanso#1414      -300     -2,921      -3,539      -6,760
#     kanso#1418    -8,216    -22,112     -23,840     -54,168
#     kanso#1419    -1,325     -4,777      -4,293     -10,395
#
# The module row changes sign twice across the five. The fourth is eight times
# the third and thirty-four times the first, and it is the only one where all
# three rows fall together by a comparable share; the fifth is a sixth of it. Nothing about the diff
# changed between them: it is three lines swapping two arms of a tag switch,
# and `kanso check` never executes one of them.
#
# What changed is the binary the rows are measured on. kanso#1418 rewrote the
# advisory fixpoint to revisit on demand, which moved the compiler's own code
# and every address after it; the reorder's bytes then land somewhere else
# again. A layout delta is a property of one arrangement, and four bases are
# four arrangements.
#
# So the practical rule, which cost this branch six rounds to learn: a compile
# row on a runtime-only change is regenerated from whatever base CI last ran,
# stated, and not reasoned from. It carries no information about the change
# and the objective cannot see it -- fifty-four thousand instructions on three
# hundred and thirty-two million is a sixtieth of a per cent, and ten thousand
# is a three-hundredth. The work vein is where this change was aimed and where
# its win is.
# 2026-09-14 (kanso#1419): ryu_d2d removes digits two at a time. This is a
# RUNTIME change -- src/runtime.c, one function -- and the library row moved
# anyway: 144,836,225 -> 144,842,133, +5,908 (+0.0041%). On the kanso#1418 base the same diff read
# -22,292 (-0.0154%); on the kanso#1423 base it RISES. Same bytes,
# opposite sign, which is the whole of what a layout delta carries.
#
# `kanso check` stops before codegen, so no decision this row counts can have
# been altered by a runtime edit. What moved is the LAYOUT: src/runtime.c is
# compiled into the compiler and its bytes shift what sits where. CLAUDE.md
# has the rule and this row is another instance of it -- regenerate from
# whatever base CI last ran, say so, and do not reason from the size.
#
# The size especially. kanso#1417 read the same three-line runtime diff on
# four bases and got four different answers, the module row changing sign
# twice and the fourth reading eight times the third. A layout delta is a
# property of one binary's arrangement and carries no information about the
# change that provoked it.
#
# compile_allocs held at 27,937 and compile_memory is byte-identical, which
# is the expected pair: those two really cannot move on a runtime edit.
#
# 2026-09-15 (kanso#1427): the leading-zero skip in k_b_to_float, a RUNTIME
# change in one function, and this row moved anyway: 144,837,840 -> 144,841,583, +3,743
# (+0.0026%), on the base kanso#1417 left. Layout, as the entry above says:
# `kanso check` never reaches the runtime, and src/runtime.c is compiled into
# the compiler. Regenerated from the base CI last ran, stated, not reasoned
# from. compile_allocs held at 27,937 and compile_memory is byte-identical.
#
# 2026-09-15 (kanso#1428): the int parser's cold tail outlined, a RUNTIME
# change in one function, and this row moved anyway: 144,841,583 -> 144,841,869, +286
# (+0.0002%), on the base kanso#1427 left. Layout, as the entries above say;
# regenerated from the base CI last ran, stated, not reasoned from.
# compile_allocs held at 27,937 and compile_memory is byte-identical.
#
# 2026-09-15 (kanso#1429): six cold helpers carry preserve_most and three
# arms are outlined, a RUNTIME change, and this row moved anyway: 144,841,869 ->
# 144,836,225, -5,644 (-0.0039%), on the base kanso#1428 left. Layout, as the entries
# above say; regenerated from the base CI last ran, stated, not reasoned
# from. compile_allocs held at 27,937 and compile_memory is byte-identical.
#
# 2026-09-15 (kanso#1430): a long copy is a cold call, a RUNTIME change,
# and this row moved anyway: 144,836,225 -> 144,841,583, +5,358 (+0.0037%), on the base
# kanso#1429 left. That is the value the row held before kanso#1429, to the
# instruction: the layout moved back. Regenerated from the base CI last
# ran, stated, not reasoned from. compile_allocs held at 27,937 and
# compile_memory is byte-identical.
#
# 2026-09-15 (kanso#1431): the third cold-frame sweep, a RUNTIME change,
# and this row moved anyway: 144,841,583 -> 144,837,205, -4,378 (-0.0030%), on the base
# kanso#1430 left. The compiler's own bytes and the layout under them move
# with src/runtime.c. Regenerated from the base CI last ran, stated, not
# reasoned from. compile_allocs held at 27,937 and compile_memory is
# byte-identical.
#
# 2026-09-15 (kanso#1432): an empty list literal opens with room for four, a
# RUNTIME change, and this row moved anyway: 144,837,205 -> 144,838,028, +823 (+0.0006%),
# on the base kanso#1431 left. The compiler's own bytes and the layout under
# them move with src/runtime.c. Regenerated from the base CI last ran,
# stated, not reasoned from. compile_allocs held at 27,937 and
# compile_memory is byte-identical.
#
# 2026-09-15 (kanso#1433): the two byte scanners are inlined, and their constants
# fold, a RUNTIME change, and this row moved anyway: 144,838,028 -> 144,845,808, +7,780 (+0.0054%),
# on the base kanso#1432 left, and 144,841,869 -> 144,845,808 against main. The compiler's
# own bytes and the layout under them move with src/runtime.c. Regenerated
# from the base CI last ran, stated, not reasoned from. compile_allocs held at
# 27,937 and compile_memory is byte-identical.
#
# 2026-09-15 (kanso#1435): a scan's short tail is one masked load, a RUNTIME
# change, and this row moved anyway, a FALL: 144,845,808 -> 144,836,246, -9,562 (-0.0066%),
# on the base kanso#1433 left, and 144,836,225 -> 144,836,246 against main (+21). The compiler's
# own bytes and the layout under them move with src/runtime.c. Regenerated
# from the base CI last ran, stated, not reasoned from. compile_allocs held at
# 27,937 and compile_memory is byte-identical.
#
# 2026-09-15 (kanso#1436): the counters a shipped binary was still counting,
# a RUNTIME change, and this row moved anyway, a RISE: 144,836,246 -> 144,840,900,
# +4,654 (+0.0032%), on the base kanso#1435 left, and 144,836,225 ->
# 144,840,900 against main (+4,675). The compiler's own bytes and the layout
# under them move with src/runtime.c. Regenerated from the base CI last ran,
# stated, not reasoned from. compile_allocs held at 27,937 and
# compile_memory is byte-identical.
#
library_instructions=144840900
