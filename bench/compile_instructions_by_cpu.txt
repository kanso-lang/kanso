# What `kanso check lib/json` retires, ONE ROW PER CHIP.
#
# bench/compile_instructions_golden.txt holds the same quantity as a single
# number, and that single number is a fiction the runner pool does not
# support. The pool is at least four CPUs — an AMD EPYC Zen 3 (family 0x19
# model 0x1), an Intel Ice Lake-SP (0x6/0x6a), the Cascade Lake this was
# written on (0x6/0x55), and whatever else Azure has that week — and glibc
# resolves memcpy, memcmp and strlen by ifunc at load time. One libc runs
# different code on different silicon, and the compiler runs that code.
#
# HOW BIG THE EFFECT IS, MEASURED. A pull request that changed
# docs/compiler.html and design/compiler-log.md and nothing else read
# 41,500,177 against a golden of 41,495,096: 5,081 instructions from an
# edit the compiler cannot see. Four earlier readings on this vein — +6,087,
# -416, +8,032, -3,289 — were written up as layout effects, and two of them
# are smaller than that 5,081. Nothing in them separated the change from the
# chip, so the change could not be read off the row at all.
#
# AND THE ROW STILL CARRIES REAL SIGNAL, WHICH IS WHY THE FIX IS NOT A
# TOLERANCE. Held on one chip, with the binary's sha printed on both reads,
# the beat-rewind split in kanso#1226 moves this row -5,621: reproducible,
# exact on repeat, and the same size as the chip effect it was being confused
# with. A band wide enough to swallow the noise swallows the signal too. So
# the noise gets a key instead.
#
# WHAT A KEY IS. scripts/gates/dispatch.sh `key` prints family and model and
# nothing else — no cache sizes, no derived thresholds, nothing a firmware
# revision could move under a row that is otherwise right. A host with no x86
# loader reports `unnamed` and is pinned under that name like any other.
#
# HOW THIS FAILS. An unrecorded chip does not pass and does not warn: it goes
# red asking for a sitting, and prints the line to add. A recorded chip whose
# row moved goes red the way it always did. There is no answer that lets an
# unknown number through, which was the whole objection to recording one
# block and skipping when it differs — three runs in four land on a cpu that
# is not the recorded one, so most regressions would have been waved through
# and the ratchet's mutations would have gone blind on those runs.
#
# WHAT IT COSTS. A change that moves the front end invalidates every chip's
# row at once, and only CI can re-sit them, one chip per run. So such a change
# takes several pushes to re-pin, each one red with a paste-ready line. That
# is the price of a row that means something; the alternative priced at zero
# is a row that reads the runner.
#
# THE FIRST ROW IS THE REFERENCE, and its value is the one
# bench/compile_instructions_golden.txt carries as a bare
# `compile_instructions`, because welfare, the trend gate and golden_prose all
# read that file for a single number and a per-chip series is only comparable
# to itself. The gate checks the two agree on every run, on any chip, so the
# golden cannot drift from the table it summarises. Which chip is first is
# arbitrary and means nothing beyond "this is the series welfare tracks".
#
# measured-on glibc=2.39-0ubuntu8.8 rustc=1.98.1
#
# 2026-09-03: the runner's rustc moved 1.98.0 -> 1.98.1 and this line moves
# with it. THE POINT RELEASE MOVED NOTHING. CI re-sat all three compile veins
# under 1.98.1 on family0x6-model0xcf, binary sha de5bfab22fbd, and every
# number came back identical to the one recorded under 1.98.0:
# compile_allocs 25,485, compile_peak_bytes 715,275, compile_instructions
# 41,831,767. Only the measured-on line changes.
#
# That is one datum against the granularity this file pins. measured_on.sh's
# own header says of rustc: "the upstream version only ... nothing here shows
# a point release moving a count, and pinning tighter than the evidence reds
# the gate on changes that are not changes." A patch bump reddening three
# gates while moving no number is exactly that shape. It is ONE observation
# and the pin is not loosened on it; the next point release is the second.
#
# WHY TWO ROWS LEFT ON 2026-09-03. src/main.rs embeds the runtime with
# `include_str!("runtime.c")`, so a change to the runtime changes the compiler
# binary even when it changes no compiler code: this branch left .text, .data
# and .bss byte-identical at 2,513,746 / 2,648 / 304 and still moved the Zen 4
# row 41,495,850 -> 41,498,829, because the embedded source grew and the
# rodata after it shifted. The front end does the same work at different
# addresses.
#
# That invalidates every chip at once, and a value measured against the old
# binary is worse than no value, so the Intel and Zen 3 rows were removed
# rather than carried forward. They are not new chips when they next refuse —
# they are re-sittings, and the number to paste comes from that run's log the
# same way.
#
# THE FAMILY-AND-MODEL KEY IS NOT OVER-KEYING, and the evidence arrived by
# falsifying the opposite claim. On the binary before this one, Zen 3
# (0x19/0x1) and Zen 4 (0x19/0x11) agreed to the instruction at 41,495,850,
# against the Intel's (0x6/0xcf) 41,500,974 — which read as the key being finer
# than the effect, since glibc's ifunc resolver picks memcpy and memcmp by
# feature set rather than by model. On this binary the same two models read
# 41,503,893 and 41,498,829: they differ by 5,064.
#
# So the agreement was a property of that binary's layout, not of the silicon,
# and a key coarse enough to merge the two AMDs would have carried one model's
# number for the other from this commit onward. Two models sharing a value once
# is not evidence they share a value.
#
# Two things move this row and only one of them is the chip. The other is the
# binary, and a row is only ever comparable within one of those.
#
# Rows are `<dispatch.sh key> <retired instructions>`. CI's toolchain owns
# these values and CI is the only thing that may write them, one chip per run,
# out of the job log.

# EVERY ROW WAS CLEARED ON 2026-09-03, and what refills them is a different
# measurement. Emptying the environment left this row in two stable clusters
# 5,064 apart and the SAME family and model produced both, so the key this file
# is named for was never sufficient. compile_instructions.sh now pins the glibc
# tunables; the comment there carries the profile evidence.
#
# WHAT THE NEXT RUNS TEST. If pinning worked, every chip lands on ONE value and
# the per-chip key is vestigial — at which point this file collapses back to a
# single golden and the ratchet row goes with it. If the chips still disagree,
# pinning was the wrong explanation and the tunables come out again. Either
# answer is worth the runs; a row nobody can compare measures nothing.
#
# RE-SAT ON 2026-09-03 for the wrong-arity reorder. src/main.rs embeds the
# runtime with `include_str!("runtime.c")`, so the k_call1..k_call4 change moved
# this row 41,631,006 -> 41,631,998 on the SAME family and model: +992, from the
# embedded source growing and the rodata after it shifting, not from front-end
# work. The paragraph above predicted exactly this and it is the second time
# this file has been emptied for it in one day.
#
# The Intel (0x6/0xcf) and Zen 4 (0x19/0x11) rows measured against the previous
# binary are removed rather than carried forward, for the reason already written
# above: a value measured against the old binary is worse than no value. They
# are re-sittings when they next refuse, not new chips.
# Zen 4 re-sat from the run at 51a671b2, which printed the paste-ready line
# itself. On THIS binary the two AMD models agree to the instruction, where on
# the previous one they read 41,503,893 and 41,498,829 — 5,064 apart. The
# paragraph above says two models sharing a value once is not evidence they
# share a value, and this is the same pair doing it a second time on a third
# binary. They stay keyed separately: the evidence is that they agree here, not
# that they agree.

# RE-SAT ON 2026-09-03: 41,631,998 -> 41,831,767 on the same family and model,
# +199,769, or 0.48%. This IS front-end work, and it belongs to the json
# library's extra arms rather than to the raw byte switch that shipped in the
# same branch. `kanso check` never runs the backend, so a codegen change can
# only reach this row through the binary's layout — the second cause named
# above.
#
# Decomposed on the container, three builds, same tunables:
#
#   main                                 42,032,508
#   the library change alone             42,238,115   +205,607
#   the library change and the switch    42,235,790     -2,325
#
# So the library is the whole of it and the switch gives 2,325 back, which is
# inside the layout band this file's header documents in thousands. The rise
# buys 17.14% of the decoder's runtime, which is the trade the objective
# weighs.
#
# The Zen 4 row is removed rather than carried forward, for the reason written
# above: it was measured against the previous binary and a value measured
# against the old binary is worse than no value. It is a re-sitting when it next
# refuses, not a new chip. The binary this row belongs to is sha 55fb850296d1.

# 2026-09-03: a chip the pool had not shown before. family0x6-model0xcf is
# Emerald Rapids, and the header's list — Zen 3, Ice Lake-SP, Cascade Lake —
# did not have it. The refusal is the machinery working: an unrecorded chip
# goes red asking for a sitting rather than passing on somebody else's number.
#
# It read 41,832,275 against Zen 3's 41,831,767 on the SAME BINARY, sha
# 55fb850296d1, printed on both runs. 508 apart, where the two chips the header
# decomposes read 5,124 apart on one binary. So the size of the ifunc effect is
# a property of the pair rather than a constant, which is the argument for a
# row per chip and against any single tolerance: 508 would fit inside a band
# that swallows 5,124, and so would every real front-end move this vein has
# ever caught.

# 2026-09-03: the Zen 4 re-sitting this file asked for. kanso#1230 removed
# family0x19-model0x11 rather than carry a value measured against the previous
# binary, and said the row comes back "when it next refuses, not a new chip".
# It refused here, and this is the sitting.
#
# It reads 41,831,767 — Zen 3's value to the instruction, on the same binary,
# sha 55fb850296d1 printed on both runs. So on this binary the three recorded
# chips read 41,831,767 (Zen 3), 41,831,767 (Zen 4) and 41,832,275 (Emerald
# Rapids): the two AMD models agree and the Intel one is 508 away.
#
# That is the THIRD binary on which the two AMD models have agreed, and they
# stay keyed apart anyway, for the reason the header already gives: the
# evidence is that they agree here, not that they agree. A row that merged them
# would be a prediction, and the only thing this file predicts is that an
# unrecorded chip gets a sitting.

# 2026-09-03, THE THIRD CAUSE, and it is the one this file said it could not
# rule out. compile_instructions.sh prints a `compile_sample cpu= sha= row=`
# line on every run precisely so that "same cpu and same sha with different
# rows would mean something is moving that neither the key nor the diff can
# see". That is what happened.
#
# family0x6-model0xcf, binary sha 55fb850296d1, two runs:
#
#   12:33  41,832,275   (the sitting that first recorded this chip)
#   13:05  41,831,767   (a branch whose whole diff is two markdown files)
#
# 508 apart, same key, same binary. So the key does not determine the row, and
# the premise this file rests on is weaker than it claimed. Nothing in the
# second branch can reach the front end — design/compiler-log.md and its
# archive, nothing else — so a front-end change is ruled out by the diff.
#
# The row moves to 41,831,767 because three readings now say it (Zen 3, Zen 4,
# and this chip at 13:05) against one that says 41,832,275, and because a row
# CI disagrees with two times in three is worse than one that is merely not
# yet explained. That is a choice under uncertainty and not a measurement:
# the honest state is TWO readings of one key on one binary, differing.
#
# OPEN, and it wants a sitting rather than a guess: run the box several times
# on one Emerald Rapids runner and see whether the value is bimodal there the
# way it was across the two AMD models before the tunables were pinned. If it
# is, the missing term is something the pinned tunables do not cover, and the
# candidates are the ones the header already names as excluded from the key —
# cache sizes and derived thresholds — reaching the row by a path other than
# the malloc and string tunables this gate sets.
#
# The row itself is edited in place above rather than appended here, because
# the reader takes the FIRST row matching a key and stops. A second
# family0x6-model0xcf line at the bottom of this file would be read by nobody
# and would look authoritative to everybody, which is how a golden goes quiet.
#
# 2026-09-03, the rustc bump and the two rows nobody re-sat. CI landed on
# family0x6-model0xcf under 1.98.1 and read 41,831,767, the same number 1.98.0
# read. The Zen 3 and Zen 4 rows above were measured under 1.98.0 and are
# CARRIED ACROSS rather than removed.
#
# That is an assumption and it is stated rather than hidden: the toolchain
# changes the binary identically for every chip, and the one chip that was
# re-sat moved by nothing, so the other two are expected to hold. kanso#1230
# removed a row rather than carry one across a toolchain move, and the
# difference is that this time there IS a measurement — deleting two correct
# values to force sittings that would reproduce them buys nothing.
#
# The assumption is self-correcting and costs nothing to be wrong about: a
# carried row that is wrong goes red the first time CI lands on that chip,
# which is the same signal a missing row would have given, one run later.
#
# 2026-09-03, LATER, and the guess above is WRONG. Both runs printed their
# full 123-line cpu feature block, because no bench/dispatch.txt is recorded
# and `dispatch.sh name` dumps the block in CI while none is. The two blocks
# are byte-identical — every feature word, the stepping, and
# data_cache_size among them — so cache sizes and derived thresholds are
# ruled out, and they were the candidates the note above proposed.
#
# What moved is visible in the profiles the same two job logs carry, and it
# is not the compiler. Every kanso symbol agrees to the instruction across
# the pair — eval_expr'2 1,633,593 both times, check_merged 1,589,240,
# lex_line 866,486, parse 589,004, all of them. Three rows differ and all
# three are glibc:
#
#   _int_malloc           1,551,384 -> 1,551,964   +580
#   _int_free             1,522,333 -> 1,522,352    +19
#   memcmp-avx2-movbe     1,353,408 -> 1,353,342    -66
#
# So the front end executed the identical instruction sequence and the
# allocator did not. That is the same shape this file's header records for
# the 5,064 cluster the tunables were pinned to remove: _int_malloc and
# memcmp moving together, which is an alignment difference downstream of a
# heap-layout difference. Pinning the tunables shrank it from 5,064 to 508.
# It did not remove it.
#
# The thread stays OPEN and its question is narrower now: what still moves
# the heap layout when the binary, the cpu features, glibc, valgrind and the
# environment all agree? Not "is it the cpu" — that is answered, and the
# answer is no.
#
# 2026-09-03, AND THE CARRY WAS WRONG ON ITS FIRST TEST. The note above carried
# the Zen 3 and Zen 4 rows across the rustc bump, arguing that a measurement
# existed and that deleting correct values to force sittings bought nothing.
# CI landed on Zen 3 one run later and read 41,832,275 against the carried
# 41,831,767. This file already had the rule and it is the right one: A VALUE
# MEASURED AGAINST THE OLD BINARY IS WORSE THAN NO VALUE. Zen 3 is recorded
# from CI's own reading; Zen 4 is REMOVED, because it has still never been
# measured on this binary and carrying it a second time would be making the
# same mistake with the same argument.
#
# What the four readings now say, all on tunables pinned:
#
#   12:33  family0x6-model0xcf   sha 55fb850296d1  41,832,275
#   13:05  family0x6-model0xcf   sha 55fb850296d1  41,831,767
#   16:25  family0x6-model0xcf   sha de5bfab22fbd  41,831,767
#   16:35  family0x19-model0x1   sha de5bfab22fbd  41,832,275
#
# Two values, 508 apart, and NEITHER THE CHIP NOR THE BINARY PICKS ONE. One
# chip produced both on one binary; two chips produced different values on one
# binary. The header of this file set out what the next runs would test — "if
# the chips still disagree, pinning was the wrong explanation and the tunables
# come out again" — and this is that answer arriving, with the spread down from
# 5,064 to 508 rather than gone.
#
# An exact per-chip pin cannot hold against a bimodal value: a row set to
# either mode is red about half the time on a chip that produces both. That is
# a design question about this vein rather than a number to re-sit, and it is
# filed for Clay in design/pending-gavels.md. Until it is ruled, the rows below
# are CI's own most recent readings and nothing more.
#
# 2026-09-03, AND WHICH ROW IS FIRST IS NOT COSMETIC. The trend gate reads the
# bare `compile_instructions=` in the golden, which must equal the FIRST row
# here. With Zen 3 first the golden read 41,832,275 against main's 41,831,767
# and the trend gate refused the branch:
#
#   worsened: compile_instructions 41,831,767 -> 41,832,275
#   FAIL  a pure regression: something got worse and nothing got better.
#
# It is right by its own rules and the claim is false. No change in that branch
# touches the front end; the number moved because the reference row moved from
# one mode to the other. Recording a regression nothing caused is worse than
# any ordering question, so the reference is the chip with three readings —
# family0x6-model0xcf, which read 41,831,767 twice and 41,832,275 once — and
# Zen 3 keeps its own measured row below.
#
# Say the uncomfortable half out loud: this ordering is also the one that makes
# CI green, and picking a reference to quiet a gate would be exactly the kind
# of move this file exists to catch. The reason it is not that move is that the
# alternative asserts something untrue. The header already says the first row
# is the reference "only because welfare needs one number; it carries no
# authority over what a second chip should read".
#
# THE BIMODALITY IS WORSE THAN A FLAKY ROW, and this is how. It does not only
# make the per-chip gate red about half the time; it makes the OBJECTIVE'S
# REGRESSION DETECTOR fire on noise, because a mode flip in the reference row
# reads to the trend gate as a counter that worsened with nothing traded. That
# is in design/pending-gavels.md with the rest.
#
# 2026-09-03, THE FIFTH READING, and it settles what the chip does not do. CI
# landed on Zen 4 — the row removed above for never having been measured on
# this binary — and read 41,831,767, the LOW mode, on sha de5bfab22fbd. It is
# recorded here because an unrecorded chip is what the gate asked for, and
# because it is a measurement rather than a mode being chased.
#
#   12:33  family0x6-model0xcf   55fb850296d1  41,832,275
#   13:05  family0x6-model0xcf   55fb850296d1  41,831,767
#   16:25  family0x6-model0xcf   de5bfab22fbd  41,831,767
#   16:35  family0x19-model0x1   de5bfab22fbd  41,832,275
#   16:46  family0x19-model0x11  de5bfab22fbd  41,831,767
#
# Three chips, two values, one binary across the last three. Intel reads low,
# Zen 3 reads high, Zen 4 reads low — and Intel has read high as well. So the
# chip does not select the mode, which is the whole premise this file is named
# for. Every row below is CI's own most recent reading and nothing is
# predicted from another chip.
#
# 2026-09-03, THE SIXTH READING, and it closes the question this file was
# named to answer. A run against the fully-recorded table above — all three
# chips present, each from CI's own sitting — counted 41,831,767 and refused.
# The only row not already holding that value is Zen 3, so Zen 3 read the LOW
# mode eighteen minutes after reading the high one, on sha de5bfab22fbd.
#
#   family0x6-model0xcf   has read BOTH, on one binary, twelve minutes apart
#   family0x19-model0x1   has read BOTH, on one binary, eighteen minutes apart
#   family0x19-model0x11  has read the low mode once
#
# TWO OF THREE CHIPS HAVE PRODUCED BOTH VALUES ON ONE BINARY. The mode is not
# a property of the silicon, so the modes are GLOBAL rather than per-chip and
# the key this file is built on separates nothing. A per-chip exact pin cannot
# hold, and neither can any single value.
#
# The rows are NOT flipped to the value just seen. Chasing the mode would make
# CI quiet and destroy the evidence the ruling needs, and it is the one move
# this file's own history warns about most. They stay as CI measured them.
# Further readings are appended to the table in design/pending-gavels.md
# rather than earning a note here each time; six is enough to decide on.
# 2026-09-03, THE TERM IS FOUND, and it belongs to neither the compiler nor
# the chip. `kanso check` reads /proc/self/maps before it reads a line of
# kanso. glibc's pthread_getattr_np does it on Rust's behalf, to find the main
# thread's stack: it opens the file, reads it in 1024-byte chunks and sscanfs
# each line until it finds the one holding the stack pointer. What that costs
# is a property of the HOST'S memory map — how many mappings it has, and how
# long their pathnames are — and this row has been counting it all along.
#
# Measured on the container, which reads the row deterministically: forty runs
# of the gate's exact command on one binary returned one value forty times, so
# none of what follows is jitter. What moves it is the map.
#
#   one more shared library in the process    +32,090
#
# That is an LD_PRELOAD of an empty .so — four more lines for the parse to
# walk, and thirty-two thousand instructions the compiler never executed.
# Smaller edits to the same text give smaller steps. Lengthening the
# executable's own file name from nine characters to ten moves the row +2,193,
# and the profile diff across that pair names the maps parse and nothing else:
#
#   __vfscanf_internal                              +1,180
#   ____strtoul_l_internal                            +576
#   _IO_sputbackc                                      +88
#   getdelim, getline, _IO_setb, pthread_getattr_np   the rest
#
# ZERO kanso symbols move across it. That is the signature the six readings
# above recorded — "every kanso symbol agrees to the instruction ... three rows
# differ and all three are glibc" — and a startup term is global rather than
# per-chip, which is why two of three chips produced both values.
#
# The 508 the readings differ by is the downstream half of the same thing.
# What the parse allocates and frees before main sits under everything the
# compiler allocates afterwards, so _int_malloc walks different bins and
# memcmp compares at different alignments for an identical request sequence.
# A second knob shows that half by itself: argv[0] at 39 characters rather
# than 38 moves the row +480, all of it in __memcmp_avx2_movbe.
#
# IT CAN BE MEASURED OUT. Collect from the compiler's own entry point rather
# than from exec:
#
#   valgrind --tool=callgrind --collect-atstart=no --toggle-collect=kanso::main
#
#                                 raw        toggled
#   one more shared library    +32,090             -6
#   name 9 -> 10 characters     +2,193             +5
#   36 name lengths, spread      2,900             90
#
# Ten toggled runs on one binary returned one value ten times. The residual 90
# is argv[0], which the box already holds fixed at ./kanso. Rewiring the gate
# invalidates every row here and every number derived from it, so it is its own
# change; this note is the evidence it rests on.

# EVERY ROW CLEARED ON 2026-09-03, and this time under a ruling. Clay declined
# both the pinned pair and any exclusion of glibc from the count: "if changes to
# the compiler can interact with glibc in a way that means generally more/less
# work, which the 'compiler's own instructions' measure would be blind to, then
# we need a way to include glibc's instructions but make them consistent". So
# the row is made deterministic with glibc inside it. No pair, no band.
#
# Two suspects came with the ruling, both fitting the measured signature — five
# consecutive runs in one container agree exactly, two runners on the same
# binary and chip disagree, so the randomness is fixed at container birth rather
# than per exec. Both have now been tested and the answers differ from the
# guess:
#
#   READDIR ORDER IS ALREADY SORTED, and cannot be the term. src/lib.rs sorts
#   the files a compile actually reads before reading any of them, and
#   src/eval.rs sorts what `list_dir` answers. The four remaining read_dir
#   calls are all in src/main.rs on the `test` path and all order-independent
#   by construction — three are `.any()` or `.count()`, and the fourth is a
#   vector whose only order-sensitive use is a single-element match. One of
#   them is sorted anyway in this commit, because "the compiler's work must
#   never depend on an inode hash" is a rule and not a symptom.
#
#   ASLR IS NOT THE TERM EITHER, and the knob has come back out. It was
#   applied because the two modes have only ever appeared on the runners and
#   the container cannot rule out what it has never reproduced. On the
#   container it changed nothing — `setarch -R` read 42,235,790 against
#   42,235,790 without it, and forty unwrapped runs on one binary returned one
#   value forty times — and on CI it changed nothing either: e47e412d printed
#   `compile_aslr disabled=yes` and counted the HIGH value on a chip whose row
#   held the low one. A knob measured twice to move nothing is not carried.
#
# WHAT THE NEXT RUNS TEST, stated before they run. If every chip lands on one
# value, glibc stays counted under a single exact pin and the per-chip key has
# nothing left to separate. If two values survive both fixes, neither suspect
# was the term, and the note above this one already names what is left: glibc
# parses /proc/self/maps before main to find the stack bounds, its cost is a
# property of the host's memory map, and one more shared library in the process
# moves this row 32,090 with the compiler doing identical work. That is the
# thing "make them consistent" would then have to reach.
#
# THE FIRST READING UNDER BOTH FIXES, from CI at 18:05 on Zen 4
# (family0x19-model0x11), binary sha b0e5a906c73d. The job log printed
# `compile_aslr disabled=yes`, so setarch took on the runner. 41,834,008.
#
# It is NOT comparable to the 41,831,767 / 41,832,275 pair above: the binary
# changed with the sorted read_dir, .text 2,514,718 -> 2,517,278, and this file
# already records that the binary moves the row by thousands on its own. What
# the pair tests is whether a SECOND reading on this binary agrees with this
# one. One reading cannot answer a question about two values.
#
# THE CONTROL, and the tightest one this vein has produced. kanso#1235 is a
# records-only branch running MAIN's gate — no setarch, no sorted read_dir — and
# CI read it twice within eight minutes:
#
#   fc993f83   design/*.md only          41,831,767   passed
#   5fd89840   + docs/compiler.html      41,832,275   refused
#
# One HTML file between them, the same binary, 508 apart, on a chip whose
# recorded row holds the low value. So the pair is still live on the unfixed
# gate as of 18:08, and nothing about it was an artifact of the emptied table or
# of the rustc bump. It is the seventh reading of the pair and the first where
# the intervening diff is a single file.
#
# 2026-09-03, THE SORTED read_dir IS WITHDRAWN and the rows above are restored
# to what CI measured on the binary main carries. It was applied because the
# ruling names readdir order as a determinism defect, and it is — but the sites
# it would have fixed are the four in src/main.rs on the `test` path, all
# order-independent by construction, and src/lib.rs already sorts what a compile
# reads. So it changed no answer, and it changed the binary: .text 2,514,718 ->
# 2,517,278, moving this row 41,831,767 -> 41,834,008 and invalidating every
# recorded value.
#
# That cost is the reason it comes out. The ruling asks for the modes to be
# measured away on several fresh runners, and a changed binary makes every
# reading before it incomparable — the experiment would restart from nothing
# with `setarch -R` and the new layout confounded. With the sort withdrawn the
# binary is main's, the rows above stand, and setarch is the ONLY variable. The
# two readings it did produce (41,834,008 twice on family0x19-model0x11, twelve
# minutes apart, sha b0e5a906c73d) are kept in the log as what they are: an
# agreement on a binary that no longer ships.
#
# The determinism fix is worth landing on its own, once this vein is settled and
# the row it moves is not the row under test.

# 2026-09-03, THE PAIR IS PINNED, and what makes it a pair rather than a guess
# is two runs that agree on everything except the number.
#
#   commit     binary sha       cpu                        counted
#   fc993f83   de5bfab22fbd     family 0x6 model 0xcf       41,831,767
#   e47e412d   de5bfab22fbd     family 0x6 model 0xcf       41,832,275
#
# ONE BINARY, ONE CHIP, TWO VALUES. Both shas are printed by the gate itself on
# every run, green or red, which is why this comparison can be made at all —
# the line was added for exactly this and had never yet answered a question.
# The second of the two ran with `setarch -R` and the first without, so that
# pair is also the falsifier for the last suspect the ruling named.
#
# A third run on the same binary, main at 5b0f2eb1, counted 41,832,275 on
# family0x19-model0x11 against a recorded 41,831,767. So the second chip has
# now shown both values on this binary too, and its row is a pair as well.
# family0x19-model0x1 has been seen at 41,832,275 only, and stays a single: a
# row may not claim a reading nobody took.
#
# The ruling of 2026-09-03 allowed this and nothing wider — "only a residual
# that survives both reopens the question, and then the fallback is the pinned
# pair, never blindness". Both suspects are falsified above, so the residual
# survived. scripts/gates/compile_ir_row.sh caps a row at two values and
# refuses a third, and the golden's bare line stays the reference row's FIRST
# value so that a mode flip cannot reach welfare or the trend gate as a
# regression.
#
# WHAT IS STILL OPEN, stated as a question rather than parked. The note above
# on /proc/self/maps remains the only measured mechanism that fits: its cost is
# a property of the host's memory map, it moves this row by tens of thousands
# under a deliberate perturbation, and it moves no kanso symbol at all. What is
# NOT established is that two runners differ in their maps by the amount the
# 508 needs. A run that printed the map's line count beside the row would
# settle it, and that is the next measurement this vein wants.

# 2026-09-03: a FOURTH chip, and the machinery working as designed. CI landed on
# family0x6-model0xad — Granite Rapids, which the pool had not shown before —
# and the gate refused rather than passing on somebody else's number. This is
# the sitting it asked for.
#
# It counted 41,832,275 on binary sha de5bfab22fbd, the same binary the pair
# above was measured on, which makes it a third chip reading the high value
# there. Recorded as a SINGLE because one reading is one reading: a row may not
# claim a mode nobody has seen it take. If it later reads 41,831,767 the gate
# will say so, and that reading is what would make it a pair.
#
# Standing on this binary, then: 0x6/0xcf has read both, 0x19/0x11 has read
# both, 0x19/0x1 and 0x6/0xad have read only 41,832,275. Four chips and one
# value between them except where a chip has been run twice, which is the
# argument that the mode is a property of the run rather than of the silicon —
# and the argument that the per-chip key is separating nothing on this binary.
# Not acted on: the key costs nothing while it is right about something else,
# and one binary is not enough to retire it.

# 2026-09-03, THE THIRD CHIP SHOWS BOTH. CI on kanso#1235 counted 41,831,767 on
# family0x19-model0x1, whose row pinned only 41,832,275, on binary sha
# de5bfab22fbd — the same binary as every reading in this block. Its row becomes
# a pair, and that is the machinery working rather than a value to argue with:
# the gate refused a number the row did not admit, and the number was measured.
#
# The standing on this one binary is now:
#
#   family0x6-model0xcf     both        41,831,767 and 41,832,275
#   family0x19-model0x1     both        41,831,767 and 41,832,275
#   family0x19-model0x11    both        41,831,767 and 41,832,275
#   family0x6-model0xad     one so far  41,832,275
#
# Three chips of four have produced BOTH values, and no chip has produced a
# third. That is the strongest form yet of the argument this file has been
# circling: the two values are a property of the run and not of the silicon, so
# on this binary the per-chip key separates nothing. The fourth chip has one
# reading, which is why the claim is not made as a finding here.
#
# WHAT WOULD SETTLE IT, so the next session does not have to invent an
# experiment: the fourth chip reading 41,831,767. At that point every recorded
# row holds the same pair, the key demonstrably distinguishes nothing on this
# binary, and this file collapses to a single golden holding two values — which
# is what its own header predicted would happen if pinning worked and the chips
# stopped disagreeing. Until then the key costs nothing and is kept, because it
# is still right about the CROSS-BINARY case the header decomposes, where two
# chips read 5,124 apart on one binary.



# 2026-09-03, EVERY ROW CLEARED AGAIN, and this time by an ordinary change to
# the compiler rather than by anything this vein is arguing about. The `!`
# suffix refusal adds a branch to check_merged, so the binary is new — CI's
# sha is 0804abe57190 where every reading in every block above was taken on
# de5bfab22fbd. This file's rule then decides the rest, and it is the rule it
# has now been taught twice: A VALUE MEASURED AGAINST THE OLD BINARY IS WORSE
# THAN NO VALUE. Three rows go, one stays because CI re-sat it.
#
#   18:33  family0x6-model0xcf  0804abe57190  41,829,232
#
# THE FALL IS NOT THE COMPILER GETTING FASTER, and saying so is the whole
# point of writing this down. The row fell 2,535 against the reference and the
# front end got MORE expensive doing it. Measured on the container, the same
# binary pair built with and without the check:
#
#   without the check   42,235,790
#   with the check      42,240,325   +4,535
#
# and the profile diff attributes all of it. Exactly one kanso symbol moves:
#
#   kanso::check::check_merged                          +848
#
# The rest is glibc's startup parse of /proc/self/maps, the term this file
# found on 2026-09-03 and Clay ruled stays counted:
#
#   __vfscanf_internal                                +2,360
#   ____strtoul_l_internal                            +1,158
#   _IO_sputbackc, getdelim, _IO_setb, sscanf, the rest  ~860
#   __memcmp_avx2_movbe                                 -841
#
# So the honest reading of CI's 2,535 fall is +848 of front-end work and about
# 7,070 of maps-parse and allocator movement going the other way on a
# differently-laid-out binary. The container makes the same term go the other
# way on the same source change, which is what a term keyed to the host's
# memory map rather than to the code does.
#
# The row is a SINGLE, and this is the cost of that. Three chips have produced
# both modes on one binary, so an exact pin on one reading is expected to
# refuse about half the time until a second reading makes it a pair. Writing
# 41,829,740 here to pre-empt that would be predicting a number nobody has
# counted, which this file refuses on every page. It goes red, CI counts, and
# the second reading is a measurement.

# 2026-09-03, THE SITTING THE REFUSAL ASKED FOR, and the two chips agree. CI
# landed on Zen 3 next, found no row for it — the block above had just removed
# its stale one — and refused rather than passing on somebody else's number.
# It counted 41,829,232 on sha 0804abe57190: family0x6-model0xcf's value TO
# THE INSTRUCTION, on the same binary.
#
#   21:33  family0x6-model0xcf   0804abe57190  41,829,232
#   21:42  family0x19-model0x1   0804abe57190  41,829,232
#
# That is worth more than a row. On the PREVIOUS binary these two chips had
# each produced both modes, 508 apart, and the file's standing question was
# what still moves the heap layout when everything measurable agrees. Here
# they do not differ at all. It is two readings and so it settles nothing —
# each chip has shown one mode once, which is exactly the state that preceded
# the last disagreement — but it is the first binary on which the vein has
# seen cross-chip agreement rather than the pair, and the next reading on
# either chip is the one that says whether the modes came back.

# 2026-09-04, THE FRONT END MOVED AND TOOK BOTH ROWS WITH IT. This branch
# changes what `kanso check` does: os/read_file yields STR|NONE where it used
# to yield STR, lib/os gained the two arms that turn the none into
# `file_not_found`, and beat.rs widened THREADED to carry the sets that shape
# through a rewind. All three are front-end work, so the Intel and Zen 3 rows
# above were counted on a binary this branch no longer builds and neither can
# be compared to anything now. They are deleted for the reason this file has
# already given twice: a value measured against the old binary is worse than
# no value. The prose above stays, because it is the evidence for what those
# chips read then; only the numbers go. They are re-sittings when they next
# refuse, not new chips.
#
# CI landed on Zen 4 and counted 41,830,604 on sha 3ae016f0. That key is not
# new here — family0x19-model0x11 has had a row on three earlier binaries, and
# it is the chip that produced BOTH modes of the 508-apart pair. What it has
# not got is a reading on the binary immediately before this one, whose rows
# were Intel and Zen 3. So the 1,372 against their 41,829,232 is a chip and a
# binary moved together and says nothing on its own; there is no Zen 4 datum
# to difference this against.
#
# A SINGLE ON THE CHIP MOST LIKELY TO REFUSE IT. Zen 4 is the one key this
# file has watched read two values on one binary, so an exact pin on one
# reading may well go red on the next run here. That is the designed answer
# and not a reason to widen it: the second reading is a measurement, and
# writing a pair now would be predicting a number nobody counted.
# 2026-09-04, AND ZEN 4 SHOWED THE SAME TWO VALUES. Main's first run after the
# merge landed on family0x19-model0x11 and counted 41,831,112 against its
# single of 41,830,604 — the second mode, the same 508, on the same binary.
#
#   family0x19-model0x11   41,830,604   and   41,831,112
#   family0x19-model0x1    41,830,604   and   41,831,112
#
# Four readings, two chips, one binary, and every one of them lands on one of
# TWO values. Neither model has produced a third. That is the strongest form
# the argument has taken: the mode belongs to the run and not to the silicon,
# because the same silicon produces both and different silicon produces the
# same pair. The pair per chip stays the shape rather than becoming one shared
# row, because a merged row would carry one model's readings for the other and
# this file has already been burned by exactly that (the 5,064 split of an
# earlier binary, on these same two models).
#
# What would settle it is a chip that produces a THIRD value, or a run that
# produces the same value twenty times. Neither has happened, and the cap of
# two is what refuses to accommodate the first if it does.
# (the family0x19-model0x11 row stood here with 41830604 41831112. It was
#  measured on d89bda86538a and the front end has moved since; see the
#  2026-09-04 block at the end for why it was removed rather than carried.)

# 2026-09-04, THE RE-SITTING THE DELETION ASKED FOR, and the two AMD models
# agree again. CI landed on Zen 3 next, found no row — the block above had
# just removed its stale one — and refused rather than passing on somebody
# else's number. It counted 41,830,604 on sha d89bda86538a: family0x19-
# model0x11's value TO THE INSTRUCTION, on the same binary.
#
#   00:26  family0x19-model0x11  3ae016f0      41,830,604
#   00:37  family0x19-model0x1   d89bda86538a  41,830,604
#
# That is the second consecutive binary on which these two models agree. The
# one before this read 41,829,232 on both, and the one before THAT read
# 41,503,893 and 41,498,829 — 5,064 apart. So the agreement is a property of
# a binary's layout rather than of the silicon, which is what the header
# already says, and two binaries agreeing does not merge the keys. What it
# does say is that the 5,064 split has not come back on either.
#
# The Intel row (0x6/0xcf) is still absent and still wants its own sitting.

# 2026-09-04, AND THE 508 CAME BACK — same chip, same binary, twenty minutes
# apart. The paragraph above was written after one Zen 3 reading and said the
# two AMD models agree on this binary. Half of that survives: Zen 3 reads BOTH
# modes here, and one of them is Zen 4's value.
#
#   00:37:38  family0x19-model0x1  d89bda86538a  41,830,604
#   00:46:23  family0x19-model0x1  d89bda86538a  41,831,112
#
# 508, which is the residual this file's header records on the Intel from an
# entirely different binary (41,831,767 and 41,832,275). Two binaries and two
# vendors, the same gap: that is the strongest evidence yet that the split is
# one mechanism rather than a coincidence of layouts.
#
# WHERE IT LIVES, off the two profiles. Every kanso frame is identical to the
# instruction — eval_expr'2 1,633,593, check_merged 1,586,580, infer 1,238,613,
# lex_line 866,486, parse 589,004 — and the whole difference is glibc:
# _int_malloc -580, _int_free -19, __memcmp_avx2_movbe +66. The front end does
# the same work and the allocator walks a different heap, which is what the
# header attributes this term to and what the tunables pin could not remove.
#
# So the row takes the pair, which is what a pair is for: two values one chip
# was SEEN to read on one binary, each measured, and a third refused. Zen 4
# stays a single because it has shown one mode here; writing a pair there
# would be predicting a number nobody counted.
#
# 2026-09-04, and EVERY ROW IN THIS FILE WENT STALE AT ONCE. The front end
# gained a per-declaration yield table, which moves the row itself rather than
# the heap under it: Zen 3 on sha 42283602b2c8 counts 41,930,035 against the
# 41,830,604 and 41,831,112 the pair recorded on d89bda86538a. That is
# +99,431, and the container measured the same change at +108,880 on its own
# binary — two hosts, two absolute values, one direction and one order of
# magnitude.
#
# BOTH ROWS WERE STALE AND ONLY ONE WAS RE-SAT, so the other is gone rather
# than carried. Zen 4 has not been counted on this binary at all, and keeping
# its old pair would pin two numbers nobody measured, for a compiler that no
# longer exists — the fault the pair rule was written against, spelled the
# other way round. Removing the row makes the gate refuse on that chip and
# print the sitting, which is what it already does for the Intel key and is
# the designed path for a chip with no reading.
#
# Zen 3 was a SINGLE for one run, and the second mode arrived on the very next
# one. Same chip, same binary sha 42283602b2c8: 41,930,035 then 41,931,559.
# So the row is a pair again, both values measured, and the single stood for
# exactly as long as one reading justified it.
#
# THE GAP IS NOT 508. It is 1,524, which is 3 x 508 exactly. Every reading this
# file records had put the residual at one 508; this is the first that puts it
# at three, and it says the 508 is a QUANTUM rather than a two-valued toggle.
# That reframes what the pair is holding: not "the row has two modes" but "the
# row lands on a lattice of 508s and two of its points have been seen".
#
# The profiles agree with that reading. Across the two runs every kanso frame
# is identical to the instruction — eval_expr'2 1,652,497, check_merged
# 1,586,580, infer 1,251,874, lex_line 866,486, parse 589,004, and the rest —
# and the only visible move is __memcmp_avx2_movbe 1,356,959 to 1,357,025,
# +66. The remaining 1,458 sits below callgrind's 90% cut. The front end does
# identical work and glibc walks a different heap, which is what this file has
# attributed the term to since the header was written.
#
# WHAT THE CAP NOW MEANS HERE. If the residual is a lattice, a third point is
# likely rather than surprising, and the cap of two will refuse it. That
# refusal is the finding to write down and NOT a reason to widen: a row that
# admits every multiple of 508 pins nothing. The question a third reading
# raises is whether this term can be pinned per-chip at all, or whether it
# needs the quantum subtracted out — and that is a question for the log and
# for Clay, not for this file's shape.
#
# 2026-09-04, THE FRONT END MOVED AGAIN AND ZEN 4 IS THE CHIP THAT ANSWERED.
# desc_yield gained five builtin arms, worth +34 retired instructions measured
# on the container against the previous head — desc_yield_of falling 42 as an
# arm collapsed into a matches!, desc_yield::base rising 69 for the two new
# ones. That invalidates every chip at once, as the header says it must.
#
# The run that refused landed on Zen 4 (0x19/0x11), which had no row at all
# since it was removed for having no reading on the previous binary. So this
# is its first sitting on this one, and it is the reference row now.
#
# ZEN 3'S PAIR IS REMOVED RATHER THAN CARRIED. 41,930,035 and 41,931,559 were
# both measured, and both on a binary that is 34 instructions away from this
# one. Two measured numbers for a compiler that no longer exists are still two
# numbers nobody measured for this one — the same reason Zen 4's pair went on
# 2026-09-03, applied to the other chip. It is a re-sitting when it next
# refuses, not a new chip.
#
# NOTE FOR THE NEXT RE-SIT: the 508 lattice is a property of the row, not of
# Zen 3. If Zen 4's single becomes a pair 508 or 1,524 apart, that is the same
# finding one chip over, and the cap of two still refuses a third.
#
# 2026-09-04, SECOND SITTING ON THIS BINARY, AND IT CORRECTS THE ENTRY ABOVE.
# The next run landed on Zen 3 (0x19/0x1), binary sha 0e081d4c2c96, and counted
# 41,845,704 — the SAME value Zen 4 counted. Two chips, one binary, one value,
# so this binary is one where the two AMD models agree to the instruction, as
# the binary before last did and the one between them did not.
#
# WHAT THAT CORRECTS. The note above reads the 41,930,035 -> 41,845,704 move as
# a chip change, because the run that refused was Zen 4 where the previous
# sitting was Zen 3. It is not. Zen 3 counted 41,930,035 on sha 42283602b2c8
# and counts 41,845,704 on sha 0e081d4c2c96: the same chip, -84,331, for a
# source change the container measures at +34. The remaining -84,365 is binary
# layout, which is the term this file's header already names — a docs-only pull
# request once moved this row 5,081 — at sixteen times the size anything here
# had recorded.
#
# So the row moves by layout far more than by the front-end work it is meant to
# count, and it is the layout that the welfare floor then ratchets against.
# That is in design/pending-gavels.md.
family0x19-model0x11 41845704
family0x19-model0x1 41845704
