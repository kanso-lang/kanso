# Retired instructions per benchmark, counted under callgrind. This is the
# work a process does, and it is the dimension every other vein here is blind
# to: allocation counters say how often the allocator was called, the emitted
# golden says how many lines the compiler wrote, the text golden says how big
# the machine code is. None of them counts an instruction.
#
# It exists because between 2026-07-27 and 2026-08-07 the decoder went
# 2,545,249,871 instructions to 2,762,364,162 — eight and a half per cent,
# matching the wall-clock regression almost exactly — with every allocation
# counter byte-identical the whole way. The chart called two of its lines
# "run speed" and "compile speed" and neither counted an instruction, so
# nothing anywhere could see it.
#
# Exact, not a band. Three consecutive runs give the same digits, because
# callgrind counts rather than samples and no other process can reach it. A
# rise is a regression to explain and a fall is a win to bank.
#
# Measured with an empty environment (env -i). The kernel copies the
# environment block onto the new process's stack and libc walks it before
# main, so a run id that gained a digit reads as fourteen instructions of work
# nobody wrote. With it pinned, two runs of unchanged code agree to the digit.
#
# These rows belong to the linux runner's compiler, and they belong to it more
# tightly than any other vein here: the same sources measured under Debian's
# clang read 2,762,364,313 for the decoder against ubuntu's 3,266,971,711,
# eighteen per cent apart, where the machine-code size of the same two builds
# differs by well under one. Instruction count is what an optimiser's inlining
# choices move most. So a toolchain bump will look like an enormous regression
# here and will not be one — regenerate every row in one go and say so in
# design/compiler-log.md, and never read a row against a number measured
# somewhere else.
#
# The line below is that rule, made checkable. The gate reads it, compares it
# against the host it is running on, and refuses to measure anywhere else —
# because the warning above did not stop this vein being regenerated from a
# container one glibc revision off the runner, whose rows sat about four
# hundred instructions low. Two ubuntu 24.04 boxes at 2.39-0ubuntu8.7 and
# 2.39-0ubuntu8.8 differ by more than most changes this vein is here to catch.
# measured-on glibc=2.39-0ubuntu8.9
#
# AND THE COMPILER THAT BUILT THEM. Since 2026-09-06 the emitter probes for
# the preserve_none calling convention and writes a DIFFERENT module when it
# is unavailable, so these rows are a property of the host's clang as well as
# its glibc. A clang-18 host writes the fallback and cannot regenerate them.
# measured-on clang=19.1.1
#
# 2026-08-12 (later): basket 56,597,311 to 56,597,316, the other three unmoved.
# Five instructions, and they are the compare that stops `k_b_str_builder`
# re-seeding a header with room to spare. That guard takes an accumulator handed
# through one intermediate function from 752,908,406 instructions to 16,519,843
# at eight thousand appends. Five here.
#
# 2026-08-12 (later still): basket 56,597,316 back to 56,597,311, the same five.
# The guard is withdrawn — spare capacity is not sole ownership, and a builder
# that trusts it writes into a buffer somebody else holds, which took two
# rewritten panels of a book chapter to 88 GB. The 752,908,406 figure above is
# withdrawn with it rather than disproven: nothing re-measured it against the
# shape that matters.
# 2026-08-13: basket 56,597,311 to 55,987,313, a fall of 609,998 and 1.08%.
#
# 
# 2026-09-04 (a stack slot stands in the entry block): ten rows fall and two
# rise. The emitter wrote each call's argument array in the block that filled
# it; sixty-eight of the decoder's seventy-three slots stood outside their
# function's first block, which LLVM reads as a dynamic stack object — frame
# pointer kept, rsp restored through it on every return. jsonbench
# 2,098,864,471 -> 1,914,624,416, a fall of 8.778%. deepbench (+0.314%) and
# scanbench (+0.133%) rise, and they are the two whose .text grows most: a
# fixed frame gives LLVM a different layout to schedule against and it inlines
# differently in both directions. No allocation counter moves.
#
# 2026-09-05 (three arms at the utf-8 door): five rows fall and none rises.
# The counter and the ascii word-test came out from behind the wide SIMD pass
# into an always_inline front door, and a short run that is not ascii walks the
# grammar rather than paying the pass's seven constant loads. jsonbench
# 1,914,624,416 -> 1,877,751,716, a fall of 1.926%. oneshot -0.816%, widebench
# -0.301%, basket -0.050%, encodebench -0.004%. The other seven are
# byte-identical: they never reach utf-8 validation. No allocation counter
# moves and the emitted line count is unchanged — this is entirely inside the
# runtime.
#
# 2026-09-05 (two doors take registers instead of boxes, and k_str_n inlines):
# nine rows fall, three rise. work_jsonbench 1,877,751,716 -> 1,791,154,445, a
# fall of 4.612%: the find2 and slice prelude twins test the argument tags in
# the emitted module and hand a raw door scalars, so the boxed guards and the
# stack-passed fourth KValue go. work_indexbench 5,242,116 -> 4,792,124
# (-8.584%) and work_scanbench 1,425,334,005 -> 1,406,298,324 (-1.335%) are
# mostly k_str_n's always_inline; scanbench's NET fall is what matters, since
# the slice twin cost it 2,495,505 on its own and k_str_n returned more than
# that. work_oneshot 29,275,729 (-1.968%), work_widebench 57,839,394 (-0.741%),
# work_pendbench 700,501,795 (-0.230%), work_basket 39,875,796 (-0.096%),
# work_encodebench 5,802,859,663 (-0.085%), work_digestbench 80,995,637 (-781).
#
# THREE RISE. work_deepbench 678,049,713 -> 678,049,725 and work_escapebench
# 130,170,743 -> 130,170,757 are twelve and fourteen instructions, which is the
# twins' tag tests on the few calls those programs make. work_readbench
# 2,000,657,813 -> 2,038,393,968, a rise of 1.886%, is k_str_n and is real: that
# benchmark reads a file and holds few short strings and many long ones, and an
# inlined copy loop loses to a called one when the copy dominates. The objective
# takes the trade -- see the entry of the same date, and bench/text_golden.txt
# for the 34,320 bytes of machine code it also costs.
#
# 2026-09-05 (third): the utf8-over-slice twin. Three rows fall and nine hold
# EXACTLY still, which is what the local A/B predicted: eight of the twelve
# benchmark binaries came out byte-identical to the ones before the change, so
# those rows could not move, and widebench's binary differed while its count
# did not. work_jsonbench lands on 1,783,183,295, a fall of 7,971,150 and
# 0.4450%. work_oneshot lands on 29,222,587, a fall of 53,142 and 0.1815%.
# work_encodebench lands on 5,802,806,121, a fall of 53,542. The container
# measured the same three deltas TO THE INSTRUCTION on its own pair of builds,
# which is the strongest agreement this vein has recorded between the two
# hosts.
#
# 2026-09-05 (fourth): the find2_below twin. TWO rows fall and TEN hold exactly
# still. work_encodebench lands on 5,563,212,521, a fall of 239,593,600 and
# 4.1289%. work_oneshot lands on 28,623,603, a fall of 598,984 and 2.0498%.
# Nothing rises. The container's local A/B measured both deltas TO THE
# INSTRUCTION -- 239,593,600 and 598,984 on its own pair of builds -- which is
# the second consecutive change on which the two hosts have agreed exactly.
# Nine of the twelve benchmark binaries came out byte-identical to the ones
# before the change, so those rows could not move; widebench's binary differed
# and its count did not.
#
# 2026-09-05 (fifth): the capture-free closure literal. FOUR rows fall and
# eight rise, and the eight are two different things. work_encodebench lands
# on 5,527,056,676 (-36,155,845, -0.6499%), work_digestbench on 80,559,329
# (-436,308, -0.5387%), work_oneshot on 28,533,505 (-90,098, -0.3148%) and
# work_scanbench on 1,406,247,707 (-50,617). Ten of the twelve deltas are the
# container's TO THE INSTRUCTION, which is the third consecutive change on
# which the two hosts have agreed exactly.
#
# work_deepbench lands on 716,506,831, a rise of 38,457,106 and 5.6724%, and
# it is code layout rather than work: the carry walk visits the same nodes to
# within 0.2% -- k_copy_size is entered 1,534,544 times before and 1,535,314
# after -- and each visit costs twelve instructions more. The other seven
# rises are small and the same shape: work_pendbench 700,529,602 (+27,807),
# work_basket 39,878,996 (+3,200), work_readbench 2,038,397,546 (+3,578),
# work_jsonbench 1,783,183,673 (+378), work_widebench 57,839,754 (+360).
# work_escapebench 130,170,757 and work_indexbench 4,792,124 do not move at
# all -- neither program has a capture-free lambda to convert.
#
# The two rows the hosts disagree on are exactly the two whose rise is
# layout: deepbench by 4,325 and pendbench by 27. A layout effect is a
# property of the binary the host built, so that is the expected shape.
# 2026-09-05 (kanso#1251): a comparison feeding an `if` branches on its own i1
# instead of building a tagged KValue for a phi to merge and k_not_failure and
# k_truthy to take apart. EVERY ROW FALLS AND NONE RISES, both sides measured
# by CI:
#
#   digestbench  80,559,329 ->    77,290,591  -3,268,738  -4.0576%
#   encodebench  5,527,056,676 -> 5,322,691,304  -204,365,372  -3.6975%
#   oneshot      28,533,505 ->    27,733,120    -800,385  -2.8051%
#   pendbench    700,529,602 ->  681,319,796 -19,209,806  -2.7422%
#   jsonbench    1,783,183,673 -> 1,745,058,173 -38,125,500  -2.1381%
#   indexbench   4,792,124 ->      4,692,123    -100,001  -2.0868%
#   widebench    57,839,754 ->    57,201,224    -638,530  -1.1040%
#   scanbench    1,406,247,707 -> 1,395,728,077 -10,519,630  -0.7481%
#   basket       39,878,996 ->    39,737,538    -141,458  -0.3547%
#   deepbench    716,506,831 ->  714,674,831  -1,832,000  -0.2557%
#
# escapebench 130,170,757 and readbench 2,038,397,546 do not move by a single
# instruction. Neither has a comparison in a hot loop -- one tagged comparison
# in escapebench's whole emitted IR, two in readbench's -- so a change that
# only touches comparisons reaches neither, and that they are EXACTLY still is
# the check that the other ten falls are the change rather than the weather.
#
# 2026-09-05 (the digit pair table): FOUR rows fall and NINE hold exactly
# still. ryu_d2d wrote its shortest digits one at a time -- a 64-bit division
# per digit into a scratch buffer, then a reversal -- and now takes the length
# from a ladder and writes pairs out of a table straight into place.
# encodebench 5,218,724,822 -> 5,150,924,022 (-67,800,800, -1.2992%) and
# livebench falls by the SAME 67,800,800 (-1.3017%), which is the two
# benchmarks running the same program on the same input. oneshot -169,502
# (-0.6170%), widebench -45,263 (-0.0793%). The nine that hold render no
# floats, and that they are EXACTLY still is the check that the four falls are
# the change rather than the weather. No allocation counter moves, no emitted
# line moves, and every .text row rises by exactly 112 bytes.
#
# 2026-09-05 (the literal's fast door): TWELVE rows fall and one rises by two
# instructions. `k_str_lit` was a real extern call at every string-literal
# evaluation -- 2,500,000 of them on encodebench alone -- to load a slot, test
# whether it had been built, and hand it back. It gets the door treatment the
# emitter already gives find2, slice and length: an alwaysinline shim that
# reads the slot's tag and returns it, calling the runtime only the first time.
# widebench -1.8506%, livebench -1.0910%, encodebench -1.0877%, deepbench
# -1.0202%, oneshot -0.7312%, basket -0.6560%, jsonbench -0.5130%; pendbench,
# scanbench, indexbench, digestbench and readbench move under a hundredth of a
# per cent. escapebench rises by 2. No allocation counter moves. Every .text
# row grows, by 64 bytes on escapebench to 2,304 on scanbench, in proportion to
# how many literal sites each program has.
#
# 2026-09-05 (the entries pair goes on the stack): THREE rows fall and TEN hold
# exactly still. k_b_entries built each map entry's two fields in an arena
# block and handed that block to k_rec, which copies its arguments into the
# storage after the record header and keeps no reference to them -- so the
# block was written once, read once and dropped. It is a stack array now.
#
#   encodebench  4,921,267,712 -> 4,893,408,112  -27,859,600  -0.5661%
#   livebench    4,911,031,714 -> 4,883,172,114  -27,859,600  -0.5673%
#   oneshot         26,642,853 ->    26,573,204      -69,649  -0.2614%
#
# encodebench and livebench fall by the SAME 27,859,600, which is the two
# benchmarks running one program over one input against two copies of the
# library. The ten that hold render no map through `entries` -- jsonbench only
# decodes -- and that they are EXACTLY still is the check that the three falls
# are the change rather than the weather.
#
# The rows above are the container's deltas applied to CI's landed values: it
# measured 4,921,267,372 -> 4,893,407,772 and 26,642,513 -> 26,572,864 on its
# own pair of builds, the fixed 340 being the exec path this vein's header
# warns about. CI's own sitting is the record and corrects these if they
# differ.
#
# 2026-09-05 (one block for a map's records): THREE rows fall and TEN hold
# exactly still, the same three and the same ten as the entry above.
# k_b_entries carved each entry's record out of its own arena bump; the records
# were already landing next to each other, since k_alloc only moves a pointer,
# so it takes one block for all n and carves it. 3,344,400 records over
# 1,104,400 calls means two bumps in every three were bookkeeping for a block
# the one before them had already reserved.
#
#   encodebench  4,893,408,112 -> 4,739,440,912  -153,967,200  -3.1463%
#   livebench    4,883,172,114 -> 4,729,204,914  -153,967,200  -3.1530%
#   oneshot         26,573,204 ->    26,188,286      -384,918  -1.4485%
#
# encodebench and livebench fall by the SAME 153,967,200 again -- one program,
# one input, two copies of the library -- and 68.7 instructions a bump saved.
# The rows are the container's own A/B deltas applied to CI's landed values: it
# measured 4,893,407,772 -> 4,739,440,572 and 26,572,864 -> 26,187,946 on its
# own pair of builds. The previous change's three predictions landed to the
# instruction, which is why this one is written the same way; CI's sitting is
# still the record.
#
# 2026-09-06 (split scans with memchr): FOUR rows fall and NINE hold exactly
# still. Nothing rises. k_b_split called memcmp at every byte position, twice
# over the string -- once to count the pieces and once to cut them -- so a
# 188,698-byte document split on a "\n" it does not contain cost 75,479,200
# calls at ten instructions each. A match can only start where the separator's
# first byte is, so memchr covers the ground and memcmp runs only at the
# candidates it hands back.
#
#   readbench    2,038,390,798 ->    45,883,331  -1,992,507,467  -97.7490%
#   scanbench    1,384,644,173 ->   774,357,155    -610,287,018  -44.0754%
#   basket          38,029,049 ->    35,508,390      -2,520,659   -6.6282%
#   pendbench      605,691,420 ->   605,536,009        -155,411   -0.0257%
#
# The nine that hold call split nowhere, and that they are EXACTLY still is
# the check that the four falls are the change rather than the weather. The
# same four are the only .text rows that move, each by the 384 bytes of the
# new helper, because the other nine binaries never link it.
#
# The rows are the container's own A/B deltas applied to CI's landed values.
# Nine of the thirteen deltas are zero, so there is little in this projection
# to go wrong; CI's sitting is still the record.
#
# 2026-09-06 (the array's second walk over the same whitespace): jsonbench
# -2.2503%, oneshot -1.0223%, livebench -0.0059%, and the other ten hold
# exactly. `array_items` opened with `skip_ws` and then called `parse_value`,
# which opens with its own; the second walk always started on the byte the
# first had stopped on. The line is gone. Rows are the container's A/B deltas
# applied to CI's landed values; ten of the thirteen deltas are zero.
#
# 2026-09-06 (the `and` under an `if` is asked in pieces): four rows move and
# nine hold. jsonbench 1,570,704,224 -> 1,564,492,424 (-6,211,800, -0.3955%),
# oneshot 24,383,381 -> 24,341,969 (-41,412, -0.1698%), livebench
# 4,436,977,137 -> 4,436,935,725 (-41,412), and scanbench 776,362,839 ->
# 776,364,842, a RISE of 2,003 or 0.00026% -- the one row that pays, where a
# condition whose arms are not constants gains two blocks and a branch that
# LLVM does not always fold back.
#
# These four are PROJECTIONS: the row is CI's and this container reads a
# different glibc, so each is the golden plus the container's own A/B delta,
# measured on one host from the repo root with both binaries in place. CI's
# sitting is the record and one red round is expected if a projection misses.
#
# 2026-09-06 (the counter switch is set once): ALL THIRTEEN rows fall, the
# first clean sweep this vein has recorded. `k_stats_on` initialised itself
# lazily inside `k_alloc`, which is inlined into every hot caller, so every
# loop that allocated carried a WRITE to that global -- and a loop that writes
# a global cannot have any read of it hoisted out. Every counter check in
# every hot loop reloaded and re-tested it once an iteration, on release runs
# where the answer never changes. A constructor sets it before main instead.
# Falls run from scanbench at 0.9646 per cent to pendbench at 0.0018.
#
# These are PROJECTIONS: the rows are CI's and this container reads a
# different glibc, so each is the golden plus the container's own A/B delta,
# measured on one host from the repo root with both binaries in place. CI's
# sitting is the record.
#
# 2026-09-06 (an append of a slice reads the range in place): three rows fall
# and ten hold. work_jsonbench 1,559,466,178 -> 1,542,925,378, a fall of
# 16,540,800 or 1.0607%; work_oneshot 24,300,109 -> 24,190,898, a fall of
# 109,211 or 0.4494%; work_livebench 4,432,486,910 -> 4,432,419,027, a fall of
# 67,883 or 0.0015%. The escape walk between two escapes is one find2 and one
# copy where it was a byte at a time, and the view the slice would have been
# is never built.
#
# 2026-09-06 (a capture-free lambda is a link-time constant): eleven of the
# thirteen fall, and the two that do not are the interesting part.
#
#   encodebench 4,421,003,600 -> 4,390,892,021   -0.6811%
#   livebench   4,432,419,027 -> 4,400,131,256   -0.7285%
#   oneshot        24,190,898 ->     24,109,317  -0.3373%
#   deepbench     705,892,821 ->    704,511,486  -0.1957%
#   basket         35,477,286 ->     35,473,609  -0.0104%
#   scanbench     768,876,199 ->    768,849,900  -0.0034%
#   pendbench     605,526,497 ->    605,519,153  -7,344
#   jsonbench   1,542,925,378 ->  1,542,924,950  -428
#   readbench       4,283,685 ->      4,283,257  -428
#   widebench      54,610,292 ->     54,609,879  -413
#   escapebench   114,584,648 ->    114,584,676  +28
#   indexbench      4,691,237 ->      4,691,265  +28
#   digestbench    76,854,629 ->     77,175,692  +321,063  +0.4177%
#
# The two encode rows carry the change: `k_call2_fast`'s tag and arity tests
# fold against a constant closure and the indirect call becomes a direct one,
# which the escape fold pays 11,658,800 times a run. The +28 on escapebench and
# indexbench is one closure built once instead of a first-visit branch taken
# once, and digestbench's +321,063 goes with a 192-byte rise in its .text: the
# direct call changes what LLVM inlines there. Welfare weighs all thirteen and
# rose, so the trade is taken; the rise is named rather than defended.
#
# 2026-09-06 (a `length` the sets prove is a header load): six rows FALL and
# none rises. encodebench 4,390,892,021 -> 4,389,082,013 (-0.0412%), livebench
# 4,400,131,256 -> 4,399,422,049 (-0.0161%), oneshot 24,109,317 -> 24,107,540
# (-0.0074%), jsonbench 1,542,924,950 -> 1,542,924,650, widebench 54,609,879 ->
# 54,609,871, digestbench 77,175,692 -> 77,175,689. basket, deepbench,
# escapebench, pendbench, indexbench, scanbench and readbench hold to the
# instruction: they have no `length` site the sets prove is bytes.
# 2026-09-06 (the tags the sets already proved, and seven copies of one body):
# encodebench 4,328,660,413 -> 4,317,272,013, a FALL of 0.2631%; livebench
# 4,339,000,449 -> 4,318,225,649, a FALL of 0.4788%; oneshot 23,956,486 ->
# 23,904,549, a FALL of 0.2168%. The emitter has a k_b_append_mut_int twin
# now, so an in-place append of a byte the sets already prove is an int skips
# the runtime tag check; and lib/json/text.kso's seven escape arms share one
# esc_pair body. The ten other rows hold to the digit -- these are the encode
# paths. CI's rows on 31537922.
# 2026-09-06 (a switch arm was never told what it had been handed): eight rows
# fall and five hold. jsonbench 1,542,924,650 -> 1,526,907,200, a FALL of
# 1.0381%; encodebench 4,317,272,013 -> 4,310,952,916, 0.1464%; oneshot
# 23,904,549 -> 23,797,766, 0.4467%; basket 35,403,587 -> 35,365,571, 0.1074%;
# digestbench 75,582,475 -> 75,565,053, 0.0231%; livebench 4,318,225,649 ->
# 4,318,118,863, 0.0025%; pendbench 605,515,753 -> 605,515,353, 400
# instructions; scanbench 768,849,900 -> 768,849,780, 120. widebench,
# deepbench, escapebench, indexbench and readbench are unchanged to the
# instruction. The switch dispatcher records what inference proved about each
# slot now, as the ladder always has, so an index into a proven-bytes slot
# skips its tag test. Every delta above was reproduced on a container whose
# glibc differs from this golden's -- the absolute rows could not be taken
# there, and the deltas agreed to the instruction on all thirteen.
# 2026-09-06 (the closure calling convention, on clang 19): ALL FOURTEEN rows
# move and they split both ways. This is two changes at once and they cannot be
# separated on CI, because the emitter's probe only answers true when clang 19
# is the compiler: the toolchain move and the convention arrive together.
#
#   digestbench   75,565,053 ->    70,784,439  -6.3265%
#   encodebench 4,310,952,916 -> 4,194,027,086  -2.7123%
#   livebench   4,318,118,863 -> 4,213,463,624  -2.4236%
#   widebench      54,609,871 ->    53,465,568  -2.0954%
#   runbench    3,043,743,748 -> 3,012,388,655  -1.0301%
#   scanbench     768,849,780 ->   766,291,267  -0.3328%
#   deepbench     702,627,486 ->   700,416,944  -0.3146%
#   oneshot        23,797,766 ->    23,738,890  -0.2474%
#   readbench       4,283,257 ->     4,283,427  +0.0040%
#   escapebench   114,584,676 ->   114,626,851  +0.0368%
#   indexbench      4,691,265 ->     4,771,217  +1.7043%
#   basket         35,365,571 ->    36,001,898  +1.7993%
#   jsonbench   1,526,907,200 -> 1,558,677,818  +2.0807%
#   pendbench     605,515,353 ->   620,703,023  +2.5082%
#
# THE DECODE PAYS FOR THE ENCODE. jsonbench and pendbench rise about 2.1% and
# 2.5%, and that is named rather than defended: a caller that clobbers every
# register spills what it wanted to keep, and the decode's hot loops hold more
# live state across a closure call than the encode's do. Welfare weighs all
# fourteen through runbench and rose 51.89 -> 51.95, so the objective takes the
# trade; the rows that pay are here so the next reader can see the price.
#
# The container measured the same direction on its own pair of builds, and its
# absolute rows cannot be taken here: it is clang 18.1.3 and would write the
# fallback module. CI's sitting is the record.
# 2026-09-10 (kanso#1363): `done` is minted and std/list's one-field `done`
# record is deleted. Ten rows FALL and four are byte-identical:
#
#   encodebench  3,963,988,518 ->  3,963,988,451      -67  (-0.0000%)
#   basket          34,694,178 ->     34,690,245   -3,933  (-0.0113%)
#   widebench       35,268,228 ->     35,268,161      -67  (-0.0000%)
#   deepbench      387,470,247 ->    387,470,234      -13  (-0.0000%)
#   pendbench      590,970,940 ->    583,758,224 -7,212,716 (-1.2205%)
#   scanbench      726,019,157 ->    726,019,079      -78  (-0.0000%)
#   digestbench     10,426,541 ->     10,426,515      -26  (-0.0002%)
#   runbench     2,369,679,773 ->  2,367,877,484 -1,802,289 (-0.0761%)
#
# jsonbench, oneshot, escapebench, indexbench, readbench and livebench do not
# move at all. The mechanism is one allocation per drained source: the record
# `next` used to answer is a word now, so a program pays the fall in proportion
# to how many sources it drains. pendbench drains one per pending cell, which
# is why it takes 1.22% where the rest take a handful of instructions.
# 2026-09-10 (a constructor no longer builds into a record it is about to
# hold): CI's sitting, four rows fall and ten are byte-identical. basket
# 34,690,245 -> 34,690,216 (-29), pendbench 583,758,224 -> 583,755,724
# (-2,500), scanbench 726,019,079 -> 726,018,879 (-200), runbench
# 2,367,877,484 -> 2,367,876,664 (-820). lib/json's `fail` emits k_rec
# instead of the wider constructor, so the four programs that reach that
# shape at run time do slightly less work; the other ten never call it.
# 2026-09-11 (a validated string knows its length): six of the fourteen fall
# and eight rise. The falls are the whole-string doors that now take the count
# the validator already had: encodebench -31,336,948 (-0.7905%), livebench
# -31,380,296 (-0.9012%), runbench -9,818,606 (-0.4340%), pendbench -4,961,799
# (-2.1870%), jsonbench -2,172,701 (-0.1477%), oneshot -24,482 (-0.1131%).
# The rises are the accumulator the validator carries for strings whose count
# nobody asks for: readbench +69,036 (+1.5133%) is the largest and is
# k_utf8_bad_wide alone, 274,748 -> 343,792 under callgrind_annotate, the whole
# delta to within eight instructions. deepbench +384,001, widebench +47,946,
# basket +25,949, and four rows move by less than a hundred: indexbench +63,
# digestbench +34, scanbench +28, escapebench +1. Measured on CI.
# 2026-09-11 (a none that reaches a group with no arm for it is refused): two
# rows move and the other twelve are byte-identical. encodebench 3,932,651,503
# -> 3,958,779,263 (+26,127,760 / +0.6644%) and widebench 35,316,107 ->
# 35,202,913 (-113,194 / -0.3205%). Both are the vendored benchmark sources,
# not the runtime: the rule refuses their lenient indexes into a group with no
# `none` arm, so each gained a strict `!` and an arm naming the end of input.
# The strict index raises where the lenient one returned a none, and the two
# benchmarks pay and collect differently for it. runbench, the objective's
# run term, does not move.
#
# 2026-09-13 (the runtime's twenty-seven counter sites leave a shipped
# binary): ALL FOURTEEN ROWS FALL. runbench 2,141,642,566 -> 2,128,867,999, a
# fall of 12,774,567 (-0.5965%); deepbench -1.4709% and scanbench -1.1258% are
# the largest, indexbench -96 the smallest. Each of the twenty-seven sites is
# one `K_COUNTING &&` in front of the `k_stats_on` test, a constant 0 in a
# shipped build, so the whole condition folds away. This is the other half of
# the split kanso#1393 began; that PR took the emitter's eight.
#
# The container projected -13,187,834 on runbench and CI reads 0.9687 of it.
# That is the ratio to expect from this box, and it is worth writing down: the
# delta is a fixed number of tests per run rather than anything that scales, so
# the two hosts differ only in what a test costs them.
#
# 2026-09-14 (kanso#1417): the string arm is asked first in k_b_at. Two rows
# move and twelve are byte-identical. runbench 1,997,551,401 -> 1,994,791,401,
# a FALL of 2,760,000 (-0.1382%); indexbench 3,265,296 -> 3,185,298, a FALL of
# 79,998 (-2.4500%). Those are CI's rows on the base kanso#1419 left, and the
# same two deltas to the instruction were read on the kanso#1415 base a day
# earlier: the saving is a fixed cost per call and the call count did not move. The dispatch tested K_LIST before K_STR, so every string
# index paid a compare and a branch for a question the arriving tag had
# already settled, and the run program's tally_4 loop asks it 690,000 times.
# A list index now pays those two instructions instead, which is why the row
# to read is the whole vein rather than indexbench alone.
#
# THE CONTAINER PROJECTED HALF OF BOTH, and that is the finding to carry.
# Its A/B read -1,380,000 on runbench against CI's -2,760,000, exactly twice,
# and -39,998 on indexbench against CI's -79,998, twice and two more. The A/B
# was run on a host this vein's gate had already refused (glibc 2.39-0ubuntu8.7
# and clang 18.1.3 against the 8.9 and 19.1.1 above). The header says a refused
# host cannot be compared for a LEVEL; this is the first time one has been
# shown to halve a DELTA. Inlining decides how many call sites reach the
# reordered test, and the two toolchains reach it a different number of times.
# From a refused host, project the sign and not the size.
#
# 2026-09-14 (kanso#1419): ryu_d2d's general branch removes digits two at a
# time instead of one. The `if (vp/100 > vm/100)` block became a `for (;;)`
# over the same body, so a float with four removable digits makes two trips
# where it made four.
#
# Five rows fall and nine hold to the digit, on the base kanso#1423 left:
#
#   encodebench  3,641,023,556 -> 3,616,600,356   -24,423,200   -0.6708%
#   livebench    3,115,992,776 -> 3,091,569,576   -24,423,200   -0.7838%
#   runbench     2,003,046,621 -> 1,997,551,401    -5,495,220   -0.2743%
#   oneshot         19,292,922 ->     19,231,864       -61,058   -0.3165%
#   widebench       33,142,691 ->     33,134,691        -8,000   -0.0241%
#
# The same five deltas, to the instruction, were read on the kanso#1418 base
# a day earlier; kanso#1423 moved the levels by +250 to +64,000 and the pair
# loop's saving did not move with them.
#
# encodebench and livebench fall by the SAME 24,423,200. They are two programs
# over one encode path and the floats in them are the same floats; the equality
# is the measurement agreeing with itself, not a coincidence to explain.
#
# The container read -0.2660% on runbench and CI reads -0.2743%, a ratio of
# 1.031. Round one of this branch profiled a binary older than src/runtime.c
# and read the OLD shape at 1,994,173,231; the number above is the rebuilt one.
#
# The margin is NARROWER than the pull request first claimed. That body said
# both loops cost the same sixteen instructions, so a trip taking two digits
# was worth two taking one. Sixteen was the count of the code being REPLACED.
# The fused loop disassembles to twenty-one at 0x39190-0x391d2, seven of them
# register moves carrying vp/vm/vr around the back edge. The trade is 21
# against 32, not 16 against 32, and the rows above are what it is worth.
#
# 2026-09-15 (kanso#1427): a float's leading zeros are skipped once, and the
# digit loops stop asking whether the first nonzero has landed. Six rows fall
# and eight hold to the digit, on the base kanso#1417 left:
#
#   jsonbench    1,250,475,761 -> 1,247,197,361    -3,278,400   -0.2622%
#   runbench     1,994,791,401 -> 1,992,627,657    -2,163,744   -0.1085%
#   widebench       33,134,691 ->     33,028,905      -105,786   -0.3193%
#   encodebench  3,616,600,356 -> 3,616,578,500       -21,856   -0.0006%
#   livebench    3,091,569,576 -> 3,091,547,720       -21,856   -0.0007%
#   oneshot         19,231,864 ->     19,210,008       -21,856   -0.1136%
#
# jsonbench and runbench are the container's two deltas TO THE INSTRUCTION:
# the saving is a fixed count per digit and the digits are the same digits
# on both hosts. encodebench, livebench and oneshot fall by ONE decode of the
# large document each, 21,856, which is jsonbench's fall divided by its 150
# decodes; widebench holds its own floats. The eight that hold never parse a
# float.
#
# 2026-09-15 (kanso#1428): the int parser's cold tail leaves the frame it was
# sizing. Five rows fall and nine hold to the digit, on the base kanso#1427
# left:
#
#   jsonbench    1,247,197,361 -> 1,243,742,261    -3,455,100   -0.2770%
#   runbench     1,992,627,657 -> 1,990,347,291    -2,280,366   -0.1144%
#   encodebench  3,616,578,500 -> 3,616,555,466       -23,034   -0.0006%
#   livebench    3,091,547,720 -> 3,091,524,686       -23,034   -0.0007%
#   oneshot         19,210,008 ->     19,186,974       -23,034   -0.1199%
#
# ELEVEN a call on the runner where the container read TWELVE: 2,280,366
# over 207,306 calls and 3,455,100 over 314,100, both to the instruction, so
# the runner's clang keeps one instruction of frame the container's
# discards. The three one-decode rows fall by 23,034, jsonbench's fall over
# its 150 decodes. The nine that hold never parse an integer.
#
# 2026-09-15 (kanso#1429): the cold helpers stop clobbering their callers'
# registers. Ten rows fall and four rise, on the base kanso#1428 left:
#
#   jsonbench    1,243,742,261 -> 1,229,738,040   -14,004,221   -1.1260%
#   runbench     1,990,347,291 -> 1,977,087,740   -13,259,551   -0.6662%
#   deepbench      371,382,179 ->   369,202,224    -2,179,955   -0.5870%
#   widebench       33,028,905 ->    32,837,013      -191,892   -0.5810%
#   encodebench  3,616,555,466 -> 3,616,439,594      -115,872   -0.0032%
#   indexbench       3,185,298 ->     3,085,763       -99,535   -3.1248%
#   livebench    3,091,524,686 -> 3,091,427,397       -97,289   -0.0031%
#   oneshot         19,186,974 ->    19,096,479       -90,495   -0.4716%
#   digestbench      9,837,152 ->     9,813,332       -23,820   -0.2421%
#   pendbench      219,476,888 ->   219,469,722        -7,166   -0.0033%
#   basket          34,281,871 ->    34,433,106      +151,235   +0.4412%
#   escapebench     82,969,017 ->    82,999,058       +30,041   +0.0362%
#   scanbench      528,870,249 ->   528,872,901        +2,652   +0.0005%
#   readbench        4,629,745 ->     4,629,808           +63   +0.0014%
#
# The container read -14,536,121 and -13,795,119 on the two rows the A/B
# watched; the runner's clang keeps a little more frame than the
# container's, as it did on kanso#1428. The four rises are the outlining's
# own price: an index whose container is a map reaches k_b_at_rest by a call
# now, the ascii fill is a call, and the refill saves every register it
# touches on each of its calls, so a program that takes those paths and holds
# little live across them pays and collects nothing. Which of the three
# basket pays was not attributed.
#
# 2026-09-15 (kanso#1430): a long copy is a cold call, and the slice door's
# rare arms are too. Nine rows fall and five rise, on the base kanso#1429
# left:
#
#   jsonbench    1,229,738,040 -> 1,218,898,014  -10,840,026   -0.8815%
#   runbench     1,977,087,740 -> 1,970,132,223   -6,955,517   -0.3518%
#   deepbench      369,202,224 ->   368,942,227     -259,997   -0.0704%
#   oneshot         19,096,479 ->    19,024,257      -72,222   -0.3782%
#   encodebench  3,616,439,594 -> 3,616,369,380      -70,214   -0.0019%
#   livebench    3,091,427,397 -> 3,091,363,554      -63,843   -0.0021%
#   escapebench     82,999,058 ->    82,993,058       -6,000   -0.0072%
#   scanbench      528,872,901 ->   528,868,084       -4,817   -0.0009%
#   pendbench      219,469,722 ->   219,466,315       -3,407   -0.0016%
#   readbench        4,629,808 ->     4,629,832          +24   +0.0005%
#   indexbench       3,085,763 ->     3,086,146         +383   +0.0124%
#   widebench       32,837,013 ->    32,845,030       +8,017   +0.0244%
#   basket          34,433,106 ->    34,534,020     +100,914   +0.2931%
#   digestbench      9,813,332 ->     9,944,479     +131,147   +1.3364%
#
# jsonbench's fall is the container's A/B to the instruction; runbench's is
# 200,123 deeper than the container's -6,755,394. The five rises are the
# wrapper's own price: a copy of sixteen bytes or more goes through
# k_copy_cold now, which saves every register it touches before memcpy, so
# a program whose copies are long and frequent pays that on each one and
# keeps no frame it did not already keep. The digest builds such copies.
#
# 2026-09-15 (kanso#1431): the third cold-frame sweep, nine more rare arms.
# Thirteen rows fall and one rises, on the base kanso#1430 left:
#
#   indexbench       3,086,146 ->      2,895,940      -190,206   -6.1632%
#   scanbench      528,868,084 ->    508,340,742   -20,527,342   -3.8814%
#   deepbench      368,942,227 ->    354,898,747   -14,043,480   -3.8064%
#   basket          34,534,020 ->     33,956,235      -577,785   -1.6731%
#   livebench    3,091,363,554 ->  3,050,693,016   -40,670,538   -1.3156%
#   runbench     1,970,132,223 ->  1,945,875,866   -24,256,357   -1.2312%
#   encodebench  3,616,369,380 ->  3,575,756,608   -40,612,772   -1.1230%
#   jsonbench    1,218,898,014 ->  1,211,426,976    -7,471,038   -0.6129%
#   oneshot         19,024,257 ->     18,968,024       -56,233   -0.2956%
#   widebench       32,845,030 ->     32,795,381       -49,649   -0.1512%
#   digestbench      9,944,479 ->      9,941,342        -3,137   -0.0315%
#   pendbench      219,466,315 ->    219,435,891       -30,424   -0.0139%
#   readbench        4,629,832 ->      4,629,709          -123   -0.0027%
#   escapebench     82,993,058 ->     83,901,717      +908,659   +1.0949%
#
# runbench's fall is 132,878 deeper than the container's -24,123,479 and
# jsonbench's 171,000 shallower than its -7,642,038. The one rise is the
# list grow's price: its permanent buffer, its registration and its release
# are preserve_most helpers now, and escapebench grows a list 12,000
# times a run (9,000 of them releasing a predecessor), so the three helpers
# and the long copy cost it 1,752,029 against 846,030 the push itself no
# longer pays (container A/B, 905,659 summed against CI's 908,659).
#
# 2026-09-15 (kanso#1432): an empty list literal opens with room for four.
# Nine rows fall, two hold and three rise, on the base kanso#1431 left:
#
#   pendbench      219,435,891 ->    208,151,563   -11,284,328   -5.1424%
#   jsonbench    1,211,426,976 ->  1,195,571,676   -15,855,300   -1.3088%
#   digestbench      9,941,342 ->      9,838,994      -102,348   -1.0295%
#   basket          33,956,235 ->     33,685,770      -270,465   -0.7965%
#   runbench     1,945,875,866 ->  1,930,572,613   -15,303,253   -0.7864%
#   oneshot         18,968,024 ->     18,862,981      -105,043   -0.5538%
#   widebench       32,795,381 ->     32,712,263       -83,118   -0.2534%
#   encodebench  3,575,756,608 ->  3,575,628,491      -128,117   -0.0036%
#   livebench    3,050,693,016 ->  3,050,596,946       -96,070   -0.0031%
#   indexbench       2,895,940 ->      2,895,940            +0
#   readbench        4,629,709 ->      4,629,709            +0
#   scanbench      508,340,742 ->    508,351,545       +10,803   +0.0021%
#   deepbench      354,898,747 ->    355,470,728      +571,981   +0.1612%
#   escapebench     83,901,717 ->     85,995,606    +2,093,889   +2.4956%
#
# runbench's fall is 342,891 deeper than the container's -14,960,362 and
# jsonbench's 630,000 deeper than its -15,225,300. escapebench's rise is the
# container's to the instruction (83,868,738 -> 85,962,627, +2,093,889) and
# it is the accumulator's new lifetime: with room for four, a list a loop
# pushes into stays in the arena through four pushes instead of one, and
# every rewind in between carries it, where before the second push moved it
# to permanent storage. memcpy +738,000, malloc +626,895, the free path
# +441,000, k_beat_rewind_slow +183,000, k_b_push_mut +162,000.
#
# 2026-09-15 (kanso#1433): the two byte scanners are inlined, and their constants
# fold. Six rows FALL and none rises, on the base kanso#1432 left: work_runbench
# 1,930,572,613 -> 1,904,577,350 (-25,995,263, -1.3465%), work_livebench
# 3,050,596,946 -> 3,018,009,215 (-32,587,731, -1.0682%), work_encodebench
# 3,575,628,491 -> 3,567,580,816 (-8,047,675, -0.2251%), work_jsonbench
# 1,195,571,676 -> 1,187,809,626 (-7,762,050, -0.6492%), work_scanbench
# 508,351,545 -> 500,829,907 (-7,521,638, -1.4796%), work_oneshot 18,862,981
# -> 18,729,910 (-133,071, -0.7055%). The other eight hold to the instruction:
# they never call either scanner. The container had read runbench -24,948,866
# and jsonbench -6,179,850; the runner's fall is deeper on both. Against main,
# work_escapebench 85,995,606 and work_digestbench 9,838,994 still stand above
# it, priced in kanso#1432's and kanso#1430's entries; nothing here moves them.
#
# 2026-09-15 (kanso#1435): a scan's short tail is one masked load. Six rows
# FALL and none rises, on the base kanso#1433 left: work_livebench
# 3,018,009,215 -> 2,879,379,542 (-138,629,673, -4.5934%), work_encodebench
# 3,567,580,816 -> 3,457,542,341 (-110,038,475, -3.0844%), work_runbench
# 1,904,577,350 -> 1,871,522,584 (-33,054,766, -1.7356%), work_jsonbench
# 1,187,809,626 -> 1,185,398,676 (-2,410,950, -0.2030%), work_scanbench
# 500,829,907 -> 500,327,419 (-502,488, -0.1003%), work_oneshot 18,729,910 ->
# 18,367,303 (-362,607, -1.9360%). The other eight hold to the instruction:
# they never reach a scanner's tail. The container had read runbench
# -30,700,390 and jsonbench +599,400; the runner reads both deeper, and the
# two encode programs, which the container did not measure, fall furthest:
# an escape scan's runs between specials are short, and every one of them
# ended in the byte walk. Against main, work_escapebench 85,995,606 and
# work_digestbench 9,838,994 still stand above it, priced in kanso#1432's
# and kanso#1430's entries; nothing here moves them.
#
# 2026-09-15 (kanso#1436): the counters a shipped binary was still counting.
# All fourteen rows FALL, on the base kanso#1435 left: work_jsonbench
# 1,185,398,676 -> 1,169,790,503 (-15,608,173, -1.3167%), work_runbench
# 1,871,522,584 -> 1,857,535,269 (-13,987,315, -0.7474%), work_livebench
# -6,326,619 (-0.2197%), work_encodebench -5,013,110 (-0.1450%),
# work_escapebench -1,215,014 (-1.4129%), work_deepbench -965,492
# (-0.2716%), and the other eight by between 232 and 128,315. Forty-four
# counter sites left the shipped binary: an increment, sometimes a
# load-store pair, on every beat iteration, every find2 call and every
# thunk escape. The container had read runbench -13,704,939 and jsonbench
# -14,902,123 for the two commits together; the runner reads both a little
# deeper. Against main, work_escapebench 84,780,592 and work_digestbench
# 9,830,211 still stand above it, priced in kanso#1432's and kanso#1430's
# entries; both fall here.
#
# 2026-09-15 (kanso#1437): a capture is a load, an own-err check is a tag test.
# Twelve rows FALL and two hold, on the base kanso#1436 left: work_jsonbench
# 1,169,790,503 -> 1,130,225,294 (-39,565,209, -3.3822%), work_scanbench
# 500,324,263 -> 460,784,763 (-39,539,500, -7.9028%), work_runbench
# 1,857,535,269 -> 1,823,669,249 (-33,866,020, -1.8232%), work_deepbench
# -9,376,000 (-2.6448%), work_widebench -448,019 (-1.3750%), work_oneshot
# -263,777 (-1.4455%), work_encodebench -305,191, work_livebench -263,777,
# work_basket -20,000, work_pendbench -8,400, work_readbench -9,
# work_digestbench -8; work_escapebench and work_indexbench hold, having no
# capture reads and no own-err checks on their paths. A capture read is a
# load instead of a three-instruction call, and the own-err check tests the
# tag in the caller and calls out only for an err. The container had read
# runbench -31,071,965 and jsonbench -34,038,770; the runner reads both
# deeper. Against main, work_escapebench 84,780,592 and work_digestbench
# 9,830,203 still stand above it, priced in kanso#1432's and kanso#1430's
# entries; nothing here moves them.
#
jsonbench 1130225294
encodebench 3452224040
oneshot 17983841
basket 33549560
widebench 32135929
deepbench 345129236
escapebench 84780592
pendbench 208137021
indexbench 2895708
scanbench 460784763
digestbench 9830203
readbench 4629430
livebench 2872789146
# 2026-09-06 (the run program joins): `runbench 3043743748` is a NEW row and
# nothing regressed to make it appear. It is the program Clay's gavel that day
# made the objective's whole run-speed term, and the thirteen rows above it
# became diagnostics.
#
# The row below is CI's, harvested from the job that refused the container's.
# The first push carried 3,043,742,734, counted where the program was written:
# a container whose glibc is 2.39-0ubuntu8.7 against this file's
# 2.39-0ubuntu8.8. CI read 3,043,743,748, a difference of 1,014 instructions or
# 0.00003%, and every other row in the same sitting was byte-identical to its
# golden -- which is the shape host divergence takes here and why host_gate.sh
# refuses the comparison rather than the measurement. The mix percentages in
# bench/runbench's header are the container's and move by less than this
# difference; they are not re-derived.
# 2026-09-10 (the runner image moves, kanso#1361): measured-on takes
# glibc=2.39-0ubuntu8.9. deepbench 389,214,232 -> 389,214,251, a rise of 19
# (+0.000005%); runbench 2,369,917,628 -> 2,369,917,611, a fall of 17
# (-0.000001%). The other twelve rows are byte-identical.
#
# Neither is a change in the work, and that is measured rather than argued.
# ubuntu-latest took the 8.8 -> 8.9 revision between 14:21Z and 14:32Z today.
# #1361's round one ran on 8.8 and this vein compared GREEN on it, against the
# rows above; round four ran the same commit content on 8.9 and read the two
# rows below. Same clang 19.1.1, same rustc 1.98.1, same benchmark sources. The
# whole of the difference is the revision, and #1361 moves this vein by nothing.
#
# It is the shape the runbench note above already records for 8.7 -> 8.8, where
# one row moved 1,014 and the rest did not, and it is smaller. What a revision
# costs is a property of the revision: the header's warning that a toolchain
# bump looks like an enormous regression is about UPSTREAM releases, and the two
# ubuntu revisions measured so far have each moved one or two rows by tens of
# instructions. The four hundred the header quotes is a different pair and a
# different row set and is not re-derived here. The gate refuses either way, and
# the refusal is why both of these were read rather than assumed.
#
# Three compile veins moved their host lines with this one and their values did
# not move at all: compile_instructions 48,866,385, entry_instructions
# 162,751,831 and library_instructions 163,473,663 were each counted once on
# each revision and read the same both times.
# 2026-09-10 (kanso#1362): the backends build the partial over a value. Ten of
# the fourteen rows FALL and none rises. work_runbench 2,369,917,611 ->
# 2,369,679,773 (-237,838, -0.0100%), work_deepbench 389,214,251 ->
# 387,470,247 (-1,744,004, -0.4481%), work_widebench 35,332,240 -> 35,268,228
# (-64,012, -0.1812%), work_pendbench 590,971,748 -> 590,970,940 (-808), and a
# uniform -8 on work_jsonbench, work_encodebench, work_oneshot,
# work_digestbench, work_readbench and work_livebench. work_basket,
# work_escapebench, work_indexbench and work_scanbench are byte-identical.
#
# The change adds a branch in front of every k_call0..4 arity test, so the
# naive expectation was a rise. What actually moved is the emitter's inlined
# fast arm, which now tests `arity == n` before it calls rather than after: the
# partial can never reach a body through it, and the test it replaced was the
# more expensive one. deepbench and widebench build the deepest and widest
# structures and call hardest, which is why they carry most of the fall.
#
# The uniform -8 across six rows is one instruction on a path each program
# takes eight times, and the four flat rows never take it at all.
runbench 1823669249
