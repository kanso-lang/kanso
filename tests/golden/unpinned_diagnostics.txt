# Diagnostics the compiler can raise that no golden pins.
#
# The error corpus is the only place a message's exact text is checked, so an
# unpinned one can be reworded, weakened or lost with nothing going red. These
# are the ones that had accumulated when the gate was written; they are
# tolerated so the gate could be turned on at all.
#
# The list only shrinks. Adding a diagnostic means adding a golden under
# tests/golden/errors — scripts/diagnostic_coverage.py fails on any message
# that is neither pinned nor listed here, and fails again when a listed one
# becomes pinned, so the line gets deleted rather than left lying.
`else` opens a branch: indent its statements beneath it
a field annotation binds tight: `name:type`
a field is written by assignment inside a `build` block: `target.field = value`
a map pair is tight: `key:value`
a re-export is `pub name` or `pub theirs:yours`
a rename is theirs:yours
a rename is tight: `field:name`
a single-expression constant is written inline: `
a value with no parameters is a constant: `
a whole module re-exports by its own name; rename exports one at a time
an `if` block header is `if condition`
an entry file needs at least one statement
an import ends with its path string
an import path is a plain string
an inline constant has no indented block
an upcast binds tight: `(expr):type`
canonical form requires at most one space here
expected a constant name
expected a top-level declaration (`fn` or `type`)
map literal keys are literals; build dynamic maps with `put`
no import offers a pub `
nothing follows the final `>>` wall
nothing to sequence: a `>>` wall needs statements above it
private type `
type ascription is tight: `
type ascription is tight: `_:type`
typeset members appear in alphabetical order
unterminated escape

# Widening the gate found these. The scan it replaced stopped a message at the
# first comma a lowercase word followed, which for a message that holds such a
# comma left the quote unclosed and the call site invisible — twenty-four of
# them. Eleven now have goldens. Of the three left, the entry-file rule needs
# a directory rather than a file and so is pinned by a test instead; the other
# two have resisted every program tried, because a different check answers
# first every time — which may mean they cannot be reached at all.
an entry file holds statements only; definitions live in
only `if` and `build` open an indented block; other calls take
unused expression: every non-final line binds a name (sequence
