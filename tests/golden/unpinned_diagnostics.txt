# Diagnostics the compiler can raise that no golden pins.
#
# The error corpus is the only place a message's exact text is checked, so an
# unpinned one can be reworded, weakened or lost with nothing going red. These
# are the ones that had accumulated when the gate was written; they are
# tolerated so the gate could be turned on at all.
#
# The list only shrinks. Adding a diagnostic means adding a golden under
# tests/golden/errors — scripts/diagnostic_coverage.py fails on any message
# that is neither pinned nor listed here, and fails again when a listed one
# becomes pinned, so the line gets deleted rather than left lying.
an inline constant has no indented block
expected a constant name
expected a top-level declaration (`fn` or `type`)
private type `

# Widening the gate found these. The scan it replaced stopped a message at the
# first comma a lowercase word followed, which for a message that holds such a
# comma left the quote unclosed and the call site invisible — twenty-four of
# them. Twelve now have goldens or a test. The two below are different: no
# program can reach either, and the mechanism is named here so nobody spends
# another sitting writing programs for them. Neither is deleted, because both
# arms are the safe side of a fork — see task #69.
#
# The other four are each answered first by a check added later, verified by
# trying them rather than by reading. An inline constant never reaches the
# parser carrying a block: the lexer folds an indented line into its head
# unless that head opens one, and a `>>` or `.` continuation is legal, so the
# shape does not exist. A non-identifier in a constant's name position is
# refused first by "a top-level line must begin with `fn`, `type`, or a
# constant binding". An indented line at the top level is refused first by the
# indentation rule or the blank-line rule. And the private-type message fires
# on a leading underscore, which the naming rule retired.
#
# That is the pattern worth naming: a diagnostic can be shadowed by a better
# one added later, and nobody notices, because it simply stops appearing.
#
# The indented-block rule cannot fire because the lexer decides, before the
# parser sees anything, which indented lines stay lines. `is_block_header`
# keeps a deeper line only under `if`, `x = if`, `else`, `build` or
# `x = build`; under anything else the line is folded into its head as one
# more argument. So `parse_block_construct` is only ever handed a head it
# already accepts, and `else` is taken by its callers first.
#
# The unused-expression rule cannot fire because the parser handles both
# halves of it. `reject_never_effect` refuses a non-final literal, list, map,
# lambda or binary operation with its own message, and consecutive expression
# statements are folded into one `Seq`, so a body never holds a non-final
# `Stmt::Expr` for the checker to complain about.
only `if` and `build` open an indented block; other calls take
unused expression: every non-final line binds a name (sequence
