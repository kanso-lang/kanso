type node
  id
  peers

pub play =
  build
    a = node "a" _
    b = node "b" [a]
    a.peers = [b]
  graph = [a b]
  build
    base = node "seed" _
    base.peers = [base]
  print "{graph}"
  >> print "{base}"
