type node
  id
  peers

pub play =
  build
    a = node "a" []
    b = node "b" [a]
    a.peers = [b]
  graph = [a b]
  build
    base = node "seed" []
    base.peers = [base]
  print "{graph}"
  >> print "{base}"
