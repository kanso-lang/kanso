type node
  id:string
  peers:list

pub play =
  graph = build
    a = node "a" []
    b = node "b" [a]
    a.peers = [b]
    [a b]
  ring = build
    base = node "seed" []
    base.peers = [base]
    base
  print "{graph}"
  >> print "{ring}"
