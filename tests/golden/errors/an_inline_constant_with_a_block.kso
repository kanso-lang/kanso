# A branch that binds names takes the block form; branches that only answer an
# expression stay in the expression form, and the compiler holds that boundary
# in both directions. This is the second half, and it reads the same whether or
# not the file around it holds declarations.
two = if (1 < 2)
  "a"
else
  "b"

pub play = print "{two}"
