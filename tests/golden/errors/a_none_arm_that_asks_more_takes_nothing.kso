# `both none none` answers a none only beside another, so `both none 2`
# hands its second arm the none, and `shown` has no arm for it.
fn both none none
  1

fn both x _
  x

fn shown 1
  "one"

pub play = print (shown (both none 2))
