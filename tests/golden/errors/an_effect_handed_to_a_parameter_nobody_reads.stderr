error[effect]: `ignored` reads none of what it is handed here, so this effect never happens — hand it back, or give `ignored` a name for it
  --> an_effect_handed_to_a_parameter_nobody_reads.kso:14:16
  14 |   r = ignored (io/write "this write never happens\n")
                      ^
