# Only `if` and `build` open a block. A constant whose head is anything else
# holds its whole value on the line, so an indented run beneath it is not a
# body the language has a meaning for.
pub two = [1]
  2

pub play = print "{two}"
