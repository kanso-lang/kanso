import "std/text"

fn grade outcome
  "grade {outcome}"

fn parse text
  text/to_int text

pub play =
  outcome = parse "oops"
  verdict = grade outcome
  report verdict

fn report verdict
  "report: {verdict}"
