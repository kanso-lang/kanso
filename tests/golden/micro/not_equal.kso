# `!=` shares its first character with the strict-index sigil, so the two are
# written next to each other here: a `!` only ends an index when an `=` does
# not follow it.
xs = [1 2 3]

print "{1 != 2}"
  .> (_ -> print "{"a" != "a"}")
  .> (_ -> print "{xs[1] != 2}")
  .> (_ -> xs[1] .> (v -> print "{v == 1}"))
  .> (_ -> print "{[1 2] != [1 3]}")
