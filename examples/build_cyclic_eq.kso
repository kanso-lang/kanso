type node
  id
  peer

fn ring_of first second
  build
    a = node first 0
    b = node second 0
    a.peer = b
    b.peer = a
  a

ring1 = ring_of 1 2

twin = ring_of 1 2

other = ring_of 1 99

print "{ring1 == twin}"
>> print "{ring1 == other}"
>> print "{ring1 == ring1}"
