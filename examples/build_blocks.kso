type node
  id
  peers

fn pair_of first second
  build
    a = node first _
    b = node second [a]
    a.peers = [b]
  [a b]

fn ring_of name
  build
    base = node name _
    base.peers = [base]
  base

graph = pair_of "a" "b"

ring = ring_of "seed"

print "{graph}"
>> print "{ring}"
